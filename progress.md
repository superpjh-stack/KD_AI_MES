# progress.md — 경동글로벌텍 제조AI (SF26179182)

**검증된 현황의 유일한 진실.** 실측값 + 검증 명령만 적는다. 명령 없이 적힌 숫자는 없는 것으로 친다.
판정 기준은 `goal.md` §2. 결정은 `decisions.md`.

---

## 2026-09-11 회전 1 — Phase 0 (아키텍트 단독 · 오케스트레이터 직접 수행)

`/loop goal.md` 동적 모드 1회전. 착수 시 이 폴더에는 `goal.md` `CLAUDE.md` 뿐이었다.
**§2 게이트를 측정할 수단이 없었으므로 회전 1의 목표를 "측정 가능 상태 만들기" 로 잡았다**(§10-15).
에이전트는 띄우지 않았다 — Phase 0 은 웨이브 A 앞이고 혼자 하는 일이다.

### 한 일

| 항목 | 실측 | 검증 방법 |
|---|---|---|
| 디렉터리 골격 | `docs/{design,산출물,cad}` `src/kyungdong/app/{routers,templates,util,static}` `db` `tools` `tests` `contracts` `work` `outputs` 생성 | `ls -R` |
| 정본 반입 | design.json 434KB · analysis.json · 사업계획서.pdf 3.1MB · 산출내역서 v0.97 · 화면설명서 HTML + 화면정의서 PPT 11MB · CAD 자료 6종 1.4MB | `du -sh docs/*` |
| **정본 재실측 (복사본 기준)** | **화면 45 · 영역 10 · 프로그램 49 · 테이블 68 · 컬럼 762 · 공통 4 · 역할 6 / 기능 45 · 비기능 13** — goal.md 표와 일치 | `uv run python -c "…"` (goal.md §9) |
| 테이블 접두 | `BAS`3 `SYS`4 `INV`6 `EST`16 `SHP`6 `PRC`7 `DAT`11 `AGT`3 `KPI`2 `IF`10 = **68** | 같은 명령 |
| DB | `kyungdong_db` 생성 · `vector` **0.8.6** 확장 설치 | `psql -d kyungdong_db -Atc "select extversion from pg_extension where extname='vector'"` |
| uv 프로젝트 | Python 3.12 핀 · `uv sync` 완료 · fastapi·psycopg·xgboost 3.4.1·shap·sklearn·pandas·numpy·jinja2 임포트 확인 | `uv run python -c "import …;print('deps ok')"` |
| `.env.example` | `KYUNGDONG_*` 26키. 가설값에 `(D-nn)` 주석을 달았다 | `wc -l .env.example` = 44 |
| `decisions.md` | goal.md §7 의 **D-01~D-27 전건 이관** + 회전 1 발견 **D-28~D-36** | 파일 |

### 스키마 생성 — `tools/gen_schema.py` → `db/schema.sql`

| 항목 | 실측 | 검증 방법 |
|---|---|---|
| **G-01 테이블** | **68** ✅ **PASS** | `psql -d kyungdong_db -Atc "select count(*) from information_schema.tables where table_schema='public'"` |
| **G-02 컬럼** | **762** ✅ **PASS** | `select count(*) from information_schema.columns where table_schema='public'` |
| FK 제약 | **98** 생성 (TD5 FK 표기 128건 − 코드성 참조 29 − 금액 오표기 1) | `select count(*) … constraint_type='FOREIGN KEY'` |
| UNIQUE 제약 | **16** (단일 14 + 범위 복합 2) = TD5 `Unique` 표기 16건과 일치 | `select count(*) … constraint_type='UNIQUE'` |
| pgvector | `AGT_VECTOR_DOCS.EMBEDDING VECTOR(1536)` 적용됨 | schema.sql · `\d AGT_VECTOR_DOCS` |

**TD5 는 내부 정합했다**(D-36) — FK 128건이 전부 실제 68표로 해석되고, PK 는 표당 정확히 1개이며,
컬럼 행 길이 위반 0건, 타입 어휘 전부 유효. 손 수정 없이 생성기만으로 스키마가 나왔다.

### 생성기가 잡아낸 것 (고치지 않고 등재했다)

| ID | 내용 |
|---|---|
| **D-32** | 코드성 FK **29건**이 물리 FK 로 성립하지 않는다 — `VARCHAR(30/50)` 컬럼이 `BAS_COMMON_CODES.CODE_ID BIGSERIAL` 을 가리킨다. 실제 의도는 `(CODE_GROUP, CODE_VALUE)` 참조. **FK 제약 미생성 + 애플리케이션 검증** |
| **D-33** | `EST_QUOTATION_ITEMS.UNIT_PRICE NUMERIC(16,2)` 이 FK='Y' 로 표기됨 — **금액 값 컬럼을 외래키로 적은 결함.** 컬럼을 추가하지 않고(G-02 고정) 제안만 |
| **D-34** | 범위 유니크 2건을 복합 제약으로 — `UNIQUE(CODE_GROUP, CODE_VALUE)` · `UNIQUE(CONFIG_TYPE, CONFIG_KEY)` |
| **D-31** | 임베딩 차원 **1536** 확정 (TD5 명시) → D-08 의 "차원 미정" 해소 |
| **D-35** | KPI 코드 확정 — `LEADTIME_MFG` · `LEADTIME_O2D` (TD5 `KPI_TARGETS.KPI_CODE` 비고) |

### 생성기 자체 결함 2건 — 만들면서 잡았다

1. **쉼표가 주석에 먹혔다.** `",\n".join()` 이 `-- 한글명,` 을 만들어 `--` 가 쉼표를 삼켰다 → `psql` 이 두 번째 컬럼에서 구문 오류. 쉼표를 **주석 앞**에 두도록 고쳤다. 이것 때문에 첫 적용이 **테이블 0개**로 끝났다.
2. **범위 유니크를 단일 유니크로 만들 뻔했다.** 비고에 `Unique` 가 있으면 무조건 `UNIQUE` 를 붙이던 로직이 `CODE_VALUE`(그룹 내)·`CONFIG_KEY`(구분 내)에 **틀린 제약**을 걸었다. `SCOPED_UNIQUE` 표에 **명시**하는 방식으로 바꿨다(추측 금지).

### 게이트 판정기 — `tools/gate.py` + `Makefile`

`make gate` 첫 실행 결과:

```
G-01 테이블 68                 PASS   68
G-02 컬럼 762                  PASS   762
정본  design/analysis = goal.md 표  PASS   화면45 영역10 프로그램49 표68 컬럼762 기능45 비기능13
G-03 / G-04 / G-05 / G-06      미구현  (check_routes·check_trace·nav.py 미작성)
G-07~G-11 / G-12~G-13 / G-14~G-25 / G-26~G-30   미구현  (QA 검사기 미작성)
G-빌드 pytest                   미구현  테스트 0파일
──────────────────────────────────────────
PASS 3 · FAIL 0 · 차단 0 · 미구현 9  /  12
```

**`미구현` 은 PASS 가 아니다.** `gate.py` 는 QA 소유 검사기를 있으면 호출하고 없으면 `미구현` 으로 표시하며
**여기서 만들지 않는다**(goal.md §3.4). 빌드 게이트는 "몇 건 실행됐는지" 를 보고 0건이면 `차단` 을 낸다(§10-15).

---

## 지금 해야 할 것 (회전 2 — 웨이브 A 아키텍트)

가장 앞선 FAIL/미구현은 **G-06 → G-03** 이다. 웨이브 A 를 끝내야 개발 3명이 코드를 쓸 수 있다.

1. `app/nav.py` — 10영역 45화면 단일 소스 (`td3.menu_shortcuts` 순서) → **G-06**
2. `app/rbac.py` — TD3 `role_matrix` **6역할 × 8영역**
3. `app/util/` — 오류 규약 10종(§2.5) · 세션 · 속도제한 · 보안헤더 · CSRF
4. `app/templates/{base,login,_placeholder,_popup,_error}.html` — `_placeholder` 가 TD3/TD4/AD2 문장을 200 으로 렌더
5. `tools/check_routes.py` → **G-03**
6. `contracts/` 4종 — `db-schema.md`(D-32·D-33 매핑 포함) · `api-contract.md` · `screen-map.md`(45행 + 담당·소유) · `interfaces.md`
7. D-20 — Agent 시안 도구 11종 ↔ TD5 68표 재배선 매핑표

## 알아둘 것

- **8020 포트를 쓴다.** 8000·8010 은 이웃 사업 서버가 점유 중이다(실측, D-28).
- **`db/schema.sql` 은 손으로 고치지 않는다.** `make gen-schema` 로만 바뀐다.
- **코드성 FK 29건은 DB 가 막아주지 않는다**(D-32). 개발 3명은 코드 값 검증을 애플리케이션에서 해야 한다 — `contracts/interfaces.md` 에 공용 검증 함수를 두는 것이 맞다.
- **`pdftotext` 가 없다**(D-30). 사업계획서는 `uv run --with pypdf` 로 읽는다.
- 이 사업의 AI 는 **CAD 견적 자동화 + RAG Agent** 다. **예지보전·품질예측을 만들면 결함**이다(D-01). 직전 사업(광성정밀)이 예지보전이라 코드를 가져올 때 가장 위험하다.

---

## 2026-09-11 회전 2 — 웨이브 A (아키텍트 · 오케스트레이터 직접 수행)

사용자가 "컴퓨터가 잠들어도 계속" 을 요청했다. 절전은 `caffeinate` 로 막았고(`work/caffeinate.pid`, 12시간),
**뚜껑 닫힘·세션 종료는 막을 수 없다**는 것을 명시하고 `RESUME.md` 로 복귀 비용을 0에 가깝게 만들었다.

### 게이트 실측 — `make gate`

```
G-01 테이블 68                          PASS   68
G-02 컬럼 762                           PASS   762
G-06 메뉴 10영역 단일 소스                 PASS
G-03 화면 45+공통4+오류1 · ph 0 · 용어 0    FAIL   ①200 PASS / ②placeholder 45건 FAIL / ③용어 PASS
정본  design/analysis = goal.md 표        PASS   화면45 영역10 프로그램49 표68 컬럼762 기능45 비기능13
G-04·G-05                              미구현  check_trace.py (QA1)
G-07~G-30                              미구현  check_data·check_ingest·check_ai·check_security (QA2·QA3)
G-빌드 pytest                            PASS   59 passed
──────────────────────────────────────────────────────
PASS 5 · FAIL 1 · 차단 0 · 미구현 6  /  12
```

**G-03 의 FAIL 은 placeholder 45건 하나뿐이다** — 개발 3명이 화면을 채우면 사라진다. 정상적인 진행 상태다.

### 만든 것

| 파일 | 역할 | 실측 검증 |
|---|---|---|
| `app/design.py` | **정본 로더** — `screen/program/requirement/table_def/trace/columns_of`. `selfcheck()` 가 정본 9항목을 goal.md 표와 대조 | 9/9 일치 (`selfcheck()`) |
| `app/nav.py` | **메뉴 단일 소스 (G-06)** — `td3.menu_shortcuts` 순서 10영역, 45화면을 design.json 에서 읽는다(45행 손으로 안 적음) | 영역 10 · 화면 45 · 소유 **개발1 15 / 개발2 16 / 개발3 14** = goal.md §3.2 와 일치 |
| `app/rbac.py` | **6역할 × 8권한영역** — TD3 `role_matrix` 셀 문구를 R/W/A 로 해석. 모르는 문구는 **예외로 터뜨린다** | 현장 작업자 → 수주견적AI관리 = `-` → **403** 확인 |
| `app/settings.py` | `KYUNGDONG_*` 로더. **가설값 11종을 값+D번호+설명 묶음**으로 들고 다녀 화면에 `가설 (D-nn)` 배지를 띄운다 | 11종 출력 확인 · prod 에서 `SESSION_SECRET` 없으면 기동 거부 |
| `app/util/http.py` | **오류 계약 7종** 한 곳 (401·403·422·500·501×2·503) + 200 으로 드러내는 알림 문구 5종 | 계약 밖 key 는 `KeyError` |
| `app/templating.py` | Jinja2 · **autoescape 기본**(§10-8) · 권한 있는 메뉴만 노출 | — |
| `app/main.py` | 45화면 + 공통 4 + 오류. **담당 라우터가 있으면 그것이 이기고**, 없는 화면만 `_placeholder` | 라우트 53 · placeholder 45 · 담당 라우터 0 |
| `app/templates/` 7종 + `static/app.css` | `_placeholder` 가 AD2·TD3·TD4·TD5 **정본 문장과 컬럼표**를 200 으로 렌더. CSS 는 **단일 파일**(§10-13) | 전 화면 200 |
| `tools/check_routes.py` | **G-03·G-06 검사기** | 아래 참조 |
| `tools/gen_screen_map.py` → `contracts/screen-map.md` | 화면 45행 + 소유권 + 모듈별 + 공통 + RBAC 표. **정본은 `nav.py`** | G-06 PASS |
| `tests/test_arch_smoke.py` | 정본 규모·메뉴·소유 3분할·추적 1:1·공통 200·45화면 200·403·메뉴 은닉·오류 계약·미구성 노출 | **59 passed** |

### 검사기·게이트 자체 결함 4건 — 만들면서 잡았다 (전부 §10 이 경고한 유형)

1. **오염 검사가 통째로 무력했다.** `QUOTE_OK` 를 **본문 전체**에서 찾아서, placeholder 의 "산출물 정본 문장이다" 에 걸려 항상 면제됐다. → 용어 **주변 160자**만 보도록 고쳤다(D-37). 고치자마자 숨어 있던 4건이 드러났다.
2. **고친 검사기가 잡은 4건은 오염이 아니었다.** `/dsh/003`·`/prc/022` 가 렌더한 "설비 예지보전 AI는 본 사업 범위 밖이다" · "포밍·용접 설비는 제외한다" 는 **배제 진술**이다. 배제 문맥을 인정하도록 규칙을 정했다(D-37).
3. **빌드 게이트가 `차단` 을 오판했다.** `gate.py` 가 stdout **마지막 줄**을 요약으로 믿었는데 경고 문서 URL 이 뒤에 붙는다 → 요약 줄을 정규식으로 찾도록 고쳤다.
4. **`-qq` 때문에 요약 줄이 아예 없었다.** `pyproject` `addopts = "-q"` 에 명령줄 `-q` 가 더해졌다 → `gate.py` 는 `uv run pytest` 로 부른다.

**둘 다 검사기가 "항상 통과" 하거나 "판정을 못 하는" 결함이었다.** 주입 시험으로 양방향을 확인했다 —
일부러 `프레스 금형 샷카운트` 를 넣으면 3건 FAIL, 빼면 0건 PASS.

### 지금 해야 할 것 (회전 3)

가장 앞선 미구현은 **G-04·G-05**(요구사항 추적)다. 웨이브 A 잔여분을 끝낸 뒤 웨이브 B 로 넘어간다.

1. `contracts/db-schema.md` — 68표·762컬럼 + **D-32 코드성 FK 29건 검증 규약** + D-33 매핑
2. `contracts/api-contract.md` — 45화면 라우트 + 공통 + Agent API + 수집 API + CAD 업로드 API
3. `contracts/interfaces.md` — `db.conn.q/x/tx` · `design.*` · `templating.render` · `util.http` · `ingest`·`cad` 시그니처
4. `db/conn.py` — psycopg 연결·`q/x/tx` (DB 죽으면 **503**, 조용히 빈 배열 금지)
5. D-20 — Agent 시안 도구 11종 ↔ TD5 68표 재배선 매핑표
6. `tools/check_trace.py`(QA1 소유지만 **게이트가 안 서면 아무 판정도 못 한다** — 아키텍트가 최소판을 만들고 QA1 이 인수할지 결정)

### 알아둘 것

- **`?as=OPERATOR` 로 역할을 바꿔 화면을 볼 수 있다**(D-40 가설). 인증이 들어오면 이 미들웨어를 **제거**해야 한다 — 남으면 보안 결함이다.
- **담당 라우터가 placeholder 를 이긴다.** 개발자는 `app/routers/<module>.py` 에 `router` 와 `SCREENS = ("MES-TD3-0nn", …)` 를 두면 된다. main.py 는 건드리지 않는다.
- `contracts/screen-map.md` 는 **생성 파일**이다. `nav.py` 를 고치고 `uv run python tools/gen_screen_map.py` 를 다시 돌린다.
- 게이트 판정을 볼 때 **`미구현` 6건이 PASS 가 아니다.** 현재 진짜 측정된 것은 G-01·G-02·G-03·G-06·빌드뿐이다.

---

## 2026-09-11 회전 3 — 웨이브 A 완료 (아키텍트)

### 게이트 실측 — `make gate`

```
G-01 68 PASS · G-02 762 PASS · G-06 PASS · 정본 PASS · G-빌드 PASS (70 passed)
G-03 FAIL — ①200 PASS / ②placeholder 45 FAIL / ③용어 PASS
G-04·G-05 · G-07~G-30  미구현 (QA 검사기 미작성)
──────────────────────────────────────────
PASS 5 · FAIL 1 · 차단 0 · 미구현 6  /  12   (회전 2와 동일 — 웨이브 A 는 게이트를 올리는 일이 아니라 개발 3명이 코드를 쓸 수 있게 하는 일이다)
```

### 만든 것

| 파일 | 역할 | 실측 검증 |
|---|---|---|
| `db/conn.py` | `q`·`q1`·`x`·`tx`·`alive`. **DB 장애 → 503**(빈 배열 금지) · dict 행 · 파라미터 바인딩 강제 | DSN 을 죽였을 때 **HTTP 503 `db_down`** 확인. 0건은 `None`(정상) |
| `contracts/screen-map.md` | 화면 45행 + 소유권 + 모듈별 + 공통 + RBAC 표 | 생성. G-06 PASS |
| `contracts/db-schema.md` | 68표·762컬럼 · 물리 FK 98 · **코드성 29(D-32)** · 오표기 1(D-33) · 디지털 스레드 · 개인정보 6 · 런타임전용 11 + **미정 6** | 생성 |
| `contracts/api-contract.md` | 45화면 라우트 + 공통 5 + EIF 4 + 오류 7종 + Agent API + **승인 필요 쓰기 6종** | 생성 |
| `contracts/interfaces.md` | 공용 시그니처 9절. **아직 없는 것 4종**(`validate_code`·`mask`·`anchor`·`audit`)도 시그니처를 먼저 공표 | 손으로 작성 |
| `tools/gen_{screen_map,db_contract,api_contract}.py` · `make contracts` | 계약 3종 생성기 | 재생성 무변화 테스트로 잠금 |
| `tests/test_arch_contracts.py` | 계약 드리프트 · DB 503 · 0건 None · 바인딩 · D-32 전건 등재 · 런타임전용 미정 구분 | **70 passed** (회전 2 59 → +11) |

### 실측으로 확인한 것

| 항목 | 실측 |
|---|---|
| 물리 FK | **98** (TD5 FK 표기 128 − 코드성 29 − 오표기 1) |
| **코드성 FK (D-32)** | **29건** — `VARCHAR` 컬럼이 `BAS_COMMON_CODES.CODE_ID BIGSERIAL` 을 가리킨다. **DB 가 막지 않으므로 애플리케이션 검증 필수.** 계약 §3 에 전건 등재(테스트로 잠금) |
| 개인정보 (G-29) | **6컬럼** — 암호화 저장 + 화면 마스킹 |
| 런타임 전용 (G-11) | **확실한 것 11표**만 계약에 넣었다 |
| 시드 여부 **미정 6표** | `INV_MATERIAL_HISTORY` · `PRC_PROCESS_HISTORIES` · `PRC_CONDITION_DEVIATIONS` · `EST_SHAP_FACTORS` · `INV_SUPPLIER_QUALITY` · `SHP_CLAIM_CAUSES` — **담당 개발자가 판정한다** |

### 이번에도 짐작을 계약에 넣을 뻔했다

런타임 전용 표를 **이름 휴리스틱**(`_HISTORY`·`_DEVIATIONS`)으로 뽑았더니 `INV_MATERIAL_HISTORY`(원자재 이력)·
`PRC_CONDITION_DEVIATIONS`(작업조건 편차)까지 "0건이 정상" 으로 분류됐다. **시드 대상 업무 데이터일 수 있다.**
G-11 판정이 여기에 걸려 있으므로 **확실한 것(로그·버퍼)만 계약에 넣고 나머지 6표는 §7.1 '미정' 으로 넘겼다.**
담당 개발자가 판정해 옮긴다.

### 새 산출물 결함 — D-41

**외주 발주 관리 항목이 없다.** TD1 `process_steps` 는 소재가공(외주)·버핑(외주)의 데이터로
"외주 발주번호, 자재 LOT, 반출·반입일, 외주처, 수량" 을 적었는데 TD5 에 **외주 발주번호·외주처 컬럼이 없다.**
구매 발주(PO) 테이블도 없고 `EST_MATERIAL_REQS.PO_NEED_YN` 뿐이다.
→ **컬럼을 추가하지 않고**(G-02 762 고정) 있는 것으로 소화한다: 반출·반입은 `PRC_PROCESS_HISTORIES.OUTSOURCE_STEP` ·
`INV_MATERIAL_HISTORY.HIST_TYPE`, 외주처는 `INV_SUPPLIERS(SUPPLY_TYPE='외주')`, 발주번호는 `PRC_WORK_ORDERS.WORK_ORDER_NO`.
화면에 `대체 표기 (D-41)` 을 붙이고 정규 컬럼 신설을 제안한다.

### D-20 Agent 도구 재배선 확정

시안 11종을 TD5 68표에 전부 매핑했다(decisions.md D-20 표). 시안 `purchase_orders` 만 대응표가 없어 D-41 로 갔다.
**프롬프트 7~10번(검색하지 않은 문서를 지어내지 않는다)·5줄 형식·`근거:` 줄·승인 명시**를 이식하고,
SQLite 스키마·`demo_data`·데모 예측 상수·Streamlit·`voice.py` 는 버린다.

### 지금 해야 할 것 (회전 4)

**웨이브 A 는 끝났다. 다음은 웨이브 B(개발 3명 병렬)이고 여기서 토큰이 크게 는다 — 사용자 확인을 받는다.**

확인을 기다리는 동안 아키텍트가 혼자 할 수 있는 잔여분을 한다(에이전트 0):

1. `app/util/` 공용 4종 — `validate_code()`(D-32 코드성 FK 검증) · `mask()`(G-29) · `anchor()`(§10-3 시간 앵커) · `audit()`(G-29 `SYS_ACCESS_LOGS`)
2. `db/seed.py` 뼈대 — 시간 앵커 발급 · 역할 6 · 계정 난수 1회 출력 · `BAS_COMMON_CODES` 10공정 + 제품군 5종
3. `app/util/` 세션·속도제한·보안헤더·CSRF (§10-12)

### 알아둘 것

- **계약 3종은 생성 파일이다**(D-43). 손으로 고치면 `test_계약을_다시_뽑아도_같다` 가 잡는다. `make contracts` 로 갱신한다.
- **`tools/check_trace.py` 는 QA1 몫이다**(D-42). 아키텍트가 대신 만들지 않는다 — G-04·G-05 는 QA1 이 올 때까지 `미구현` 이고 **`미구현`은 PASS 가 아니다**. 추적 1:1 자체는 스모크 테스트가 이미 단언한다.
- 개발자는 `main.py` 를 건드리지 않는다. 자기 `app/routers/<module>.py` 에 `router` 와 `SCREENS = (...)` 만 두면 placeholder 에서 빠진다.

---

## 2026-09-11 회전 4 — 공용 모듈·공통 시드 (아키텍트 · 에이전트 0)

웨이브 B 는 사용자 확인 대기 중이라 아키텍트 잔여분만 했다.

### 게이트 — `make gate`

```
PASS 5 · FAIL 1 · 차단 0 · 미구현 6  /  12      G-빌드 88 passed (70 → +18)
```

### 만든 것

| 파일 | 역할 | 실측 검증 |
|---|---|---|
| `app/util/codes.py` | **D-32 코드성 FK 검증** — `validate_code` · `require_code`(위반 시 **422**) · `code_group_for`. 29컬럼 → 코드 그룹 매핑을 **명시**(추측 아님) | 매핑 **29/29**, 미지정 **0**. `require_code` 위반 시 422 확인 |
| `app/util/pii.py` | **G-29 마스킹** — name·phone·email·generic | `홍길동`→`홍**` · `010-1234-5678`→`010-****-5678` · `so*****@example.com` |
| `app/util/clock.py` | **시간 앵커**(§10-3). 앵커가 없으면 **터진다** — 조용히 오늘 날짜로 대체하지 않는다 | 재실행해도 앵커 불변 확인 |
| `app/util/audit.py` | **G-29 감사추적** `SYS_ACCESS_LOGS`. 기록 실패를 삼키지 않는다 | LOG_TYPE 은 TD5 4종만 허용(위반 시 예외) |
| `db/seed.py` | 시간 앵커 · 역할 **6×10=60행** · 계정 6 · 공통코드 25 | **3회 연속 exit 0 · 행 수 diff 0 · 비밀번호 불변** |
| `tests/test_arch_seed.py` | G-07 멱등 · §10-11 비밀번호 · 앵커 고정 · RBAC DB 반영 · 실명 없음 · 빈 그룹 · D-32 검증 · 마스킹 | **88 passed** (70 → +18) |

### 실측으로 확인한 것

| 항목 | 실측 |
|---|---|
| `SYS_ROLE_PERMISSIONS.AREA_CODE` | TD5 비고가 정한 `DSH/INV/EST/SHP/PRC/BAS/DAT/AGT/KPI/SYS` **10코드가 `nav.py` 라우트 접두와 정확히 같다** — 정본이 접두 선택을 확인해 줬다 |
| 역할·권한 | **60행** (6역할 × 10영역). 현장 작업자 `EST` 는 `READ_YN='N'` — TD3 셀 `-` 가 DB 까지 내려왔다(G-28) |
| 채운 코드 그룹 | **6** — 공정 10(레이저커팅만 자동 수집) · 제품군 5 · 검사구분 3(수압·기밀·진공) · 공급구분 3 · 외주구간 2 · 설비 2(수집 지점 2개소) |
| **비운 코드 그룹** | **7** — 품목 · 재질 · 고객사 · 보관위치 · 불량유형 · 클레임유형 · 원가대상. **정본에 값이 없어 지어내지 않았다**(D-47). 테스트가 "비어 있음" 을 단언한다 |

### 실물로 터진 산출물 결함 — D-44

시드 재실행이 **`ForeignKeyViolation` 으로 죽었다**:

```
update or delete on table "sys_role_permissions" violates foreign key constraint "fk_sys_users_role_id"
DETAIL:  Key (role_perm_id)=(1) is still referenced from table "sys_users".
```

원인은 TD5 설계다 — `SYS_USERS.ROLE_ID` 가 `SYS_ROLE_PERMISSIONS.ROLE_PERM_ID` 를 가리키는데
그 표는 **역할 × 영역(× 화면) 조합마다 한 행**이다. 사용자가 "역할" 이 아니라 **권한 행 하나**를 가리킨다.

→ **컬럼을 추가하지 않고**(G-02 762 고정) 두 가지로 풀었다.
1. 시드는 `ROLE_ID` 에 **그 역할의 대표 행(최소 `ROLE_PERM_ID`)** 을 넣고, 권한 판정은 `ROLE_CODE` 조인으로 한다.
2. **물리 유니크**(D-45)를 걸어 시드를 `delete+insert` 에서 **upsert** 로 바꿨다 —
   `UNIQUE NULLS NOT DISTINCT (ROLE_CODE, AREA_CODE, SCREEN_ID)`. 근거는 TD5 criteria
   "물리 인덱스·파티션·실제 제약명은 **구현 단계에서 확정**한다" 다. 컬럼은 그대로 762 다.

역할 마스터 분리를 도입기업에 제안한다.

### 또 하나 — D-46 모듈명 함정

`util/anchor.py` 의 모듈명과 `anchor()` 함수명이 겹쳐 `from util import anchor` 가 **함수로 해석**돼
`anchor.CONFIG_TYPE` 이 `AttributeError` 로 터졌다. 개발 3명이 똑같이 걸릴 함정이라
모듈을 **`util/clock.py`** 로 바꿨다. 함수명은 `anchor()` 그대로다.

### 지금 해야 할 것 (회전 5)

**웨이브 A 는 완전히 끝났다. 남은 것은 전부 웨이브 B·C 다 — 사용자 확인이 필요하다.**

확인이 오면: 개발1(입고·기준·시스템·데이터 15화면) · 개발2(대시보드·공정·출하·KPI 16화면) ·
개발3(AI 14화면) 을 §5 프롬프트로 기동한다. **ML 학습이 있는 개발3 회전은 단독**(§10-2).

확인이 없으면 아키텍트가 할 수 있는 것이 거의 없다. 남은 것:
1. `app/util/` 세션·속도제한(계정 5회/IP 20회)·보안헤더·CSP(외부 CDN 0)·CSRF (§10-12)
2. `app/auth.py` 골격 — 단 인증 본체는 개발1 몫이라 **시그니처만** 두고 넘긴다

### 알아둘 것

- **시드 계정 6개는 직무 계정**이다(`exec` `quality` `prod` `operator` `supplier` `admin`). 실명 없음(D-39).
  비밀번호는 **최초 1회만 출력**되고 재실행해도 바뀌지 않는다 — 잃어버리면 계정을 지우고 다시 시드한다.
- **`require_code()` 를 저장 전에 부른다**(D-32). DB 가 막아주지 않는 29컬럼이다.
- **`clock.anchor()` 를 쓴다.** `date.today()` 를 쓰면 §10-3 사고가 재현된다.
- 비운 코드 그룹 7종(D-47)에 값을 넣고 싶으면 **정본 근거를 먼저 찾는다.** 없으면 화면 입력으로 둔다.

---

## 2026-09-11 회전 5 — 보안 기반 (아키텍트 · 에이전트 0)

웨이브 B 확인 대기 중. §3.1 아키텍트 범위의 마지막 항목(세션·속도제한·보안헤더·CSRF 기반)을 했다.

### 게이트

```
PASS 5 · FAIL 1 · 차단 0 · 미구현 6  /  12      G-빌드 105 passed (88 → +17)
```

### 만든 것 — 직전 사업 사고를 처음부터 막는다(§10-12)

| 파일 | 역할 | 실측 검증 |
|---|---|---|
| `app/util/security.py` | 보안 헤더 6종 + **CSP `default-src 'self'`**. HSTS 는 **prod 에서만** | 렌더 결과에 `http://`·`https://` **0건** 단언 |
| `app/util/session.py` | 서명 쿠키 + **서버측 무효화** · 유휴 만료 · `destroy_user()`(계정 잠금 시 전 세션 차단) | 지운 세션의 쿠키가 **더 이상 안 먹힘** 확인 · 위조 쿠키 거부 |
| `app/util/ratelimit.py` | 계정 5회 / IP 20회 (10분 창). 성공 시 해제 | 계정·IP 한도가 **서로 독립**임 확인 |
| `main.py` 미들웨어 | 세션 → 역할. **prod 에서는 개발용 전환을 받지 않는다** | `prod` + `?as=SYSADMIN` → **403** 확인 |

| 헤더 | 값 |
|---|---|
| Content-Security-Policy | `default-src 'self'; script-src 'self'; style-src 'self' 'unsafe-inline'; img-src 'self' data:; font-src 'self'; connect-src 'self'; frame-ancestors 'none'; base-uri 'self'; form-action 'self'` |
| X-Content-Type-Options / X-Frame-Options / Referrer-Policy / Cache-Control | `nosniff` / `DENY` / `same-origin` / `no-store` |

### 판단한 것

- **세션 테이블을 만들지 않았다**(D-48). TD5 68표에 없다 → G-01 은 68 고정이다. 저장소는 프로세스 메모리이고, **재기동하면 세션이 끊긴다**는 한계를 적어 두었다. 다중 워커로 가면 공유 저장소가 필요하다(사업계획서 4.7 에 ElastiCache 가 있다).
- **속도 제한 수치는 사업계획서에 없다**(D-49). 9.2 는 "비밀번호 복잡도·주기적 변경" 까지만 적었다. 직전 사업 교훈에서 온 값이므로 `가설 (D-16)` 배지를 화면 문구에 넣었다 — 목표가 없는 것을 있는 척하지 않는다.
- 잠금 문구가 `(가설 D-16)` 으로 나가 앱 전체 배지 형식 `가설 (D-16)` 과 어긋났다. 테스트가 잡아서 **`settings.Hypothesis.badge` 한 곳을 쓰도록** 통일했다.

### 지금 해야 할 것 (회전 6)

**아키텍트 범위(§3.1)가 끝났다.** `contracts/` 4종 · 스키마 생성기 · 공용 모듈 · RBAC · 오류 규약 ·
감사로그 훅 · `check_routes.py` · `gate.py` 가 전부 있다. **남은 것은 전부 웨이브 B·C 다.**

- 웨이브 B — 개발1(입고·기준·시스템·데이터 15화면) · 개발2(대시보드·공정·출하·KPI 16화면) · 개발3(AI 14화면)
- 웨이브 C — QA1(기능·계약) · QA2(데이터·수집) · QA3(AI·보안)

**사용자 확인 없이는 더 진행할 수 없다.** 다음 회전에도 확인이 없으면 루프를 멈추고 대기 상태로 보고한다 —
빈 회전을 반복하는 것은 토큰 낭비다.

### 알아둘 것

- **인증을 넣을 때(개발1) `main.py` 의 개발용 역할 전환 분기를 지운다**(D-40·D-51). prod 는 이미 막혀 있지만 dev 분기가 남아 있다.
- **외부 리소스를 하나라도 참조하면 테스트가 깨진다**(D-50). 폰트·아이콘·차트 라이브러리 전부 인라인이다.
- 세션은 **재기동하면 끊긴다**(D-48). 개발 중 로그인이 풀리면 버그가 아니다.

---

## 2026-09-11 회전 6 — 웨이브 B 기동 · 스톨 오판 (오케스트레이터)

사용자 승인으로 **개발 3명을 동시 기동**했다(§10-2 "3~4명까지"). **개발3 의 ML 학습(R1-b)은 제외** —
학습이 있는 회전은 단독으로 돌려야 API 세션 한도를 치지 않는다. 견적·BOM·SHAP 은 화면·데이터 구조까지만.

각 프롬프트에 박은 것: 읽을 파일 **3개 제한**(§10-9 스톨 4회가 전부 문서 읽기 단계) · 예지보전 금지(D-01) ·
타 사업 용어 4계열 금지 · **수치를 만들지 말고 `차단`으로 남겨라**(§10-18) · **건수 카드 `0 건` 은 정답**(§10-14) ·
`main.py` 금지 + `SCREENS` 튜플 규약.

### 오케스트레이터가 스톨을 오판할 뻔했다 — 새 교훈

폴백 감시 틱에서 `find . -newermt '-10 minutes'` 로 변경 파일을 셌더니 **0건**이 나왔다.
§10-9 의 스톨 신호와 똑같아 보여 **에이전트 3명을 죽이고 재기동할 뻔했다.**

**원인은 명령이 틀린 것이다.** macOS BSD `find` 는 `-newermt` 에 `'-10 minutes'` 같은 **상대 표현을 받지 않는다.**
오류를 내지 않고 **조용히 0건**을 돌려준다. 실제로는 세 명 다 13:35~13:59 사이에 라우터 10종·템플릿 7디렉터리·
`app/kpi.py`·`seed_dev1/2`·테스트 3종·RAG `sample_docs` 를 쓰고 있었다.

| 판정 근거 | 결과 |
|---|---|
| `find -newermt '-10 minutes'` (틀림) | 0건 → "스톨" |
| `find -newermt "$(date -v-15M '+%Y-%m-%d %H:%M:%S')"` (맞음) | **20건 이상** |
| 트랜스크립트 크기 30초 증분 | 두 명 +32KB·+11KB, 한 명 0 (API 호출 중) |

**교훈**: 스톨 판정은 **두 가지 독립 신호**로 한다 — ① 절대 시각 기준 파일 변경 ② 트랜스크립트 크기 증분.
한 신호만 보고 에이전트를 죽이지 않는다. 그리고 **0건을 보면 먼저 그 명령이 맞는지 의심한다**(§10-15 와 같은 유형).

### 이번 틱에서 한 것

없다. 게이트를 돌리지 않았다 — **동시 쓰기 중 측정은 오염된다**(§10-17). 에이전트 소유 파일도 건드리지 않았다.
완료 알림을 받은 뒤 **조용한 창에서** 게이트를 직접 다시 돌려 확인한다(§4.2-5).

---

## 2026-09-11 회전 6 — 개발1 완료 (개발2·개발3 진행 중)

### 개발1 보고 — **아직 미검증이다**

개발2·개발3 이 쓰는 중이라 **게이트를 돌리지 않았다**(§10-17 동시 측정 오염).
개발1 의 아래 수치는 **본인 보고**이고, 조용한 창에서 오케스트레이터가 직접 다시 잰다(§4.2-5).

| 개발1 주장 | 검증 상태 |
|---|---|
| 화면 15건 placeholder 에서 전부 빠짐 | **미검증** |
| `seed_dev1.py` ×2 행 수 diff 0 | **미검증** |
| 전체 pytest 407 passed / 개발1 150 | **미검증** |
| `check_routes` G-03-② PASS · G-03-③ FAIL 1건 | **일부 검증** — FAIL 원인이 내 파일이라는 것만 확인했다(아래) |

### 개발1 이 **내 결함 3건**을 잡아냈다 — 2건 즉시 수정

| 지적 | 확인 | 조치 |
|---|---|---|
| **D-104** `util/security.py:3` 의 회사명이 G-03-③ 를 FAIL 시킨다 | **사실** — `grep` 으로 확인 | **회사명을 뺐다**(D-53). 면제 목록을 늘리지 않았다 — 늘리면 검사가 헐거워진다. 수정 후 소스 전체 근거 없는 용어 **0건** |
| **D-105 CSRF 가 구현되지 않았다** | **사실** — `grep -rn csrf` 결과 설정 플래그뿐 | **내가 회전 5 에 "CSRF 기반" 을 했다고 적은 것은 오기재다**(D-52). `util/csrf.py` 를 만들었다 — HMAC · 세션 바인딩 · 위반 403. 다른 세션 토큰이 거부되는 것까지 실측. **강제 적용은 조용한 창에서** |
| **D-106** `util/__init__` 이 `audit`·`mask` 로 모듈을 가린다 | **사실** — D-46 과 같은 함정이 두 군데 더 | **조용한 창으로 미룬다**(D-54). 지금 바꾸면 개발2·3 의 진행 중 import 가 깨진다. `csrf` 는 처음부터 모듈로 노출했다 |

**설정 플래그만 켜 두고 구현이 없는 것이 가장 나쁘다** — 켜져 있다고 착각하게 만든다.
개발1 이 없었으면 QA3 단계까지 갔을 것이다.

### 개발1 이 지어내지 않은 것 (좋은 판단)

- **D-101/D-56**: 품목·재질 코드가 D-47 로 비어 있어 유효 자재 LOT 을 만들 수 없다 → **입고·재고 시드 0건이 정답**이라 판정하고 대신 유입 경로 3단을 만들었다. 정본도 같은 말을 한다(TD3-005 "초기 이관 범위를 착수 시 확정"). **코드를 지어내 시드를 채우지 않았다.**
- **D-107/D-55**: 공급처 평가 4지표의 산식이 정본에 없어 **산출하지 않고 미확정 렌더**. 불량률만 실측.
- 못 한 것(인증·엑셀 생성·삭제·007 표준화 규칙)을 **"미구현" 이라고 화면에 렌더**했다. 성공한 척하지 않았다.

### 다음

개발2·개발3 완료 대기. 셋 다 끝나면 **조용한 창**에서:
1. `make db-reset` → `make gate-full` **연속 2회**(§4.4-3)
2. CSRF 미들웨어 강제 적용(D-52) + `util/__init__` 정리(D-54)
3. 개발1 주장 전건 재측정

### 개발2 완료 (14:1x) — **미검증**. 개발3 진행 중

개발3 이 아직 쓰고 있어 게이트를 돌리지 않았다(§10-17). 아래는 **개발2 본인 보고**다.

| 개발2 주장 | 검증 상태 |
|---|---|
| 화면 16 + `/board` = 17 경로 200 · `_placeholder` 0건 | 미검증 |
| `seed_dev2.py` ×2 diff 0 · pytest **546 passed** | 미검증 |
| `gate.py` **PASS 6 · FAIL 0 · 차단 0 · 미구현 6** | **미검증 — 개발3 동시 쓰기 중 측정이라 참고값이다**(§10-17) |
| 통제 경로: 불합격 LOT 출하 422 · 승인 권한 없음 403 · 외주 공정 실적 422 | 미검증 |

**잘한 판단으로 보이는 것** (조용한 창에서 확인한다)
- KPI 감소율 18.2/16.7 을 **상수로 박지 않고 계산**했다 — 기존·목표값이 바뀌면 따라간다.
- 표본 0건이면 `None` 을 돌려 화면이 `미수집` 을 렌더한다. **0.0 으로 메우지 않았다.**
- 채번을 자기 형식으로 쓰지 않고 **개발1 이 `BAS_COMMON_CODES('LOT채번')` 에 공표한 것을 DB 에서 읽어** 썼다.
- 전제(`EST_PROJECTS`·`BAS_QUALITY_STANDARDS`·코드 그룹)가 0건이라 시드가 막히자 **임시 전제로 채우지 않고**,
  N건 경로는 테스트 안에서만 실증하고 `KPI_TARGETS` 2건만 실제로 넣었다.

**즉시 확인한 것**: **D-205 는 사실이다** — `/board` 가 `main.py:99` 와 `routers/kpi.py:232` 양쪽에 있다(D-57).
라우터가 이기지만 한 경로에 핸들러가 둘인 것은 함정이다. 조용한 창에서 `main.py` 쪽을 지운다.

**개발3 생존 확인**(두 신호): 최근 10분 파일 변경 있음(`test_dev3_*` 5종·`agent/service.py`·`routers/{est,agt}.py`) ·
트랜스크립트 20초에 **+9,135 byte**. 정상 작동 중이다.

---

## 2026-09-11 회전 8 — 웨이브 C 기동 (QA1 · QA2)

사용자에게 순서를 물었으나 답이 없어 **프로토콜대로 정했다**(§4.2-3): FAIL 0 · 남은 것은 전부 `미구현` 이므로
다음은 **웨이브 C** 다. 웨이브를 건너뛰지 않는다.

**QA 3명을 동시에 띄우지 않았다.** §10-11 이 기록한 사고 — `check_ai` 가 끝나며 자기 비밀번호로 공통 시드를
되돌려 다른 에이전트 테스트가 401 로 무더기 실패했다. **QA1+QA2 먼저, QA3(재시드·AI)는 단독**으로 돌린다.

| | 만들 검사기 | 재는 게이트 |
|---|---|---|
| QA1 | `tools/check_trace.py` | G-04 · G-05 + 오류 계약 7종 · RBAC 6×8 · LOT 클릭 · 목업 일치 · 폰 폭(§10-13) |
| QA2 | `tools/{check_schema,check_data,check_ingest}.py` | G-07~G-13 + **KPI 독립 재계산** |

프롬프트에 넣은 것: **"내 말을 믿지 말고 직접 확인해라"**(D-60·D-61·D-57 은 내가 찾은 것이다) ·
§10-14 건수 카드 `0 건` 은 정답 · §10-16 검사기는 정본 함수를 직접 부른다 · §10-4 skip 금지(D-62 가 실례) ·
§10-11 시드 되돌리기 · §10-17 동시 변경은 `판정 불가`.

**QA1 에게 따로 물은 것**: D-61(계약에 TD5 에 없는 컬럼 `ADOPT_YN`) 같은 유형이 **더 있는지**.
내가 한 번 저질렀으면 더 있을 수 있다.

### QA2 완료 (15:3x) — QA1 진행 중

**QA2 판정 (검사기 2회 연속 동일)**
```
G-01 PASS · G-02 PASS   (762컬럼 이름·타입 전수 대조 — 불일치 0)
G-07 PASS · G-11 PASS · KPI PASS
G-08 차단 · G-09 차단 · G-10 차단   (분모 0 — EST_PROJECTS·BAS_QUALITY_STANDARDS·코드그룹 0건)
G-12 FAIL · G-13 FAIL
```

**QA2 가 내 파일 결함 3건을 잡았다** — 전부 확인했다.
- **D-65** `gate.py` 가 `check_trace.py` 를 **실행하지 않고** G-04·05 를 FAIL 고정 (문구는 "미작성" 이라고 말한다)
- **D-66** `gate.py` 가 G-07~G-13 을 **종료코드 하나로 뭉갠다** — §10-6 위반. `check_routes` 에는 줄 파싱을 넣고 여기는 안 넣었다
- **D-67** `contracts/missing-policy.md` **부재** — goal.md G-10 이 가리키는 문서를 안 만들었다 → **만들었다**

`gate.py` 수정은 **QA1 이 끝난 뒤** 한다(돌고 있는 중 바꾸면 QA1 출력이 달라진다).

**QA2 가 지어내지 않은 것**
- G-08·09·10 을 **분모 0** 이라 `차단` 으로 남겼다. "0% 도 100% 도 아니다 — 임시 코드로 85% 를 만들지 않았다."
- 2.7.4 '정상 데이터' 정의가 정본에 없자 **조작적 정의를 명시**하고 그 사실을 리포트·검사기에 박았다(D-68).
- 동기화율은 목표가 없어(D-09) **실측만 보고**했다.
- `make cad-ingest` 를 **일부러 돌리지 않았다** — 995건이 커밋되면 다른 QA 의 0건 경로가 오염된다.

**QA2 가 자기 오판 2건을 스스로 잡았다** (리포트에 기록)
1. 비율 카드(`설비 가동률`·`합격률`)의 보조설명 "0건" 때문에 건수 카드로 오인해 **§10-14 를 거꾸로** 적용할 뻔했다 → 카드 **값** 기준으로 고쳐 **거짓 결함 5건 제거**.
2. store-and-forward 재전송의 전역 순서 역전 8건을 위반으로 적을 뻔했다 → **라이브/재전송 구간별** 판정으로 고쳤다.

**넘길 결함** — D-69(수집 중단 배지가 구조적으로 못 뜬다, Critical, 개발2) · D-70(중단 판정 두 곳 복제, 반대 결론) ·
D-71(G-13 전처리가 어휘만 있고 구현 0, 개발3) · D-72(§7.1 판정 1건 불일치, 개발2).

**주의**: QA2 마감 회차에 다른 에이전트가 동시에 쓰고 있어 G-07·빌드가 흔들렸다. QA2 가 **동시 변경 2신호 감지**를
넣어 그 회차를 `판정 불가` 로 찍었다 — FAIL 로 기록하지 않았다. **종료 판정은 조용한 창에서 다시 한다**(§10-17).

---

## 2026-09-11 회전 9 — QA1·QA2 완료 · **게이트가 거짓말하고 있었다**

### 조용한 창 실측 (에이전트 0 · `db-reset` → `gate` 연속 2회 **판정 동일**)

```
이전(뭉갠 판정)   PASS 6 · FAIL 0 · 차단 0 · 미구현 6   / 12
지금(게이트별)    PASS 8 · FAIL 4 · 차단 3 · 미구현 17  / 32
```

**이전의 "FAIL 0" 은 거짓 안심이었다.** 내 `gate.py` 가 게이트를 뭉개서
G-05·G-12·G-13 FAIL 이 보이지 않았고, `미구현 6` 은 실제로 **17개 게이트**였다.

| | 판정 |
|---|---|
| PASS 8 | G-01 68 · G-02 762 · G-03(45화면·placeholder 0·용어 0) · G-04 추적 1:1 · G-06 메뉴 · G-07 시드 멱등 · G-11 조용한 빈칸 · 정본 대조 |
| **FAIL 4** | **G-05**(`/dat/033` 인터페이스 0/4) · **G-12**(수집 결함 2) · **G-13**(전처리 결함 5) · **G-빌드**(1 failed) |
| 차단 3 | G-08 · G-09 · G-10 — **분모 0**(EST_PROJECTS·BAS_QUALITY_STANDARDS·코드그룹 0건) |
| 미구현 17 | G-14~G-30 — `check_ai.py` · `check_security.py` (QA3) 미작성 |

### 내가 고친 것 — 게이트 자체의 결함 2건

- **D-65/D-74**: `gate.py` 가 `check_trace.py` 를 **실행하지 않고** G-04·05 를 FAIL 로 하드코딩했다.
  **`G-04 FAIL` 은 거짓이었다** — 검사기는 처음부터 PASS 를 찍고 있었다. QA1·QA2 가 각자 독립적으로 잡았다.
- **D-66**: G-07~G-13 을 **종료코드 하나로** 뭉갰다(§10-6 위반). `check_routes` 에는 줄 파싱을 넣어 놓고
  `CHECKERS` 목록에는 안 넣었다.
→ `gate.py` 를 **게이트별 판정 줄 파싱**으로 다시 썼다. 검사기가 있는데 그 게이트 줄을 안 찍으면 **PASS 가 아니라 `차단`** 이다.

### 내 보고가 틀렸다 — D-73

회전 7 에 "CSRF 가 POST 27개 중 **4곳** 연결" 이라고 적었다. **실제는 0곳이다**(QA1 DEF-QA1-002).
내가 센 "4" 는 **내가 쓴 `util/csrf.py` 안의 문서·상수 줄**이었다 — 호출 위치가 아니었다.
재실측: `csrf.require()` **0** · 템플릿 `_csrf` **0** · POST **27**.
**`CSRF_ENFORCE=1` 을 먼저 켜면 쓰기 27개가 전부 403 이 된다.**

### QA 가 정직했던 지점

- **QA1**: 터치 버튼 크기 기준이 정본에 없자 **남의 기준(44px)을 끌어와 FAIL 을 만들지 않았다**(D-82).
  실측만 남겼다 — 개발2 36px · 개발1 38px · **좁은 폭 미디어쿼리에 버튼 규칙 없음**.
  폰 폭은 브라우저가 없어 **CSS 정적 판정만 했다고 밝혔다**(D-83).
- **QA2**: G-08·09·10 을 **분모 0** 이라 `차단` 으로 남겼다 — "임시 코드로 85% 를 만들지 않았다".
  자기 오판 2건을 스스로 잡아 **거짓 결함 5건을 제거**했다.
- 둘 다 **동시 실행 중 흔들린 수치를 `판정 불가`** 로 기록하고 FAIL 로 적지 않았다(§10-17).

### 빌드 FAIL 1건 — QA2 의 자기 무효화 표지

`test_qa2_data.py:263` 이 `contracts/missing-policy.md` **부재**를 단언하면서
"생기면 결함 해소이니 리포트를 갱신하라" 는 메시지로 실패하게 해 뒀다. 내가 문서를 만들어 **의도대로 터졌다.**
→ QA2 를 다시 깨워 표지 테스트를 **문서 준수 검사**로 바꾸고 리포트를 갱신하게 했다(QA2 소유 파일이라 내가 고치지 않는다).

### 넘길 결함 (담당별)

| 담당 | 결함 |
|---|---|
| **개발1** | D-75 `/dat/033` 인터페이스 0/4 (**G-05 FAIL 사유**. `/sys/029` 에 4/4 가 이미 있다 — 열 하나 이동) · D-81 `ORDER BY` 없는 `[0]` 단언 |
| **개발2** | D-69 **수집 중단 배지가 구조적으로 못 뜬다**(Critical) · D-70 중단 판정 두 곳 복제(반대 결론) · D-72 §7.1 판정 1건 불일치 |
| **개발3** | D-71 **G-13 전처리가 어휘만 있고 구현 0** · D-76 프로젝트번호 링크가 `?lot=` (항상 0건) · D-77 수집 API 파싱 실패가 500 |
| **전원** | D-60/D-73 **CSRF 0/27** (Critical) · D-78 폼 검증이 권한 검사보다 먼저 → 403 이 422 로 |
| **아키텍트** | D-78 오류 계약에 **검사 순서** 명문화 · D-80 계약 경로를 코드에 맞춤 |

### QA3 스톨 의심 (16:3x) — 아직 죽이지 않는다

| 신호 | 관측 |
|---|---|
| 트랜스크립트 | **90초+ 완전 정지** (1,394,364 byte 불변) |
| 파일 변경 | 최근 1분 **0건** (직전 10분에는 `check_ai.py`·`rag_goldset.json`·`cad_labelset.json` 생성됨) |
| 프로세스 | `check_ai`·`check_security`·`pytest` **실행 중 없음**. DB 활성 세션 1 = 내 질의 |

세 신호 모두 음성이다. 다만 §10-9 의 기준은 **600초**이고, 지금 죽이면 진행분을 버린다.
**10분 뒤 한 번 더 본다.** 그때도 정지면 **새 에이전트에 잔여 작업만** 준다(§10-9 — 재개보다 빨랐다):
`tools/check_security.py`(G-26~G-30) + `outputs/qa3-AI비기능보안.md`. 이미 만들어진 `check_ai.py`·정답셋은 **그대로 쓴다**.

**교훈 적용**: 한 신호로 죽이지 않는다. 이번에는 **세 번째 신호(프로세스 유무)**를 추가했다 —
긴 도구 호출 중이면 트랜스크립트는 원래 멈추므로 그것만으로는 스톨이 아니다.

### 16:48 재확인 — **스톨이 아니었다. 죽이지 않은 것이 옳았다.**

| 신호 | 16:32 | 16:48 |
|---|---|---|
| 트랜스크립트 | 1,394,364 (90초+ 정지) | **1,968,166** (+574KB) |
| 최근 파일 변경 | 0건 | 3건 |
| 검사기 프로세스 | **0건** | **3건** |

`tools/check_security.py`(750줄)까지 생겼고 리포트만 남았다. 90초 정지는 **몇 분짜리 도구 호출**이었다.
그 순간 프로세스가 0 으로 보인 것도 도구 호출 **사이**였을 뿐이다.

**§10-9 교훈 정정 — 스톨 판정에는 시간 축이 필요하다.**
세 신호를 **한 순간에** 읽어서는 안 된다. 순간 판독은 도구 호출 사이의 정적과 구분되지 않는다.
판정 기준: **600초 동안 지속적으로** ① 트랜스크립트 증분 0 ② 파일 변경 0 ③ 프로세스 0 이 모두 유지될 때만 스톨이다.
직전 사업의 "600초" 는 임계가 아니라 **관측 창의 길이**였다.
이번에 순간 판독으로 죽였다면 **`check_ai.py` 788줄 · `check_security.py` 750줄 · 정답셋 2종을 버렸을 것이다.**

---

## 2026-09-11 회전 16 — CSRF 전면 적용 (조용한 창 실측)

```
PASS 17 · FAIL 4 · 차단 11 · 미구현 0 / 32      (웨이브 D 착수 시 PASS 13 · FAIL 7)
빌드 11 failed, 1199 passed
```

### 결정적 검증 — CSRF 로 인한 실패 **0**

```
KYUNGDONG_CSRF_ENFORCE=1  실패 집합
KYUNGDONG_CSRF_ENFORCE=0  실패 집합
diff → 0줄
```
착수 시 `ENFORCE=1` 은 **71 failed + 15 errors** 였다. 지금은 켜고 끄고 **한 줄도 다르지 않다.**
**G-26 FAIL → PASS** (`CSRF 31/31 · ENFORCE=1 · 배지 노출 False`).

### 숨어 있던 함정 — 검사기 자신이 토큰을 안 실었다

제품 하네스만 고쳤다면 `ENFORCE=1` 에서 **G-29·G-30·G-24 가 PASS → FAIL 로 떨어졌을 것이다** —
`check_security.py`·`check_ai.py` 의 POST 가 토큰 없이 나갔기 때문이다. 함께 고쳤다.
토큰은 **기준점을 잡기 전에 한 번만** 받는다 — 측정 창 안에서 화면을 다시 열면
`SYS_ACCESS_LOGS` 에 '접속' 행이 섞여 G-29 실측이 흐려진다.

### 게이트를 낮추지 않았다는 근거

- `ENFORCE` 를 테스트 안에서 0 으로 되돌리던 곳을 **제거**했다(`test_dev2_screens` 의 `finally` 가 `"0"` 을 박고 있었다)
- `csrf.require` monkeypatch **0** · `skip`·`xfail` 추가 **0** · `EXEMPT_PATHS` **무변경**
- CSRF 표지 3건은 **삭제하지 않고 방향을 뒤집었다** — 이제 *연결이 풀리면* 깨진다

### 내 지시서가 실측과 달랐던 점

예시로 준 `page="/est/012"` 는 실제로 **403** 이 난다 — 그 화면이 토큰을 찍지 않기 때문이다.
토큰은 **경로가 아니라 세션에 매인다**(`util/csrf.py _bind`). 에이전트가 `/login` 에서 받아 해결했고 그 차이를 보고했다.

### 남은 FAIL 4건 — **전부 QA 검사기·표지가 낡았다** (제품 결함 아님)

| 게이트 | 실태 |
|---|---|
| G-12 | `check_ingest.py ③b` 가 "실시각 수집 직후 배지 없음 = 결함" 이라는 낡은 가정. 지목한 `anchor() - last_dt` 코드는 **grep 0건** |
| G-13 | **결측 보정·이상치 처리가 생겼다.** 남은 것은 Feature 5종 **D-05 차단**뿐 → **`차단`이 정직한 끝**(D-91) |
| G-25 | 롤백이 생겨 미충족 **5/5 → 4/5**. `check_ai.py` 가 FAIL 하드코딩 |
| G-빌드 | 11건 전부 **"이 기능은 아직 없다" 는 QA 표지** — 해당 기능이 붙어 낡아졌다 |

### 넘길 결함 1건 (차단 아님)

`/est/011`·`/est/012`·`/est/013`·`/agt/040` 화면이 `csrf_field()` 를 찍지 않는다.
지금은 그 화면 쓰기 버튼이 전부 `disabled` 플레이스홀더라 브라우저 쓰기 흐름이 없다.
**버튼이 살아나는 순간 한 줄이 필요하다** — 안 넣으면 실사용자가 403 을 받는다.

---

# 최종 보고 — 2026-09-11 (§4.4 종료)

## 종료 조건 판정

| 조건 | 실측 | 판정 |
|---|---|---|
| ① 게이트 전건 PASS (`차단`은 사유·D-번호 기재) | **PASS 21 · FAIL 0 · 차단 11 · 미구현 0 / 32** (2026-09-12 실측 · 빌드 1,286 passed · G-10 이 D-184 로 풀렸다). 차단 사유 D-04·05·09·12·13·47·167·169 | **만족** |
| ② QA 리포트 3종 Critical 0 | 미해소 Critical **0건** (마지막 2건은 CSRF 낡은 문구 — 오케스트레이터 실측으로 갱신) | **만족** |
| ③ 조용한 창에서 `db-reset` → `gate` **연속 2회 동일** | 2회 판정 동일 · 검사기가 넣은 행 전량 회수(0행) | **만족** |

빌드 **1210 passed · 0 failed · skip 0 · xfail 2**.

## 만든 것

화면 **45** + 공통 4 + 오류 (`_placeholder` **0건**) · 테이블 **68** · 컬럼 **762** · 프로그램 **49** ·
요구사항 추적 **AD2 45 ↔ TD3 45 ↔ TD4 45 · 고아 0 · 중복 0** · 테스트 **1,210**.

## 차단 13건 — **통과한 게 아니라 잴 수 없는 것이다**

코드로 해결되지 않는다. **도입기업 자료가 필요하다.**

| 필요한 것 | 막히는 게이트 |
|---|---|
| **품목·재질·보관위치 코드 · 공급처 목록** (D-56·D-47) | G-08 디지털 스레드 · G-09 정확성/정합성/연계성 — 유효 자재 LOT 을 만들 수 없어 **분모가 0** |
| **과거 견적금액·실제 제조원가 Label** (D-04) | G-15 견적 정확도 ±5% · G-17 납기 예측 85% |
| **Autodesk API · YOLOv8 · OCR** (D-05) | G-14 CAD 객체 인식 80% · G-19 견적 5단계 · G-13 Feature 5종 |
| **전문가 선정 변수 목록** (D-13) | G-18 설명가능성 90% |
| **목표치 자체가 사업계획서에 없음** (D-09) | G-21 인용 정확도 · G-23 응답 지연 — **실측만 보고했다** |
| 납기 허용 오차 (D-12) | G-17 |

**합성 라벨·합성 예측·임시 코드로 채우지 않았다.** 분모 0 은 0% 도 100% 도 아니다.

## 도입기업에 제안할 산출물 결함 (고치지 않고 등재)

| 결함 | 내용 |
|---|---|
| **D-02** | **회사 정체성 오염** — 사업계획서 2.5·2.7.4·10.1 이 이 회사를 「금속 구조물/판금 가공」으로 서술. 잔재 용어 실측 **포밍 10 · 롤포밍 1 · 로봇용접 2 · 레토르트 1 · 판금 1 · 금속 구조 2**. 1.1 의 ETO 서술과 CAD 아카이브 실측이 일치하므로 1.1 을 채택했다 |
| **D-32** | **코드성 FK 29건이 물리 FK 로 성립하지 않는다** — `VARCHAR` 컬럼이 `BAS_COMMON_CODES.CODE_ID BIGSERIAL` 을 가리킨다. DB 가 막지 않아 애플리케이션 검증으로 대체 |
| **D-33** | `EST_QUOTATION_ITEMS.UNIT_PRICE`(금액)를 FK 로 표기 |
| **D-41** | **외주 발주번호·외주처 컬럼이 없다** — TD1 이 요구한 데이터에 자리가 없다 |
| **D-44** | **`SYS_USERS.ROLE_ID` 설계 결함** — 역할이 아니라 권한 행 하나를 가리킨다. 시드 재실행이 실제로 FK 위반으로 죽었다 |
| ~~**D-98**~~ | **해소됨 (D-160·D-161)** — 도입기업이 **3종**으로 확정했고, TD5 `OBJECT_TYPE` 비고는 `… 등` 으로 끝나는 **열린 목록**이라 담을 자리가 있었다. `TD5 에 담을 자리가 없다` 던 **내 판단이 틀렸다**. 모집단 835 vs 1,283 은 남는다 |
| **D-11·D-19** | 견적 정확도 표기(80% vs ±5%) · FP(281.08 vs 340.76) 상충 |
| D-07·D-18·D-25 | ERP 보유 여부 · Vector DB 제품 · 장비 명칭 상충 |

## 이 루프가 실제로 한 일 — **게이트가 거짓말하는 것을 잡았다**

| 사건 | 내용 |
|---|---|
| **거짓 "FAIL 0"** | 내 `gate.py` 가 판정을 뭉개 `PASS 6 · FAIL 0 / 12` 를 냈다. 게이트별 파싱으로 고치자 **`PASS 8 · FAIL 4 · 차단 3 · 미구현 17 / 32`** 가 드러났다. `check_trace.py` 를 **실행하지 않고** FAIL 을 하드코딩해 **G-04 FAIL 도 거짓**이었다 |
| **내 보안 과대 보고 2회** | ① CSRF 설정만 만들고 구현 없음(D-52) ② 세션·속도제한 모듈을 만들고 **부르는 곳 0**(D-94). 둘 다 "모듈을 만든 것" 과 "연결한 것" 을 구분하지 않고 보고했다 |
| **내 CSRF 패턴의 숨은 결함 2건** | `with context` 누락으로 **토큰이 빈 문자열**로 렌더 · 쿠키 타이밍 오류. **그대로 켰으면 쓰기 전부 403** |
| **검사기 자신이 토큰을 안 실었다** | 제품만 고쳤으면 `ENFORCE=1` 에서 G-24·G-29·G-30 이 PASS → FAIL 로 떨어질 뻔했다 |
| **내 측정이 3번 조용히 틀렸다** | `find -newermt` 상대표현(스톨 오판) · CSRF 4/27(실제 0) · `app.routes` 7개(앱 정상). 전부 **오류 없이 틀린 값**을 돌려줬다 |
| **내 계약에 직전 사업 컬럼명** | `ADOPT_YN`·`ADOPT_BY` 는 TD5 에 없다(광성정밀 컬럼). §6 오염 경계를 내가 어겼다 |

## 지어내지 않은 지점들

- QA3 가 **RAG 임계값을 내리지 않았다** — 0.70 → 0.55 면 G-21·G-22 가 좋아지지만 "QA 가 정할 값이 아니다" 며 결함으로 넘겼다.
- QA1 이 **터치 버튼 44px 기준을 끌어오지 않았다** — 정본이 "큰 버튼" 이라고만 적어 실측만 남겼다.
- QA2 가 **분모 0 을 85% 로 만들지 않았다** — "0% 도 100% 도 아니다".
- 개발3 이 **Feature 6종을 채우지 않았다** — 5종이 D-05 로 막혀 `차단`이 정직한 끝임을 받아들였다.
- 마지막 검사기 갱신에서 **FAIL → PASS 는 G-12 하나뿐**이고 나머지는 **FAIL → 차단**이다. 차단을 PASS 로 올린 것은 **없다**.

## 도입기업 답변 1차 회신 (2026-09-12 · D-160)

요청서 7항목 중 **4건**에 답을 받았다. **게이트 수는 움직이지 않았다** — 받은 답이
차단을 푸는 답은 아니었기 때문이다. 그 사실을 그대로 적는다.

| 항목 | 답 | 무엇이 바뀌었나 | 게이트 |
|---|---|---|---|
| ② 재질 14종 · STS=SUS | **O** | 코드 마스터 8종이 `가정` → **확정** 표기로 섰다 | **안 바뀜** — G-09 분모는 재질로 열리지 않는다(D-152, 실측) |
| ③ 단위 미표기 620건 | **mm** | 표지 문구가 `가정` → `확정`. 변환 동작은 그대로 | 안 바뀜 (가정과 답이 같았다) |
| ④ 라벨 어휘 | **3종** | **D-98 해소** · 홀 라벨 3,391건과 **`P/R/F1 0.9009` 회수** | G-14 **차단 유지** — 이유가 깨끗해졌다 |
| ⑥ 고객사 19종·제외 판단 | **O** | 이미 확정(D-139)된 것의 재확인 + 케이테크 승계 확정 | 안 바뀜 |

**이 회전이 실제로 한 일은 수치를 지운 것이다.** `0.9009` 는 임계 80% 를 넘는 숫자였고
게이트 줄에 찍혀 있었다. 답변 ④ 가 오면서 그 라벨이 **범위 밖**임이 드러났고, 순환이라는
기존 사유와 **각각 단독으로** 회수 사유가 됐다. 지우지 않았다면 남은 것은 인용 가능한 거짓 증거다.

### 그 과정에서 잡은 조용한 결함 — **표시 문구를 패턴에 복사한 7곳** (D-163)

`%단위 가정 (%` 5곳 · `가정 답변 기준 %` 2곳이 SQL LIKE 에 **문자열 그대로** 박혀 있었다.
②③ 이 확정으로 바뀌며 표시가 `도입기업 확정 (D-160)` 이 되자 **일곱 곳이 전부 조용히 0건**이
될 참이었다 — 표지가 안 걸리면 **선언을 지워도 파생물이 남는다.** 회수 장치가 통째로 죽는다.
문구가 아니라 **불변 부분**으로 걸도록 고쳤다: 결정번호 괄호 `%(D-%)%` · 변환 화살표 `%도면단위→%`.
그중 2곳은 **테스트였다** — 테스트가 공집합 위에서 참이 되어 아무것도 못 지키게 될 뻔했다.

### 아직 답이 없는 것 (D-167 · 차단이 정답이다)

| 필요한 것 | 막힌 게이트 |
|---|---|
| **3종 정답 라벨 30~50건** | G-14 · G-19 — 유일한 분모다. 우리가 만들면 다시 순환이다 |
| **레이저커팅기 PLC · 현장POP 의 IP 주소** | **운영 수집 자체** — 없으면 prod 에서 403 이다 (D-168·D-169). 시뮬레이터로는 전 구간이 돈다 (D-174) |
| **PLC 태그맵 (태그 ↔ 실물 레지스터 주소)** | Gateway 가 어느 주소를 읽을지 — 시뮬레이터는 `SIM-D0001…` 을 쓴다 (D-176) |
| 실제 제조원가 · 실납기 | G-15 · G-17 |
| 전문가 선정 변수 목록 | G-18 |
| `.cad` 325건 제작 프로그램 | 표본 781 → 1,106 |
| 두께 200mm 11건 진위 · 홀 최소 지름 · 용접선 레이어 | Feature 정확도 |

## 답변 4건을 기다리지 않고 돈 회전 — **개발에서는 안 보이던 것 하나를 찾았다**

도입기업 답을 못 받은 4건(①⑤⑦·라벨)을 빼고, **우리 손에서 끝나는 것만** 돌렸다.

### 찾은 것 — prod 에서 수집이 안 된다 (D-168)

**`POST /api/ingest/plc` 와 `/api/ingest/gateway/resend` 가 prod 에서 403 이다.**
세션이 없으면 역할이 빈 문자열이고 `rbac.can_write()` 가 막는다 —
**PLC 와 Gateway 가 운영에서 데이터를 넣지 못한다.**

**왜 안 보였나**: 개발에서는 `x-kyungdong-role` 헤더가 SYSADMIN 을 준다. 그래서 G-12 를
포함한 게이트가 **전부 통과**했다. 환경을 prod 로 올려 직접 불러 보기 전에는 드러나지 않는다.

**왜 못 고치나 (D-169)**: 인증에 쓸 칸이 없다. TD5 `IF_DEVICE_REGISTRY` **11컬럼에 비밀키·
인증서 칸이 없고** 컬럼은 추가하지 않는다(G-02 762 고정). 쓸 수 있는 유일한 칸 `IP_ADDRESS` 는
**등록된 2대 모두 NULL** — 도입기업이 장비 IP 를 주지 않았고 시드가 지어내지 않았다.

**그래서 한 것 (D-170·D-171)**: 동작은 그대로 두고 **진단을 고쳤다.**
`데이터관리 등록 권한 없음`(→ 현장에서 권한 설정을 뒤진다) 대신
`등록된 수집 장비 2대 전부 IP_ADDRESS 가 비어 있다 … 화면 034 에서 IP 를 넣으면 열린다`.
되돌림 시험으로 **실제로 열리는 것**도 확인했다 — IP 를 등록하니 `200 {"stored":1}`,
다른 IP 로 바꾸니 403. 시험 뒤 IP 를 NULL 로 되돌리고 적재 행도 지웠다(G-11 런타임 0건 유지).

### 나머지 두 건

| | 내용 |
|---|---|
| **D-172** | **화면 POST form 마다 토큰이 실렸는지** 세는 검사를 넣었다. 전에는 라우트 수(31/31)만 셌다 — 라우터가 멀쩡해도 form 이 토큰을 빠뜨리면 **그 화면의 쓰기만 403** 이고 게이트는 통과한다(D-73 이 그 사고였다). 지금 **10/10**. 토큰 하나를 빼는 시험에서 G-26 이 **PASS → FAIL** 로 떨어지며 빠진 form 을 이름으로 찍었다 |
| **D-173** | **D-136 해소** — `cad/archive.py` 하드코딩 고객사 18종을 확정 정본에서 읽게 바꿨다. 박아 뒀더니 실제로 틀려 있었다: `GD1806-최성배` **3폴더가 사람을 고객사로** 세고 `GD2003-01-썬바이오` **2폴더는 진짜 고객사를 빠뜨리고** 있었다(40→39 · 고유 11→10). **진실을 두 곳에 두면 한 곳이 조용히 낡는다** — D-163 과 같은 결함이다 |

**게이트는 이번에도 안 움직였다** — 셋 다 게이트가 재던 범위 밖에서 잡힌 것들이다.
D-168 은 오히려 **게이트가 놓치고 있던 것**이다: G-12 는 dev 에서만 재고 있었다.

## PLC 시뮬레이션 — **장비 IP 없이 수집 전 구간을 돌린다** (D-174~D-178)

장비 IP 가 없어(D-169) 실물 PLC 를 못 붙인다. 그래서 **PLC 쪽을 세웠다.**

### 왜 SQLite 파일인가 — 우리 DB 가 아니어야 하기 때문이다

실물 구성은 `PLC 레지스터 → Gateway 폴링 → 수집 API → PostgreSQL` 이다. PLC 는 자기 메모리를
들고 있고 Gateway 가 **주기적으로 읽어 간다.** 예전 시뮬레이터는 값을 만들어 곧장
`collector.ingest_batch()` 를 불러서 그 두 다리가 통째로 빠져 있었다 — 그러면
**끊긴 동안 값이 장비에 남아 있다가 복구 후 올라가는 것**을 잴 수가 없다.
`work/plc_device.db` 를 `kyungdong_db` **밖에** 둔 것이 요점이다: 같은 DB 에 넣으면
*장비가 들고 있던 값*과 *우리가 적재한 값*이 구분되지 않는다.

```
make plc-scan         ① PLC 스캔 — 레지스터에 쌓고 멈춘다 (아직 아무것도 안 보냄)
make plc-status       레지스터 8종 · 스캔/보냄/**미전송**
make plc-register     시뮬레이터 IP + 선언 (실물 IP 가 아니다 — 둘이 함께 붙고 함께 떨어진다)
make run              서버
make plc-poll         ② Gateway 폴링 — 진짜 POST /api/ingest/plc 로 올린다
make plc-unregister   IP·선언을 함께 거둔다 → prod 수집은 다시 403 (실측 상태로 복귀)
```

### 내가 틀렸던 것 — 단절을 뭉뚱그렸다 (D-175)

처음엔 단절 하나에 **Gateway 버퍼와 PLC 레지스터가 같은 값을 둘 다** 들고 있었다.
복구하면 **두 번 올라간다.** 끊긴 자리가 다르면 값이 남는 자리도 다르다.

| 단절 | 폴링 | 값이 남는 곳 | 실측 |
|---|---|---|---|
| **PLC↔Gateway** (`--plc-offline`) | **안 된다** | **장비 레지스터** (Gateway 버퍼 0 — 본 적도 없다) | 스캔 80 · 적재 32 · **잔량 48** → 복구 후 **80/80 유실 0** |
| **Gateway↔클라우드** (`--disconnect`) | 된다 | **`IF_GATEWAY_BUFFER`** (PLC 는 넘긴 것으로 본다) | 버퍼 3배치 → 재전송 후 **80행 유실 0** |

### prod·HTTP 를 실제로 통과시켰다 (D-178)

서버를 `KYUNGDONG_ENV=prod` 로 띄우고 `--via http` 로 진짜 `POST /api/ingest/plc` 를 보냈다 —
**D-168 이 막아 뒀던 그 경로다.** 장비 인증 · 태그 8종 검증 · `RUN_STATUS` 어휘를 전부 지나
`IF_PLC_SIGNALS 80 · DAT_TIMESERIES 60 · PRC_EQUIP_SIGNALS 10`.
기본값 `--via inproc` 은 HTTP·장비 인증을 지나지 않으므로 **그것만으로는 D-168 을 못 잡는다.**
서버가 없으면 `--via http` 는 **소리내어 죽는다** — 조용히 inproc 으로 되돌아가지 않는다.

### 지어내지 않은 것

- 레지스터 주소는 `SIM-D0001…` 이다. **실물 XGB(XBC-DN32H) 주소를 아는 척하지 않는다** —
  Gateway 가 어느 레지스터를 읽어야 하는지는 **도입기업 태그맵**이 있어야 안다 (D-176).
- 기본 상태는 **IP 미등록**이다. 실측(IP 없음)을 그대로 두고 시뮬레이션은 명령 한 줄로 켠다.
- 보냄 표시는 **적재에 성공한 뒤에만** 한다. 폴링하자마자 찍으면 적재가 실패해도 보낸 것이
  되어 **유실이 숨는다.**

### 게이트가 내 코드를 잡았다 — **FAIL 3** (D-179·D-180)

시뮬레이터를 넣고 게이트를 돌리니 `PASS 20 → 17 · FAIL 3` 이 됐다. 둘 다 내가 만든 것이다.

| 게이트 | 무엇 | 검사기가 준 것 |
|---|---|---|
| **G-07** | `plcdb.mark_sent` 가 `datetime.now()` (§10-3 위반) | **파일·줄·함수 + 고칠 방법**을 찍었다 — `src/kyungdong/ingest/plcdb.py:144 (mark_sent) — … clock.anchor() 를 쓴다` |
| **G-26** | `urllib` import 를 외부 통신 흔적으로 잡았다 (9.2 외부 접속 최소화) | 파일명 |

G-07 은 그대로 고쳤다(벽시계로 적으면 날짜가 바뀔 때마다 같은 시뮬레이션이 다른 값을 낸다).

G-26 은 **검사기를 느슨하게 하지 않고 정밀하게** 만들었다. 전에는 import 만 보고 잡아서
**"우리 앱에 POST 하는 시뮬레이터"와 "외부 API 호출"이 구분되지 않았다.**
이제 **제품 `src/` 는 통신 모듈 0건이 절대 기준**(그대로)이고, `tools/` 는 **외부 목적지
리터럴이 있을 때만** 결함이다. 동시에 시뮬레이터를 **실제로 막았다** — `check_loopback()` 이
`127.0.0.1`·`localhost`·`::1` 밖이면 거절한다. 외부 URL 하나를 넣는 되돌림 시험에서
G-26 이 **PASS → FAIL** 로 떨어지며 목적지를 찍는 것을 확인했다.

## 대장 정합성 + G-10 (D-181~D-187)

### 대장이 사실과 어긋나 있었다 — **45건 중 16건이 이미 해소**

`CLAUDE.md` 가 "대장에 없는 것을 마음대로 정하지 않는다" 고 못박는데 **그 대장이 낡아** 있었다.

| 대장 | 적힌 상태 | 실측 |
|---|---|---|
| D-120 | 차단 — 고객사 코드 0건이라 적재 불가 | 코드 **19건** · `EST_PROJECTS` **12행** |
| D-69·D-70 | **차단(Critical)** — 중단 판정이 두 곳 | `stale_verdict()` **한 곳**으로 합쳐짐 |
| D-102 | **차단(Critical)** — CSRF 를 못 켠다 | `CSRF_ENFORCE=True` |
| D-94 | **차단(Critical)** — 로그인·속도제한 호출 0 | `POST /login 1 · ratelimit 6 · session.create 1` |
| D-77 | 차단 — 파싱 실패가 500 | **422** |
| D-75 | 차단 — `/dat/033` 연계 0/4 | 렌더 200 · **4/4** |
| … | 그 외 10건 | 실측으로 닫음 |

**다음 할 일을 이 목록에서 고르면 끝난 일을 또 한다.** 실제로 이 목록을 그대로 드릴 뻔했다.

### 손으로 닫으면 또 낡는다 → 검사기를 만들었다

`tools/check_decisions.py` — 항목마다 **근거를 다시 재는 probe**(27종)를 둔다.

- probe 가 `해소` 인데 대장이 `차단` 이면 **결함**이다.
- probe 가 없는 것은 **`수동` 이라고 적는다**(18종 — 전부 정본 부재·도입기업 미제공이라
  코드로는 알 수 없다). **확인한 척하지 않는다.**
- 닫은 항목은 **되열림**도 재검한다.
- 원문은 **지우지 않고** 취소선 + 실측 근거를 붙였다 — 왜 열려 있었는지가 역사다.
- **대장은 사람이 닫는다.** 검사기가 쓰면 어떤 근거로 닫혔는지가 사라지므로
  `write_text` 금지를 테스트가 단언한다.

**게이트는 늘리지 않았다** — `goal.md` 32건 고정. 대신 **테스트가 진다**(`test_arch_decisions.py` 6건).
깨지면 `G-빌드` 가 FAIL 이라 **대장이 낡은 채로 회전이 끝나지 않는다.**

**내가 두 번 틀렸고 둘 다 테스트로 막았다** — ① probe 3건(D-32·D-71·D-75)이 조건을 안 재고
문장만 돌려줬고, 그래서 **D-32 를 해소로 잘못 찍었다**(코드성 FK 29개를 0개로 셈).
② 상태를 **글자 포함**으로 봐서 `확정 — G-10 이 차단에서 풀렸다` 를 열린 항목으로 셌다(D-187).

### G-10 이 차단에서 풀렸다 (D-184~D-186)

```
G-10  PASS  시뮬레이터 기준 D-174 — 실물 설비 실측이 아니다 ·
            결측률 10.0% (주입 10.0%) · 정렬 위반 0/0 · 목표치 없음(D-09)
```

`make db-reset` 직후 런타임 표는 늘 0이라 G-10 은 **영원히 차단**이었다. 그런데 측정은
`check_ingest.py` 가 시뮬레이터로 **이미 하고 있었고** 그 결과가 G-10 에 귀속되지 않았을
뿐이다 — **결함 수정이 아니라 판정이 사실을 말하게 만든 것**이다.

- **완벽한 시계열을 만들지 않았다.** 20주기 중 **2주기를 빼서** 결측을 냈다 — 완벽하면
  결측률이 늘 0% 라 **지표가 결측을 잡는지 알 수 없다**. 되돌림 시험에서 주입을 4주기로
  늘리니 **10.0% → 20.0%** 로 따라 움직였다.
- **고지를 수치 앞에 뒀다** — `gate.py` 가 120자에서 자르므로 뒤에 두면 `PASS · 결측률 10.0%`
  만 남아 **실물 설비 실측처럼** 인용된다(D-151 과 같은 이유).
- 정렬 위반 SQL 을 `order_violations()` **한 곳**으로 뺐다 — 두 판정이 같은 SQL 을 들고 있으면
  한쪽만 고쳐져 같은 DB 에 반대 결론이 난다(**D-70 이 정확히 그 사고였다**).
- 끝나고 런타임 표를 **0으로 되돌린다** — 남으면 G-11 이 오염된다.

## 다음에 할 일

1. **도입기업 2차 답변** (①⑤⑦ · D-167) — 이것 없이는 차단 12건이 풀리지 않는다. 1차 답변 4건은 받았고 게이트는 움직이지 않았다
2. **ML 학습 단독 회전**(§10-2) — Label 확보 후
3. `/est/011`·`012`·`013`·`/agt/040` 에 `csrf_field()` 한 줄 — **쓰기 버튼이 살아나는 순간 필요**
4. `cad/pipeline.BLOCKED_FEATURES` 에 `형상 복잡도` 사유 (D-71)
5. 장비 등록 기반 인증 — CSRF 면제한 기계 엔드포인트 4건의 대체 보호 수단(D-103)
