# decisions-dev3.md — 개발3(AI) 결정 대장 (D-3nn 대역)

`확정` / `가설` / `차단` 세 상태만 쓴다. 공용 대장은 `decisions.md` 이고 여기는 **개발3 소유 결정**이다.
근거가 없으면 `차단` 으로 남긴다 — 수치를 만들지 않는다.

---

## 회전 6 (2026-09-11) — R1-a 수집 · R1-c RAG · R2 화면 골격

| ID | 상태 | 내용 | 근거·처리 |
|---|---|---|---|
| **D-301** | 확정 | **`EST_SHAP_FACTORS` 는 런타임 전용 표다** — 시드 대상이 아니다 (`contracts/db-schema.md` §7.1 판정) | `EST_SHAP_FACTORS.PREDICT_ID` 가 `EST_ML_PREDICTIONS`(런타임 전용, G-11)를 **NOT NULL FK** 로 본다. 예측이 0건이면 SHAP 행은 존재할 수 없다. → §7 런타임 전용 목록으로 옮긴다. 깨끗한 DB 에서 **0건이 정상**이고 화면 015 는 `미수집` 을 렌더한다 |
| **D-302** | 차단 | **`contracts/api-contract.md` §5 의 `ADOPT_YN`·`ADOPT_BY` 컬럼이 TD5 에 없다.** `AGT_RECOMMENDATIONS` 는 `REVIEW_STATUS`(미검토/승인/수정/반려) · `REVIEWER_ID` 만 가진다 | **컬럼을 추가하지 않는다**(G-02 762 고정). `POST /api/agent/recommend/{id}/adopt` 는 `REVIEW_STATUS`·`REVIEWER_ID`·`UPDATED_DT` 로 기록한다. 계약 문서 문구 수정을 아키텍트에게 요청한다 |
| **D-303** | 차단 | **`AGT_VECTOR_DOCS.DOC_TYPE` 어휘에 '견적·조달 기준' 구분이 없다.** TD5 비고는 입고기준서/검사기준서/SOP/출하기준서/클레임 매뉴얼 5종이다 | 시안 `quote_and_procurement_guide.md` 를 `IF_EXT_DOCUMENTS` 에 **등록만 하고 임베딩 대상에서 뺐다**(`EMBED_TARGET_YN='N'`). 어휘에 없는 구분을 만들어 넣지 않는다. 구분 추가를 도입기업에 제안 |
| **D-304** | 차단 | **도면 중복 제거 키의 '고객사' 축이 없다.** `DAT_DATASET_ITEMS.DEDUP_KEY` 비고는 「도면번호+버전+고객사」인데 실측 인벤토리(`docs/cad/…제품별정리.xlsx`)에 **고객사 열이 없다**(D-03) | 고객사를 지어내지 않고 제품/프로젝트 폴더명을 **대체 축**으로 쓴다(`--scope product`, 키에 `[대체 표기 D-03]` 표기). 고객사 없이 본 결과(`--scope none`)도 함께 측정해 둔다 — **등록 893 / 제외 390** vs 대체 축 **등록 995 / 제외 288**. 고객사 메타데이터 확보를 도입기업에 요청 |
| **D-305** | 차단 | **수집 데이터의 원천(실측/시뮬레이터) 구분 컬럼이 TD5 에 없다.** `IF_PLC_SIGNALS`·`PRC_EQUIP_SIGNALS` 에 출처 열이 없다 | 컬럼을 추가하지 않고 `DAT_TIMESERIES.COLLECT_PATH`(비고 "PLC→Gateway→시계열DB")에 **`시뮬레이터(PLC→Gateway)`** 를 적어 구분한다. `--realtime` 으로 넣은 것만 실수집 경로 문자열을 쓴다. 정규 출처 컬럼 신설을 제안 |
| **D-306** | 확정 | **`app/routers/ingest.py` 는 `main.py` 가 싣지 않는다.** `main.py` 는 `nav.all_screens()` 의 module 집합만 임포트하는데 인터페이스 3종(TD4-047·048·049)은 **화면이 없다** | `main.py` 를 건드리지 않고 `routers/agt.py` 끝에서 `router.include_router(ingest.router)` 로 싣는다. `ingest.SCREENS` 는 빈 튜플이라 placeholder 판정에 영향이 없다 |
| **D-307** | 가설 | **인증 전까지 질의 사용자 해석.** `AGT_QUERY_LOGS.USER_ID` 가 NOT NULL FK 라 사용자를 모르면 "질의 100% 기록" 이 깨진다 | `agent.service.resolve_user(role_code)` 가 `SYS_ROLE_PERMISSIONS.ROLE_CODE` 로 역할 대표 계정을 찾는다. **개발용 보조이며 D-40 의 연장이다** — 개발1 이 인증을 넣으면 세션이 대체하고 이 함수와 `main.py` 역할 전환 미들웨어를 함께 지운다. 남아 있으면 보안 결함 |
| **D-308** | 차단 | **확정 객체로 실제 집계 가능한 Feature 는 `홀 수량` 1종뿐이다.** TD5 `EST_CAD_FEATURES.FEATURE_TYPE` 어휘 6종 중 나머지 5종(총 절단장·판재 면적·용접장·재질·두께)은 `EST_CAD_OBJECTS` 에 원천 컬럼이 없다 | 0 이나 추정값으로 채우지 않는다. `cad.pipeline.BLOCKED_FEATURES` 에 사유를 적고 화면 011 이 그대로 렌더한다. 경로 길이·외곽 좌표·표제란 OCR 확보 후 해제 (D-05) |
| **D-309** | 확정 | **이번 회전에 ML 학습을 하지 않았다**(goal.md §10-2 — 학습이 있는 회전은 단독 기동) | `kyungdong/ml/` 에는 **읽기 경로와 차단 사유만** 둔다. `EST_ML_MODELS`·`_TRAIN_RUNS`·`_PREDICTIONS` 0건. 성능 수치를 만들지 않는다 |
| **D-310** | 확정 | **LLM·임베딩 공급자 호출 구현이 없다.** `.env` 에 값을 넣어도 `501 LLM 미구성` 이다 | "구성됐다고 선언했는데 실제로 없다" 를 조용히 넘기면 §2.5 위반이다. `agent/llm.py` 의 `KNOWN_PROVIDERS`·`_CHAT`·`_EMBED` 에 핸들러가 등록돼야 `configured` 가 참이 된다. 모델명·엔드포인트 확정 대기 (D-08) |
| **D-311** | 확정 | **한국어 키워드 검색은 `to_tsvector('simple', …)` 만으로 부족하다.** 조사가 붙으면 놓친다 | 시안과 같은 **바이그램 보조 검색**을 둔다(`retrieval.search` 2단계). 점수는 매칭 비율(0~1)이고 임계(D-10)와 비교한다. 임베딩이 들어오면 `vector_pgvector` 로 올라간다 |
| **D-312** | 확정 | **정제 규칙이 D-03 의 183건보다 더 많이 제외한다 — 실측 284건**(대체 축 기준) | 파일명 완전일치 중복은 **183건(D-03 재현)** 이고, 여기에 확장자만 다른 같은 도면(`.cad` ↔ `.dwg`)과 버전 토큰 분리로 묶이는 것이 더해져 **284건**이 된다. 두 수치를 모두 보고한다 — 하나만 적으면 오해를 부른다 |
| **D-313** | 확정 | **`app/routers/__init__.py` 는 소유자가 지정되지 않은 파일이다.** 패키지 인식용으로 docstring 만 넣었다 | 다른 개발자가 같은 파일에 내용을 넣으면 **충돌한다**. 여기에는 로직을 두지 않는다 |
| **D-314** | **확정 — D-308 정정** | **차단은 Feature 별이 아니라 `파일 형식 × Feature` 로 갈린다.** `.dxf` 는 순수 텍스트라 group code 파싱만으로 홀 수량·총 절단장·판재 면적·두께가 나온다(D-110-b). D-308 이 "5종 전부 차단" 이라 적은 것은 `.dwg`(2,240)·`.cad`(1,030) 에는 맞고 `.dxf`(29) 에는 틀렸다 | `cad/pipeline.FORMAT_FEATURE_SUPPORT` · `support_for()` · `blocked_for()` 를 추가해 형식별로 가른다. `BLOCKED_FEATURES` 의 **키 집합은 그대로 두고**(`tools/check_ingest.py` ⑤ 가 읽는다) 사유 문자열에 D-05 와 D-110-b 를 함께 적는다. **표본은 dxf 29/CAD 3,299 = 0.9%, 내용 해시 기준 서로 다른 도면 9건** — 이 수로 G-14(80%)를 주장하지 않는다 |
| **D-315** | 차단 | **`재질` 은 값이 읽혀도 적재할 자리가 없다.** DXF TEXT 표제란에서 5/9 도면의 재질(STS304·STS316L·SUS316·A240)을 읽었는데 `EST_CAD_FEATURES.FEATURE_VALUE` 가 `NUMERIC(16,4)` 다 | 0 이나 코드번호로 바꿔 넣지 않는다. `dxf.features()` 가 `value=None` · `text=<재질>` 로 돌려주고 적재기가 건너뛰며 사유를 적는다. **차단 사유가 D-05(OCR)가 아니라 스키마다** — 문자형 Feature 칸(또는 `SOURCE_DESC` 규약)을 아키텍트에게 요청 |
| **D-316** | 차단 | **`EST_PROJECTS` 는 폴더명 파싱으로 채울 수 없다.** `CUSTOMER_CODE` 가 NOT NULL 이고 `BAS_COMMON_CODES` '고객사' 그룹이 0건(D-47·D-56)이라 `require_code()` 가 422 다. QA2 `test_G07_공통시드가_멱등이다…` 도 `EST_PROJECTS == 0` 을 단언한다 | GD 폴더 **99개를 파싱 실패 0 으로 읽었고**(서로 다른 번호 34) 고객사 실명도 40/99 에서 읽혔지만 **파일시스템 파생 후보이지 마스터가 아니다**(D-114). 적재하지 않고 `docs/cad/archive_scan.json` 에 실측만 남긴다. 고객사 코드 마스터 확인이 선행 조건이다 |
| **D-317** | 차단 | **`EST_QUOTATIONS` 는 구조적으로 한 행도 못 넣는다.** `PROJECT_ID` 가 NOT NULL FK → `EST_PROJECTS` 인데 위가 0건이다 | 견적 총액 **22/26(고유 파일 기준)** · 납기 **8/26** · 품목 **462행**을 실제로 뽑아 리포트에만 남긴다. **이것이 D-04 의 Label 후보다 — 후보와 Label 은 다르다.** D-316 이 풀리면 같이 풀린다 |
| **D-318** | 확정 | **견적 총액은 "금액 표기가 있다" 와 "정확히 뽑았다" 가 다르다.** D-111 은 29/31 에서 금액 표기가 읽힌다고 적었으나, 실제 추출은 **파일 경로 27/31 · 내용이 서로 다른 파일 22/26** 이다 | 추출 방식을 `숫자셀`·`괄호숫자`·`통화기호`·`한자금액` 넷으로 나누고 **어느 방식으로 뽑았는지 함께 저장**한다. 한 행에 금액 후보가 둘 이상이면(`전기견적-석광.xls` 1,759,443 vs 5,200,000) **고르지 않고 실패로 적는다**. 명세표의 `금  액` 열 머리글과 구별이 안 되므로 **맨 '금액' 은 총액 라벨에 넣지 않는다** — 넣었더니 품목 단가를 총액으로 집어 왔다 |
| **D-319** | 확정 | **한 파일이 견적 한 건이 아니다.** `반응기2000-타견적.xls` 한 통합문서에 갑지가 2장(8000리터·2000리터) 들어 있고, `견적서갑지세로.xls` 는 48장이 전부 다른 건설 설비공사 견적이다 | 파일 단위 총액과 **갑지(시트) 단위 총액(90건)** 을 따로 센다. 첫 갑지를 대표값으로 쓰되(`total_amount`) 나머지는 `sheet_totals` 에 전부 남긴다 — 버리면 Label 후보를 조용히 잃는다 |
| **D-320** | 확정 | **복사본을 걷어내지 않으면 표본이 부풀려진다.** `20 경동도면함/` 아래에 16~19 도면함이 통째로 또 들어 있고 `CAD도면_제품별정리(신규)/` 는 같은 파일에 `113_2020-11-14_` 접두어를 붙여 놨다 | **내용 해시**(앞 1MB + 뒤 1MB + 크기)로 가른다. dxf 29 → **서로 다른 도면 9**, 견적 xls 31 → **서로 다른 파일 26**, GD 폴더 99 → **서로 다른 번호 34**. 리포트에 둘 다 적는다 |
| **D-321** | 확정 | **DXF 인코딩을 앞부분만 보고 정하면 틀린다.** 145MB 도면은 앞 64KB 가 ASCII 라 무엇으로든 읽히는 것처럼 보인다 — D-115 와 같은 함정이다 | `dxf.detect_encoding()` 이 **파일 전체**를 증분 디코더로 검증한다. UTF-8 를 먼저 엄격 검증하고, 안 되면 `$DWGCODEPAGE` 선언(ANSI_949→cp949 등), 그 다음 후보 순회, 마지막이 `latin-1` 이다. 실측 9건: utf-8 4 · cp949 2 · cp1252 3 |
| **D-322** | 확정 | **DXF 길이·개수는 과소 계상이다.** `INSERT` 가 참조하는 `BLOCK` 내부 형상을 전개하지 않았다 | 전개하면 블록 삽입 변환(스케일·회전)까지 맞춰야 하는데 검증할 정답이 없다. 대신 `inserts` 횟수를 **Feature 근거 문자열에 같이 적어** 과소 계상 사실이 화면에 드러나게 한다 (실측 최대 2,050회) |
| **D-323** | 차단 | **길이 단위를 모르는 도면이 많다.** `$INSUNITS` 가 있는 것은 9건 중 1건뿐이다 | mm 라고 단정하지 않는다. 없으면 `UOM` 을 **`도면단위`** 로 적는다. 단위를 모르는 절단장은 원가 환산에 바로 못 쓴다 |
| **D-324** | **확정 — D-314 재정정** | **`.dwg` 는 구조적 불가가 아니었다. 변환기가 없었을 뿐이다.** `dwg2dxf`(libredwg 0.14)로 바꾸면 `cad/dxf.py` 가 **그대로** 돈다 — 전량 실측 `.dwg` 802건 중 **772건 변환 성공(96.3%)**, 변환분 파싱 **100%** (D-123) | `BLOCKED_FEATURES` 사유를 "DWG·CAD 는 바이너리라 못 읽는다" → **"변환 후 산출 가능 · 변환 실패분은 차단"** 으로 바꿨다. **D-05 를 지우지 않았다** — 결정이 무효가 된 게 아니라 차단 모집단이 3,270건에서 **서로 다른 도면 355건**으로 줄었다. `support_for('DWG')` 는 `shutil.which('dwg2dxf')` 로 재서 변환기가 없으면 빈 표(= D-05 그대로)를 돌려준다 |
| **D-325** | 차단 | **`.cad` 1,030건(서로 다른 도면 325건)은 DWG 가 아니다.** 앞 6바이트가 `V10.00`(324건)·`V15.00`(1건) 으로 **AutoCAD 서명 `AC1xxx` 가 아니다** → libredwg 의 대상 자체가 아니고 변환 성공 **0건(0%)** 이다 | 확장자만 보고 "DWG 계열" 로 묶지 않는다. 어느 CAD 의 포맷인지 **단정하지 않는다** — 도입기업 확인 요청 ⑪ 로 올린다. `FORMAT_FEATURE_SUPPORT['CAD'] = {}` 로 전량 차단을 유지한다 |
| **D-326** | 확정 | **변환 실패는 DWG 버전으로 완전히 갈린다.** `AC1014`(R14)·`AC1015`(R2000)·`AC1018`·`AC1021`·`AC1024`·`AC1027`·`AC1032` 는 **전부 100%** 성공하고, `AC1009`(R11/R12) 30건은 **전부 0%**(`DWG_SENTINEL_R11_*_BEGIN/END not found`) 다 | 실패 사유 분류 바구니를 **지어내지 않았다.** ① 변환기 stderr 첫 오류줄에서 경로·숫자·주소만 지워 정규화한 문자열 ② 파일 앞 6바이트 서명, 둘을 교차해 센다. 그러니 "30%가 실패한다" 가 아니라 **"R11/R12 와 비-DWG 가 실패한다"** 로 적을 수 있다 |
| **D-327** | 차단 | **전량 실측 Feature 를 `EST_CAD_FEATURES` 에 넣으면 G-13 이 PASS→FAIL 이 된다 — 실제로 재 봤다.** 도면 781행·Feature 3,226행을 적재하니 `check_ingest` ⑤ 가 결함 2건(확정 객체 0 + Feature N행 = 합성 / 차단 Feature 적재)을 냈다 | **그 규칙이 맞다.** 확정(`CONFIRM_YN='Y'`)은 `review_object()` 의 사람 승인으로만 생기고(G-24) 승인을 지어낼 수는 없다. 규칙을 우회하지 않고 **적재를 `--load-db --load-features` 명시 옵션으로만** 두고 기본은 0행이다. 실측값의 정본은 `docs/cad/dwg_features.json` 이다. `EST_CAD_DRAWINGS` 는 `PROJECT_ID` 가 NULL 허용이라 **프로젝트 없이도 적재 가능**임을 실측으로 확인했다(D-120 은 `EST_PROJECTS`·`EST_QUOTATIONS` 에만 걸린다) |
| **D-328** | 차단 | **표본이 1,136건이 돼도 G-14 는 차단이다.** 우리가 뽑은 것은 **기하 집계**(홀 수량·절단장·면적)이고 G-14 가 요구하는 것은 **정답 라벨 대비 IoU 0.5 P/R/F1** 이다. `work/cad_labelset.json` 의 `labels`·`predictions` 가 **여전히 0건**이라 TP/FP/FN 의 분모가 없다 | **표본이 늘어난 것과 라벨이 생긴 것은 다른 일이다.** 둘을 섞어 적지 않는다. 다만 차단의 축은 줄었다 — 예측 박스는 이제 변환된 DXF 에서 뽑을 수 있으므로 **남은 유일한 구속은 정답 라벨**이다. 그래도 라벨 0건이면 80%도 0%도 아니다 |

---

## 시그니처 변경 요청 — 아키텍트에게

| 대상 | 요청 |
|---|---|
| `contracts/api-contract.md` §5 | `ADOPT_YN`·`ADOPT_BY` → `REVIEW_STATUS`·`REVIEWER_ID` 로 문구 수정 (D-302) |
| `contracts/db-schema.md` §7 | `EST_SHAP_FACTORS` 를 §7.1(미정)에서 §7(런타임 전용)로 이동 (D-301) |
| `contracts/interfaces.md` §9 | `ingest` 수집 API 행에 `collector.status()` 공표 시그니처 링크 추가 |

## 도입기업 확인 요청

1. **과거 견적금액·실제 제조원가 Label** 보유 여부와 규모 (D-04) — 없으면 G-15·G-17 은 영구 차단이다.
2. **Autodesk API 계정 · YOLOv8 가중치 · OCR 엔진** 확보 계획 (D-05).
3. **도면 고객사 메타데이터** — 중복 제거 키의 세 번째 축 (D-304).
4. **설명가능성 전문가 평가 변수 목록** (D-13) — 없으면 G-18 차단.
5. **LLM·임베딩 모델명과 엔드포인트** (D-08).
6. **품목·재질 코드 마스터** (D-47) — 없으면 BOM Rule 전개가 돌지 않는다.
7. **고객사 코드 마스터** (D-114 · D-316) — 폴더명에서 두루텍·한모루·동광제약·웰이엔씨·대한열기·
   대호테크·엔에프테크·비나텍·그린텍을 읽었다. **공식 코드인지 확인이 필요하다.** 확인되면
   `EST_PROJECTS` → `EST_QUOTATIONS` 가 한 번에 열리고 G-08·G-15·G-17 의 전제가 풀린다.
8. ~~**`.dwg` → `.dxf` 변환** (D-314)~~ → **`.dwg` 는 해소됐다** (D-324) — `dwg2dxf`(libredwg)로
   802건 중 772건(96.3%)을 직접 변환했다. 남은 것은 R11/R12 30건(⑫)과 `.cad` 325건(⑪)이다.
9. **홀 지름 기준** (D-314) — `CIRCLE` 중 무엇이 가공 홀인지 가르는 지름 범위. 없으면 '원 전량' 이다.
10. **문자형 Feature 칸** (D-315) — `EST_CAD_FEATURES.FEATURE_VALUE` 가 NUMERIC 이라 재질을 못 담는다.
11. **`.cad`(V10.00) 포맷의 출처 CAD** (D-325) — 1,030건(서로 다른 도면 325건)이 AutoCAD DWG 가
    아니다. 어느 프로그램으로 만든 것인지와 DWG·DXF 로 내보낼 방법이 필요하다. 이것이 풀리면
    표본이 서로 다른 도면 781 → 1,106건으로 늘어난다.
12. **R11/R12 DWG 30건** (D-326) — libredwg 0.14 가 못 읽는다. 최신 AutoCAD 로 다시 저장해 주면 된다.
13. **CAD 객체 정답 라벨** (D-328) — G-14 의 유일한 남은 구속이다. 예측은 이제 만들 수 있다.

---

## D-150 — 가정 답변으로 예행한다 (사용자 지시 2026-09-12)

**확정 — 중요.** 도입기업 답변은 **오지 않았다.** 사용자가 "요청서를 받았다고 가정하고
진행해줘" 라고 지시했다. 가정한 답변은 `docs/assumed/customer_answers.json` **한 곳**에 모았고
**그 파일이 가정의 유일한 출처**다. 파일 밖의 값을 만들면 그것은 가정이 아니라 조작이다.

**이것은 AI 정확도를 증명하는 작업이 아니다.** 데이터가 실제로 도착했을 때 파이프라인이
도는지 보는 **예행**이고, 값은 **코드 경로 검증**에 있다(G-08 합성이 제품 결함 3건을 잡아낸 것과 같은 자리).

**선언은 두 겹이다.** ① 파일 ② `SYS_CONFIGS('시스템설정','ASSUMED_ANSWERS')='D-150'`.
둘이 같은 결정번호를 가리킬 때만 `app/util/assumed.declaration()` 이 값을 돌려주고,
아니면 파생물은 **전부 차단**이다. `SYNTHETIC_THREAD`(D-131) 와 같은 방식이다.

**고지 문구를 코드에 박지 않는다** — 선언 파일의 백틱 안에서 꺼낸다. 박아 두면 선언을 지워도
문구가 남아 거짓 증거가 된다.

**G-14 는 순환이다.** 정답 라벨과 예측을 우리 DXF 파싱에서 둘 다 뽑았다. 그래서 나온 수치는
**측정이 아니라 우리가 고른 값**이고, 판정 줄에 `가정 답변 기준 (D-150) · 순환 라벨 —
정확도 증거 아님` 을 반드시 함께 찍는다. `PASS` 로 올리지 않고 **판정 불가**다.

**`제공되지_않은_것` 5건은 채우지 않는다** — G-15·G-17·G-18 은 **차단이 정답**이다.

**갈아끼우는 법**: `docs/assumed/customer_answers.json` 의 값을 실제 답변으로 바꾸고
`make db-reset && make db-seed`. 파일을 지우면 파생물이 회수되고 게이트가 차단으로 돌아간다
(실측 확인 — `progress-dev3.md` ⑤).
