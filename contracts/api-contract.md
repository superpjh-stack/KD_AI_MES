# contracts/api-contract.md — 라우트 계약

> **생성 파일이다.** 정본은 `src/kyungdong/app/nav.py`(경로·소유) + SF-TD4(처리 흐름).
> `tools/gen_api_contract.py` 가 뽑는다. **코드와 다르면 코드가 맞다** → `nav.py` 를 고치고 다시 뽑는다.

## 0. 규약

| # | 규약 |
|---|---|
| 1 | 화면은 **GET = 조회 렌더**. 등록·수정은 같은 경로에 **POST**, 삭제는 **POST + `_method=delete`**(폼 전송). |
| 2 | 조회 조건은 **쿼리스트링**, 저장 payload 는 **폼**. JSON API 는 Agent·수집·CAD 만 쓴다. |
| 3 | **모든 업무 화면의 최상위 조회 조건은 프로젝트(수주)번호·도면번호**다(TD3 standard_note 2). |
| 4 | 권한 없음 = **403**(G-28). 좌측 메뉴는 권한 있는 것만 노출한다. |
| 5 | **LOT·프로젝트번호 열은 클릭 가능**하고 `/prc/024`(공정이력조회)로 간다(G-08). |
| 6 | 오류는 §4 계약 코드로만 낸다. **조용한 200 금지**(G-30). |
| 7 | 페이지네이션은 `?page=&size=` (기본 size 20). 정렬은 `?sort=<컬럼>&dir=asc|desc`. |

## 1. 공통 화면

| 경로 | 메서드 | 화면 | 담당 |
|---|---|---|---|
| `/login` | GET | 로그인 화면 | 아키텍트 → 개발1(인증·레이아웃) / 개발2(현황판) |
| `/` | GET | 메인시안 | 아키텍트 → 개발1(인증·레이아웃) / 개발2(현황판) |
| `/board` | GET | 대시보드 화면 | 아키텍트 → 개발1(인증·레이아웃) / 개발2(현황판) |
| `/popup` | GET | 팝업 화면 | 아키텍트 → 개발1(인증·레이아웃) / 개발2(현황판) |
| `/error` | GET | 공통 오류 화면 | 아키텍트 → 개발1(인증·레이아웃) / 개발2(현황판) |
| `/health` | GET | 헬스체크 — 정본 규모 대조. 불일치면 **500** | 아키텍트 |
| `/static/app.css` | GET | 단일 CSS(§10-13) | 아키텍트 |

## 2. 화면 45

| 경로 | 화면 | 요구사항 | 프로그램 | 담당 | 처리 흐름 (SF-TD4) |
|---|---|---|---|---|---|
| `GET /dsh/001` | 001 생산현황 분석 | MES-AD2-001 | MES-TD4-001 | 개발2 | 관리자 Web·현황판 → 대시보드 API → 공정실적·설비신호 집계 → 결과 반환(주기 갱신) |
| `GET /dsh/002` | 002 품질현황 분석 | MES-AD2-002 | MES-TD4-002 | 개발2 | 관리자 Web → 품질 대시보드 API → 검사결과·공정실적 집계 → 이상 LOT 표시 |
| `GET /dsh/003` | 003 설비상태 모니터링 | MES-AD2-003 | MES-TD4-003 | 개발2 | PLC → Data Gateway → 수집 API → 설비신호 적재 → 대시보드 표시 / 임계 초과 시 알림 |
| `GET /dsh/004` | 004 출하현황 분석 | MES-AD2-004 | MES-TD4-004 | 개발2 | 관리자 Web → 출하 대시보드 API → 출하·프로젝트·LOT 집계 → 납기 리스크 표시 |
| `GET /est/010` | 010 CAD 도면분석 | MES-AD2-010 | MES-TD4-010 | 개발3 | 관리자 Web → CAD 분석 API → 파일 수집(MES-TD4-048) → Parsing·Vision 병렬 실행 → 객체·도면 DB 저장 |
| `GET /est/011` | 011 객체인식 결과관리 | MES-AD2-011 | MES-TD4-011 | 개발3 | 관리자 Web → 객체관리 API → 검증 이력 기록 → 객체 확정 → Feature 생성·저장 |
| `GET /est/012` | 012 견적자동산출 | MES-AD2-012 | MES-TD4-012 | 개발3 | 관리자 Web → 견적 API → Feature·단가 조회 → XGBoost 예측 → 공정별 원가 산출 → 견적 DB 저장 |
| `GET /est/013` | 013 BOM자동생성 | MES-AD2-013 | MES-TD4-013 | 개발3 | 관리자 Web → BOM API → 객체 관계 분석 → BOM 헤더/자재/공정구조 생성 → 소요량 산출 → DB 저장 |
| `GET /est/014` | 014 ML분석 | MES-AD2-014 | MES-TD4-014 | 개발3 | 관리자 Web → ML API → 데이터셋 조회 → 학습 실행 → 성능 지표 기록 → 모델 배포 상태 갱신 |
| `GET /est/015` | 015 영향요인분석 | MES-AD2-015 | MES-TD4-015 | 개발3 | 관리자 Web → 영향요인 API → 예측결과 조회 → SHAP 산출·저장 → Top-N 리포트 출력 |
| `GET /inv/005` | 005 입고관리 | MES-AD2-005 | MES-TD4-005 | 개발1 | 스마트패드·관리자 Web → 입고 API → LOT 채번·정합성 검증 → 입고/LOT/재고 DB 저장 |
| `GET /inv/006` | 006 원자재 이력조회 | MES-AD2-006 | MES-TD4-006 | 개발1 | 관리자 Web → 이력조회 API → 자재이력·LOT추적 조인 조회 → 결과 표시 |
| `GET /inv/007` | 007 입고 데이터관리 | MES-AD2-007 | MES-TD4-007 | 개발1 | 관리자 Web → 데이터관리 API → 정합성 검증·표준화 → ERP 인터페이스(MES-TD4-046) 송수신 → 결과 기록 |
| `GET /inv/008` | 008 공급처 품질분석 | MES-AD2-008 | MES-TD4-008 | 개발1 | 관리자 Web → 공급처 분석 API → 입고·검사·클레임 집계 → 평가 결과 저장 → 리포트 출력 |
| `GET /inv/009` | 009 입고 AI Agent | MES-AD2-009 | MES-TD4-009 | 개발3 | 스마트패드 → Agent API → Vector DB 검색(입고기준서·검사기준서) + LOT 데이터 조회 → 응답 생성 → 질의이력 저장 |
| `GET /prc/021` | 021 공정실적관리 | MES-AD2-021 | MES-TD4-021 | 개발2 | 현장POP·PLC → 공정실적 API → 작업지시 검증 → 실적 DB 저장 → KPI·대시보드 반영 |
| `GET /prc/022` | 022 공정 데이터 모니터링 | MES-AD2-022 | MES-TD4-022 | 개발2 | PLC → Data Gateway → 수집 API → 시계열DB 적재 → 임계 비교 → 화면 표시·알림 |
| `GET /prc/023` | 023 작업조건관리 | MES-AD2-023 | MES-TD4-023 | 개발2 | 관리자 Web → 작업조건 API → 표준조건 저장 → 실측값 대조 → 편차 산출·알림 |
| `GET /prc/024` | 024 공정이력조회 | MES-AD2-024 | MES-TD4-024 | 개발2 | 관리자 Web → 공정이력 API → LOT 기준 이력 조회 → 체류시간 산출 → 결과 표시 |
| `GET /prc/025` | 025 공정데이터 분석 | MES-AD2-025 | MES-TD4-025 | 개발2 | 관리자 Web → 분석 API → 공정이력·실적·편차·검사 조인 집계 → 병목 판정 → 리포트 출력 |
| `GET /shp/016` | 016 출하관리 | MES-AD2-016 | MES-TD4-016 | 개발2 | 관리자 Web → 출하 API → 검사 합격 검증 → 출하 DB 저장 → ERP 인터페이스(MES-TD4-046) 전송 |
| `GET /shp/017` | 017 LOT추적관리 | MES-AD2-017 | MES-TD4-017 | 개발2 | 관리자 Web·현장POP → LOT추적 API → 자재LOT·작업지시·공정이력 연결 → 추적 DB 저장/조회 |
| `GET /shp/018` | 018 검사결과관리 | MES-AD2-018 | MES-TD4-018 | 개발2 | 관리자 Web·스마트패드 → 검사 API → 품질기준 대조·판정 → 검사 DB 저장 → 출하 가능 상태 갱신 |
| `GET /shp/019` | 019 클레임분석 | MES-AD2-019 | MES-TD4-019 | 개발2 | 관리자 Web → 클레임 API → LOT·검사·공정이력 조회 → 원인분석 저장 → 리포트 출력 |
| `GET /shp/020` | 020 출하 AI Agent | MES-AD2-020 | MES-TD4-020 | 개발3 | 관리자 Web·스마트패드 → Agent API → Vector DB 검색(출하기준서·클레임 매뉴얼) + 검사·LOT 조회 → 응답 생성 → 이력 저장 |
| `GET /bas/030` | 030 품질기준 관리 | MES-AD2-030 | MES-TD4-030 | 개발1 | 관리자 Web → 기준정보 API → 중복·범위 검증 → 품질기준 DB 저장 → 검사·품질현황 화면 참조 |
| `GET /bas/031` | 031 작업표준관리 | MES-AD2-031 | MES-TD4-031 | 개발1 | 관리자 Web → 기준정보 API → 작업표준 저장 → SOP 문서 임베딩(MES-TD4-049) → 표준조건 연계 |
| `GET /bas/032` | 032 코드관리 | MES-AD2-032 | MES-TD4-032 | 개발1 | 관리자 Web → 기준정보 API → 코드 중복 검증 → 공통코드 DB 저장 → 전 모듈 참조 |
| `GET /dat/033` | 033 데이터통합관리 | MES-AD2-033 | MES-TD4-033 | 개발1 | 관리자 Web → 데이터통합 API → 수집대상·작업 설정 저장 → ETL 실행 → 로그·품질 결과 기록 |
| `GET /dat/034` | 034 데이터조회 | MES-AD2-034 | MES-TD4-034 | 개발1 | 관리자 Web → 데이터조회 API → 시계열·비정형·LOT 조인 → 연계율 산출 → 결과 표시 |
| `GET /dat/035` | 035 데이터시각화 | MES-AD2-035 | MES-TD4-035 | 개발1 | 관리자 Web → 시각화 API → 시계열·KPI 집계 → 차트 데이터 반환 → 화면·현황판 표출 |
| `GET /dat/036` | 036 데이터 다운로드 | MES-AD2-036 | MES-TD4-036 | 개발1 | 관리자 Web → 다운로드 API → 권한 검증 → 파일 생성 → 다운로드 이력 저장 |
| `GET /dat/037` | 037 AI학습 데이터관리 | MES-AD2-037 | MES-TD4-037 | 개발3 | 관리자 Web → 학습데이터 API → 전처리 규칙 적용 → 데이터셋·항목·분할 저장 → 모델 학습 연계 |
| `GET /agt/038` | 038 통합 AI질의 | MES-AD2-038 | MES-TD4-038 | 개발3 | 관리자 Web·스마트패드 → 통합 Agent API → Vector 검색 + 운영 DB 조회 → 응답 생성 → 질의이력 저장 |
| `GET /agt/039` | 039 생산/품질 분석 | MES-AD2-039 | MES-TD4-039 | 개발3 | 배치·요청 → 분석 API → 실적·검사·편차 집계 → 분석 결과 저장 → 관리자 검토·승인 |
| `GET /agt/040` | 040 의사결정 지원 | MES-AD2-040 | MES-TD4-040 | 개발3 | 관리자 Web → 의사결정 API → 예측·표준조건 조회 → 추천 생성·저장 → 승인 시 운영계획 반영 |
| `GET /agt/041` | 041 알림 및 추천 | MES-AD2-041 | MES-TD4-041 | 개발3 | 이벤트 발생 → 알림 API → 알림 기준(SYS_CONFIGS) 대조 → 알림 발송·추천 저장 → 확인 처리 기록 |
| `GET /agt/042` | 042 사용자 질문이력 | MES-AD2-042 | MES-TD4-042 | 개발3 | 관리자 Web → 질의이력 API → 이력 조회 → 개선 대상 표시 → 재학습·문서 보강 연계 |
| `GET /kpi/043` | 043 생산성 KPI 조회 | MES-AD2-043 | MES-TD4-043 | 개발2 | 관리자 Web → KPI API → 작업지시·공정이력·포장완료 집계 → 리드타임 산출 → KPI 측정실적 저장 |
| `GET /kpi/044` | 044 품질 KPI 조회 | MES-AD2-044 | MES-TD4-044 | 개발2 | 관리자 Web → KPI API → 검사·불량·클레임 집계 → 품질 지표 산출 → 대시보드 표시 |
| `GET /kpi/045` | 045 KPI 관리 | MES-AD2-045 | MES-TD4-045 | 개발2 | 관리자 Web → KPI 관리 API → 목표 저장 → 출하·프로젝트 시각 집계 → 달성률 산출·저장 |
| `GET /sys/026` | 026 사용자 관리 | MES-AD2-026 | MES-TD4-026 | 개발1 | 관리자 Web → 사용자 API → 중복·정책 검증 → 사용자/권한 DB 저장 → 접속로그 기록 |
| `GET /sys/027` | 027 로그 관리 | MES-AD2-027 | MES-TD4-027 | 개발1 | 관리자 Web → 로그 API → 로그 테이블 조회 → 결과 표시/다운로드 이력 기록 |
| `GET /sys/028` | 028 알림 설정 | MES-AD2-028 | MES-TD4-028 | 개발1 | 관리자 Web → 설정 API → 알림 기준 저장 → 이벤트 발생 시 알림 발송 → 결과 기록 |
| `GET /sys/029` | 029 시스템 설정 | MES-AD2-029 | MES-TD4-029 | 개발1 | 관리자 Web → 설정 API → 설정 저장 → 인터페이스 연계 상태 확인 → 결과 표시 |

## 3. 화면 없는 인터페이스 (EIF 4건 — TD4-046~049)

화면이 없다. 연계 **상태 조회**는 `/dat/033`(데이터통합관리)에서 한다(G-05).

| 프로그램 | 이름 | 제안 엔드포인트 | 담당 | 처리 흐름 |
|---|---|---|---|---|
| MES-TD4-046 | ERP 연계 인터페이스 | `POST /api/if/erp/{receipts|shipments|stocks}` · Excel 적재도 같은 경로(D-07) | 개발1 | 스케줄러/이벤트 → ERP 연계 배치 → API·DB 조회 → 매핑·검증 → 연계 테이블 적재 → 운영 테이블 반영 |
| MES-TD4-047 | IoT/PLC 수집 인터페이스 | `POST /api/ingest/plc` (태그 배열) · `GET /api/ingest/status` | 개발3 | PLC/터치PC → Data Gateway(OPC-UA·Modbus) → 프로토콜 변환·버퍼링 → MQTT/Kafka 전송 → 수집 API → 시계열DB 적재 |
| MES-TD4-048 | CAD 파일 수집 인터페이스 | `POST /api/cad/files` (multipart) · `GET /api/cad/import-logs` | 개발3 | 파일 업로드/배치 스캔 → 형식·크기 검증 → 중복·손상 제외 → Data Lake 적재 → 도면 마스터 등록 → 분석 대기열 등록 |
| MES-TD4-049 | 외부 표준문서 수집 인터페이스 | `POST /api/docs/import` · `GET /api/docs/embed-logs` | 개발3 | 문서 수집 → 텍스트 추출 → 청크 분할 → 임베딩 생성 → Vector DB 적재 → 처리 로그 기록 |

## 4. 오류 계약 (goal.md §2.5 — QA1 테스트 케이스 원본)

| 상황 | HTTP | 화면 문구 |
|---|---|---|
| 필수값 누락 / 코드 중복 / 상·하한 역전 / 검사 합격 아닌 LOT 출하 선택 / 승인 전 견적 확정 | **422** | 입력값을 확인해 주세요 |
| unauthenticated | **401** | 로그인이 필요합니다 |
| 권한 없음 / 승인 권한 없는 추천값 반영 (G-24·G-28) | **403** | 접근 권한이 없습니다 |
| db_down | **503** | 서비스 일시 중단 |
| API 키 없음 — 조용한 폴백 금지 (D-08) | **501** | LLM 미구성 |
| Autodesk API·YOLOv8 미확보 — 조용히 합성 Feature 를 내지 않는다 | **501** | CAD Parsing 미구성 (D-05) |
| SYS_ACCESS_LOGS 기록 + 공통 오류 화면 | **500** | 처리 중 오류가 발생했습니다 |

**200 으로 렌더하되 숨기지 않는 것**

| 상황 | 표시 |
|---|---|
| 임베딩 미구성 | `검색 모드: tsvector_keyword (D-08)` — 검색 모드 라벨 |
| RAG 근거 0건·신뢰도 미달 | `검토 필요 — 근거 부족` + 담당자 이관 (G-22) |
| 수집 중단 | `수집 중단 — 마지막 수집 hh:mm:ss` 배지 |
| 값 미확정 | `미확정 (D-nn)` 배지 |
| 수집 대상 아님 | `미수집 (D-nn)` — 수집 지점 2개소 밖 공정 (D-06) |
| 가설값 | `가설 (D-nn)` 배지 |

## 5. Agent API (개발3)

| 경로 | 메서드 | 설명 |
|---|---|---|
| `/api/agent/query` | POST | 통합 질의. **폐쇄형** — 외부 검색 0. 근거 0건이면 LLM 을 부르지 않고 `검토 필요` |
| `/api/agent/history` | GET | `AGT_QUERY_LOGS` 조회 (화면 042) |
| `/api/agent/recommend/{id}/adopt` | POST | 추천 채택. **승인 권한 없으면 403**, `ADOPT_YN`·`ADOPT_BY` 기록 (G-24) |

질의·응답·근거·응답시간을 **100% `AGT_QUERY_LOGS` 에 기록**한다(G-20). LLM 미구성이면 **501**, 임베딩 미구성이면 200 + `tsvector_keyword` 라벨.

## 6. 승인이 필요한 쓰기 (G-24 — 승인 없이 바뀌면 결함)

| 대상 테이블 | 무엇 | 승인 역할 |
|---|---|---|
| `EST_QUOTATIONS` | 견적 확정 | 승인 권한(A) 보유 역할 |
| `EST_BOM_HEADERS` | BOM 확정 | 〃 |
| `SHP_INSPECTIONS` | 검사 합격 판정 | 품질 담당 |
| `SHP_SHIPMENTS` | 출하 확정 — **검사 합격 LOT 만**(아니면 422) | 〃 |
| `PRC_STD_CONDITIONS` | 표준 작업조건 변경 | 생산관리 |
| `EST_CAD_OBJECTS` | 객체인식 결과 확정 | HITL 검증자 — `EST_OBJECT_REVIEWS` 기록 |

AI 는 **추천까지**다. 승인 전에는 **확정 상태로 표기하지 않는다**(TD3 standard_note 4).
