# /goal — 경동글로벌텍 제조AI 45화면 실연동 (SF26179182 · 06 Coding Agent · 루프 엔지니어링)

> **이 문서가 메타프롬프트다.** 투입되는 7명(아키텍트 1 · 개발 3 · QA 3)은 작업 시작 전 이 문서와
> `contracts/` · `decisions.md` · `progress.md` 를 먼저 읽는다. 여기 적힌 수치는 전부 정본(설계 JSON · 분석 JSON ·
> 사업계획서 보완본 · 사업비 산출내역서 v0.97)에서 **실측한 값**이다. 지어낸 값은 하나도 없다. 실측 방법은 §9 에
> 적어 두었으므로 의심되면 다시 세어 본다. 방법론은 직전 사업(광성정밀·평창꽃순이김치·송월타월)과 같으며,
> 그 사업들에서 실제로 겪은 문제(§10)를 이 문서에 미리 반영했다.

| 항목 | 값 | 출처 |
|---|---|---|
| 도입기업 | **경동글로벌텍** (대표 제미애 · 경기 화성시 마도면 청원로 238-22 · 설립 2013-04-24 · **종업원 8명** · 매출 '22년 9억 / '23년 9억 / **'24년 13억**) | 사업계획서 보완본 1.1 · AD1 |
| 업종·제품 | **ETO(Engineer-To-Order) 주문 설계·제작 고정밀 산업장비.** 반응기(Reactor) · 교반기(Agitator) · 진공건조기 · 누체필터(Nutsche Filter) · 저장탱크(Receiver Tank) · Pure Steam Generator. 제약·식품·화학·바이오 공급. **ASME · KS** 인증 | 사업계획서 1.1 · AD1 |
| 공급기업 · 과제번호 | **주식회사 재일** (대표 김형미, 컨소시엄 없음) · **SF26179182** | design.json `meta` · AD1 |
| 시스템명 | 경동글로벌텍 제조AI 시스템 | `meta.system_name` · `td3.solution_name` |
| 사업비 | 총 **398,510천원** = 개발비 200,000 + H/W 58,150 + S/W 50,000 + N/W 16,000 + 기타 2,360 + 도입기업 인건비(현물) 36,000 + 클라우드 36,000. 정부지원금 **199,255** / 부담금 163,255 / 현물 36,000 | 사업계획서 4.1 (TD2 정합 확인) |
| 규모 | **화면 45 / 업무영역 10 / 프로그램 49 / 테이블 68 (컬럼 762)** | TD3·TD4·TD5 실측 (§9) |
| 요구사항 | **기능 45 (MES-AD2-001~045) + 비기능 13 (046~058) = 58** | AD2 실측 (§9) |
| 공정 **10단계** | 수주견적관리 → 입고검사 → **소재가공(외주)** → 가공(**레이저 커팅**) → 제관 및 용접 → **버핑(외주)** → 조립 → 출하검사(**수압·기밀·진공**) → 포장 → 출하 | 사업계획서 1.3 · TD1 |
| **AI 과제 = 3대 공정** | ① **수주·견적 — CAD 견적 자동화 AI** (Parsing + Vision AI → 객체인식 → BOM 자동생성 → 견적 자동산출 → SHAP) ② **입고·보관 — 입고 RAG AI Agent** ③ **보관·출하 — 출하 RAG AI Agent** | 사업계획서 2.5 |
| **예지보전 아님** | 가공(포밍)·제관 및 용접·포장 공정의 **설비 예지보전·품질예측(용접 비드 이미지 판정 포함)은 사업계획서 2.5 에서 "현 사업 미적용"** 으로 명시. 화면·프로그램·테이블·모델을 만들지 않는다 → **D-01** | 사업계획서 2.5 · TD1 overview · TD2 review |
| AI 모델 | **Autodesk API Parsing + YOLOv8 + OCR**(도면 객체 인식) · **Rule + GNN**(BOM 생성) · **XGBoost**(견적 원가) · **SHAP**(영향요인) · **LangChain/LangGraph + LLM + pgvector**(폐쇄형 RAG) | 사업계획서 2.5·2.4 · TD2 sw_layers |
| **수집 지점 2개소뿐** | 신규 레이저커팅기 **Master PLC 1지점**(Ethernet/OPC-UA — 작업시간·수량·가동상태) + **터치PC(현장POP) 1지점**. 그 외 공정 실적은 터치PC·스마트패드 **수동 입력**. 전 공정 실시간 수집을 전제하지 않는다 | 사업계획서 2.7.1 |
| **KPI (확정)** | **제조 리드타임 1,320h → 1,080h**(−18.2%, 가중치 0.5) · **수주출하 리드타임 1,440h → 1,200h**(−16.7%, 가중치 0.5) | 사업계획서 1.5 |
| AI 성능목표 5종 | CAD 객체 인식 **80% 이상** / 견적 산출 정확도 **±5% 이내** / BOM 생성 정확도 **85% 이상** / 납기 예측 정확도 **85% 이상** / 설명가능성 신뢰도 **90% 이내** | 사업계획서 2.6 (**산식 칸 5개 전부 공란 — D-13**) |
| 데이터 유효성 | 정확성 **85%↑** · 정합성 **85%↑** · 연계성 **85%↑** · AI 학습 적합성 **판정** | 사업계획서 2.7.4 · AD2-056 |
| 목표 수준 | 기초 → 중간1 | 사업계획서 1.1 |
| 코드 위치 | `/Users/gerardo92/Desktop/AI Coding/KyungDong AI Platform` (이 폴더) | — |

---

## 0. 이 문서를 어떻게 쓰는가

### 0.1 루프 기동 — 이 한 줄을 붙여 넣는다

```
/loop goal.md 를 판정 기준으로 06 Coding Agent 를 돌린다. §4.2 한 회전 절차를 그대로 따른다 —
progress.md·decisions.md 를 읽고, `make gate` 로 §2 게이트 G-01~G-30 을 실측하고, FAIL 중 가장 앞선
웨이브의 항목 하나를 골라 §5 프롬프트로 담당 에이전트(아키텍트 1 · 개발 3 · QA 3, 병렬 가능한 것은 한 번에,
단 ML 학습이 있는 개발3 은 단독)를 기동하고, 보고를 믿지 말고 게이트 명령을 직접 다시 돌려 확인한 뒤
실측값과 검증 명령을 progress.md 에 적는다. 막히면 decisions.md 에 D-번호로 `차단`을 올리고 다음 항목으로
넘어간다 — 사람을 기다리며 멈추지 않는다. 게이트를 낮추거나 테스트를 건너뛰지 않는다.
§4.4 종료 조건 3개를 동시에 만족하면 최종 보고를 쓰고 멈춘다.
```

인터벌을 주지 않으므로 모델이 스스로 회전 간격을 정한다(작업 중이면 즉시 다음 회전, 대기 중이면 길게).
중단 후 재개는 `/re-begin` 또는 위 한 줄을 다시 붙여 넣으면 된다 — 상태는 전부 파일에 있다(§4.5).

### 0.2 이 문서의 성격

`/goal` 은 **한 번 읽고 끝나는 문서가 아니라 루프의 판정 기준**이다.

- 루프가 한 바퀴 돌 때마다 §2 수용 게이트 표를 실행해 **PASS/FAIL 을 실측**한다.
- FAIL 한 항목 중 **가장 앞선 웨이브의 것 하나**를 골라 담당 에이전트를 기동한다.
- 결과는 `progress.md` 에 **실측값과 검증 명령을 함께** 적는다. 명령 없이 적힌 숫자는 없는 것으로 친다.
- 모르는 값은 지어내지 않는다. `decisions.md` 에 `D-번호`로 올리고 **화면에는 `미확정 (D-nn)` 을 렌더링**한다.
  사용자가 "임의로 정하고 나중에 조정"을 지시하면 그 값은 `가설 (D-nn)` 배지로 표시하고 `.env`/시드에만 둔다(§10-1).
- 게이트를 낮추지 않는다. 못 맞추면 게이트를 고치는 게 아니라 `차단`으로 기록하고 다음으로 넘어간다.
- **이 문서 자체도 실측으로 정정된다.** 여기 적힌 수치가 정본과 다르면 정본이 이긴다. 발견자가 `decisions.md` 에 적고 이 표를 고친다.

### 0.3 정본 우선순위 — 이 사업의 핵심 규칙

**사업계획서 보완본이 상위 정본이다.** 설계·분석 산출물은 이미 보완본을 근거로 작성됐으므로 광성 사업만큼
정면 충돌은 없다. 그래도 충돌하면 **사업계획서가 이긴다.**

| 무엇을 정하는가 | 정본 | 비고 |
|---|---|---|
| **모든 수치·목표·정책·과제 성격** — KPI · AI 과제 3대 공정 · AI 성능목표 5종 · 데이터 유효성 · 보안 · HW · 수집 지점 · 공정 단계 · 사업비 | **`docs/사업계획서.pdf`** (보완본 60p) | 충돌 시 무조건 이긴다 |
| **코드 골격** — 화면 45 · 프로그램 49 · 테이블 68 · 컬럼 762 · ID 체계 · 그리드 열·라벨 | **`docs/design/design.json` + `docs/design/analysis.json`** | 사업계획서가 이 수준까지 정하지 않았으므로 산출물을 쓴다. 단 **내용이 사업계획서와 다르면 내용을 사업계획서에 맞춘다** |

**그러나 사업계획서 자체에 타 사업 잔재가 남아 있다**(§6 · D-02). 사업계획서가 정본이라는 말은
**"경동글로벌텍 1.1·1.3·2.5·2.6·2.7 의 서술"** 이 정본이라는 뜻이지, 잔재 문장까지 정본이라는 뜻이 아니다.
잔재는 §7.2 에 전부 D-번호로 등재돼 있다. 조용히 한쪽을 고르지 않는다. 산출물은 고치지 않는다 — 차이를 도입기업에 제안한다.

---

## 1. 목표

**SF-TD3 45화면 전부를 PostgreSQL 68테이블 실 데이터 + FastAPI 실 API에 연동한다.**

지금 상태를 정확히 알고 시작한다. **이 폴더는 `goal.md` 와 `CLAUDE.md` 뿐이다.** 스캐폴딩부터 짓되
**새로 설계하지는 않는다.** 화면·컬럼·API·라벨은 전부 산출물 문장이다. 이 회사는 **ETO 주문 제작 산업장비 기업**이다 —
직전 세 사업(프레스 가공·김치·수건)과 도메인이 완전히 다르므로 골격만 가져오고 **도메인 코드는 한 줄도 복사하지 않는다**(§6).

끝났다고 말할 수 있는 상태는 이것뿐이다.

1. 45화면 + 공통 4화면(로그인·메인시안·팝업·대시보드) + 오류 화면이 전부 HTTP 200 이고 `_placeholder` 가 **0건**이다.
2. 68테이블이 실제로 만들어져 있고, 화면이 읽는 데이터가 **시드 또는 실 입력에서 온 것**이다.
3. `MES-AD2-001~045` 45건이 화면 45개 · 프로그램 `MES-TD4-001~045` 과 **1:1** 로 이어지고, EIF 4건은 `MES-TD4-046~049`(화면 없음)로 소화된다 (고아 0, 중복 0).
4. **디지털 스레드가 실제로 연결된다** — `EST_PROJECTS`(프로젝트=수주번호)를 최상위 키로 도면 → BOM → 자재 LOT → 작업지시 → 공정실적 → 검사 → 출하가 이어지고, `SHP_LOT_TRACES` 가 제품 LOT 축의 중심이며, 어느 화면에서 LOT·프로젝트번호를 눌러도 **024 공정이력조회**로 간다.
5. CAD 견적 자동화 파이프라인(도면 업로드 → 객체 인식 → **사람 검증(HITL)** → BOM 자동생성 → 견적 자동산출 → SHAP 근거)이 **끊김 없이 한 번 돌고**, 산출물마다 **Confidence Score 와 근거**가 화면에 표시되며, **담당자 승인 전에는 확정 상태로 표기되지 않는다**.
6. RAG Agent 3종(입고 009 · 출하 020 · 통합 038)이 **폐쇄형**으로 동작하고 근거가 없으면 지어내지 않으며, 모든 질의·응답·근거를 `AGT_QUERY_LOGS` 에 남긴다.
7. 수집 경로(레이저커팅기 PLC 시뮬레이터 → 수집 API → `IF_PLC_SIGNALS` → `PRC_EQUIP_SIGNALS`·`DAT_TIMESERIES`)가 유실 없이 받고, **Gateway 로컬 버퍼링 후 재전송**이 재현되며, 수집이 멈추면 화면이 **마지막 수집시각과 상태**를 보여 준다.
8. KPI 2종(제조 리드타임 1,320→1,080 · 수주출하 리드타임 1,440→1,200)이 **사업계획서 산식 그대로** 한 곳에서 계산되고 대시보드·현황판·KPI 화면이 같은 값을 보여 준다.
9. QA 3명이 서로 다른 관점으로 교차검증한 리포트 3종이 `outputs/` 에 있고 치명 결함이 0이다.

### 1.1 재사용 자산 — 새로 짓기 전에 먼저 본다

| 자산 | 위치 | 쓰임 |
|---|---|---|
| **정본 설계 JSON** | `../02 Design Agent/outputs/20260814-094524-all/design.json` (434KB) | TD1~TD5 전문. **코드 범위의 정본.** `td3.screens[*].screens[*]`(화면 45 · `mockup` dict) · `td4.details[*].programs[*]`(49 · `flow`·`functions`·`tables`) · `td5.details[*].columns`(68표 762컬럼, 행 = `[한글명, 영문명, 타입, PK, FK, NULL, 비고]`) · `td3.role_matrix`(6역할×8영역) · `td3.common_screens`(4) · `td2.sw_layers` |
| **정본 분석 JSON** | `../01 Analyze Agent/outputs/20260812-143653-all/deliverables.json` | AD1(현행·문제점 8건·CAD 아카이브 실측)·AD2(58건)·AD3(45행) |
| **사업계획서 보완본** | `/Users/gerardo92/Desktop/26년 제조AI 스마트공장  프로젝트 /01 경동글로벌텍 제조AI 플랫폼 구축 프로젝트/[사업계획서] 제조AI특화 스마트공장 사업계획서_경동글로벌텍_보완.pdf` (60p) | **수치·목표·정책의 정본.** KPI · AI 성능목표 5종 · 데이터 유효성 · 수집 지점 · HW · 클라우드 · 보안 9.2 |
| 사업비 산출내역서 | 같은 폴더 `11. (제조AI특화 SW개발비) 사업비 산출내역서_경동글로벌텍_v0.97.xlsx` | FP 산정 · 단위프로세스 127행 · ILF 17행(RET 58) · EIF 4행(RET 10) = **테이블 68 의 도출 근거** |
| **CAD 원천 데이터** | 같은 폴더 `01 경동Data/` — `경동글로벌텍_CAD도면_제품별정리.xlsx` · `경동도면함_CAD분류_20260721.xlsx` · `경동글로벌텍_파일인벤토리분석_20260721.xlsx` · `라벨링 가이드.docx` · `경동CAD 객체인식 및 견적 프로세스_20260721_v1.2.pptx` · 도면 zip | **CAD 견적 AI 의 유일한 실 데이터.** 전체 3,237건 중 CAD 원본 **1,283건**(dwg 918 · cad 355 · dxf 10), **77개 프로젝트**. REACTOR 448 · RECEIVER TANK 102 · 경동도면함 733 |
| 화면설명서 HTML · 화면정의서 PPT | `../03 UI Agent/outputs/경동글로벌텍_제조AI_화면설명서_v1.0.html` · `경동글로벌텍_제조AI_화면정의서_v1.0.pptx` | 화면별 목업 렌더본. 레이아웃 판단은 여기, 데이터는 JSON |
| 테스트 케이스 | `../04 QA Agent/outputs/경동글로벌텍_제조AI_단위테스트결과서_v1.0.docx` · `통합테스트결과서` · `사용자테스트결과서` · 체크리스트 HTML 3종 · `메뉴얼` · `운영지원계획서` · `시스템전환결과서` | QA 시나리오 원본. QA1~3 이 여기서 케이스를 끌어온다 |
| **경동글로벌텍용 Agent 시안** | `../08 AI Agent Making Agent/work-kyungdong-cockpit/kyungdong-globaltech-ai-agent/` (1,368줄) | **같은 회사용으로 이미 만든 것.** 개발3 은 **이식·확장**한다(재작성 금지). 상세는 §1.2 |
| **CAD 견적 Agent 시안 (타사)** | `../08 AI Agent Making Agent/onetouch-cad-quotation-agent-cockpit/` | 원터치용 CAD 견적 Cockpit. `sample_docs/`(cad_feature_standard · bom_routing_standard · costing_approval_policy · model_validation_guide)의 **구조만** 참고. **도메인 문자열은 가져오지 않는다** |
| 플랫폼 골격 (직전 사업) | `../KwangSung AI Platform/src/kwangsung/app/` · `contracts/` · `tools/{gen_schema,check_routes,gate}.py` | 같은 파이프라인의 직전 구현. **골격·도구·계약 양식을 본뜬다.** 도메인 문자열·예지보전 코드는 가져오지 않는다 |
| 팀 편성·웨이브 | `../06 Coding Agent/README.md` · `goal.md` | 방법론 원본 |

### 1.2 경동글로벌텍 Agent 시안의 재사용 범위 (개발3) — 실측 완료

`kyungdong-globaltech-ai-agent/` 는 **Streamlit + OpenAI Responses API 기반 Agent Cockpit 데모**다(app.py 372줄 +
`kyungdong_agent/` 996줄). SQLite/PostgreSQL 겸용 12테이블(`projects` · `designs` · `materials` ·
`incoming_inspections` · `operations` · `equipment_readings` · `quote_predictions` · `purchase_orders` ·
`fat_tests` · `claims` · `knowledge_documents` · `rules`) · 읽기 전용 도구 **11종** · 시스템 프롬프트 11원칙 ·
근거 인용 · 승인 대기 도출 · 음성(`voice.py`) · `sample_docs/` 3종을 갖췄다.
실제 CAD 파이프라인 · ML 모델 · 인증/RBAC · 감사로그 · 벡터 검색 · 그라운딩 임계 분기는 **없다**.

| 이식한다 | 버린다 |
|---|---|
| `factory_tools.py` 패턴 — strict JSON schema · 허용목록 밖 도구 이름 거부 · 쓰기 동사 금지 | SQLite `data_hub.py` 12테이블 스키마 (TD5 68표로 대체) |
| `prompts.py` 11원칙 — 특히 **7~10번 문서 인용 규칙**(검색하지 않은 문서명·조항·페이지를 지어내지 않는다), **5줄 형식**, `근거: …` 마지막 줄 | `demo_data: true` 플래그 · 데모 예측 상수 |
| `citations.py` 근거 파일명·청크 표시 | OpenAI 고정 호출 (공급자 추상화로 감싼다) |
| `ui_helpers.py` 승인 대기 도출 규칙 | Streamlit `app.py` (FastAPI+Jinja2 로 대체, D-14) |
| `sample_docs/` 3종(`material_receiving_sop` · `quote_and_procurement_guide` · `fat_safety_claim_manual`) → RAG 정답셋 시드 | `voice.py` (범위 밖 — 필요하면 D 로 올린다) |

도구 11종(`search_knowledge` · `get_rules` · `get_db_records` · `get_project_summary` · `get_material_status` ·
`get_incoming_inspection_issues` · `get_quote_analysis` · `get_procurement_risks` · `get_fat_quality` ·
`get_lead_time_status` · `get_claim_trace`)은 **TD5 68표에 맞춰 재배선**한다. 상세 매핑은 아키텍트가 `decisions.md` D-20 에 적는다.
`fat_tests` → `SHP_INSPECTIONS`(수압·기밀·진공), `quote_predictions` → `EST_QUOTATIONS`+`EST_ML_PREDICTIONS`,
`equipment_readings` → `PRC_EQUIP_SIGNALS` 가 출발점이다.

---

## 2. 수용 게이트

**루프는 매 회전마다 이 표를 실행한다.** 판정은 명령의 출력으로만 한다. 게이트 번호는 바꾸지 않는다(코드 주석·QA 리포트가 참조).

### 2.1 구조 게이트

| # | 항목 | 게이트 | 검증 |
|---|---|---|---|
| G-01 | 테이블 | **68** (public 스키마) | `psql -d kyungdong_db -Atc "select count(*) from information_schema.tables where table_schema='public'"` |
| G-02 | 컬럼 | **762** (TD5 정의 전량) | `tools/check_schema.py` — TD5 `details[*].columns` ↔ `information_schema.columns` 대조 |
| G-03 | 화면 | **45 + 공통 4 + 오류 1** 전부 HTTP 200 · `_placeholder` 0건 · **타 업종·타 사업 용어 0건**(§6) | `make check-routes` · `grep -rc _placeholder src/**/routers/` = 0 |
| G-04 | 요구사항 추적 | `MES-AD2-001~045` ↔ `MES-TD3-001~045` ↔ `MES-TD4-001~045` **1:1, 고아 0** · EIF 4건 ↔ `MES-TD4-046~049` · 비기능 13건(046~058)은 화면 없음이 정상 | `tools/check_trace.py` |
| G-05 | 프로그램 | **49** (화면 45 + 인터페이스 4: `MES-TD4-046` ERP · `047` IoT/PLC · `048` CAD 파일 · `049` 외부 표준문서 — 화면 없음, 연계 상태는 **033 데이터통합관리**에서 조회) | `tools/check_trace.py` |
| G-06 | 메뉴 | **10영역** (`대시보드·견적AI·입고·공정·출하·기준·데이터·Agent·KPI·시스템` = `td3.menu_shortcuts`) 단일 소스 | `nav.py` ↔ `contracts/screen-map.md` ↔ `design.json` 대조 |

### 2.2 데이터 게이트 (사업계획서 2.7.4 · AD2-056)

| # | 항목 | 게이트 | 검증 |
|---|---|---|---|
| G-07 | 시드 멱등 | 두 번 돌려도 행 수 diff 0 · **생성 기준일은 시간 앵커(`SYS_CONFIGS`)에 고정** — `date.today()` 금지(§10-3) | `make db-seed` ×2 + 행수 비교 |
| G-08 | **디지털 스레드** | `EST_PROJECTS`(프로젝트=수주번호) → `EST_CAD_DRAWINGS`(도면번호·리비전) → `EST_BOM_HEADERS`/`EST_BOM_ITEMS` → `INV_MATERIAL_LOTS`(자재LOT) → `PRC_WORK_ORDERS` → `PRC_PERFORMANCES` → `SHP_LOT_TRACES`(제품LOT) → `SHP_INSPECTIONS` → `SHP_SHIPMENTS` 가 **양방향** 연결. **LOT 기반 데이터 매핑 성공률 ≥ 85%**. LOT·프로젝트번호 열이 있는 **모든 화면**에서 클릭 → **024 공정이력조회** 이동 | `tools/check_data.py` + `tests/test_qa1_trace_click.py` (0건 경로도 단언) |
| G-09 | 정확성·정합성·연계성 | 정확성 **≥ 85%**(정상 데이터 수 ÷ 전체 × 100) · 정합성 **≥ 85%**(CAD ↔ BOM ↔ 원가 Cross Check · 재질·두께·길이 일관성) · 연계성 **≥ 85%**(수주→도면→BOM→생산→검사→출하 연결 성공률) | `tools/check_data.py` — 산식은 사업계획서 2.7.4 표 그대로 |
| G-10 | 시계열 신뢰성 | `DAT_TIMESERIES`·`PRC_EQUIP_SIGNALS` Timestamp 정렬 · 결측/지연 건은 `IF_GATEWAY_BUFFER`·`DAT_JOB_LOGS` 에 기록 · 공통 시간축 리샘플링. **동기화율 목표는 사업계획서에 없다 → 실측만 보고**(D-09) | `tools/check_data.py` — 결측 분모·분자는 `contracts/missing-policy.md` 규약 |
| G-11 | 빈 테이블·조용한 빈칸 | 비어 있어도 좋다. **단 그 그리드는 "미수집 / 미확정(D-nn)" 을 렌더링**해야 한다. 런타임 전용 표(`DAT_DOWNLOAD_LOGS`·`AGT_QUERY_LOGS`·`DAT_JOB_LOGS`·`SYS_ACCESS_LOGS`·`IF_*_LOGS`)는 깨끗한 DB 에서 0건이므로 0건 경로를 화면·테스트 모두 명시(§10-4). **건수 카드의 `0 건` 은 정답이지 "미수집" 이 아니다**(§10-14) | QA2 육안 + `check_data.py` (`make db-reset` 직후 실행) |
| G-12 | 수집 경로 | **수집 지점은 2개소뿐이다**(레이저커팅기 Master PLC 1 + 터치PC 1). 시뮬레이터가 PLC 태그(작업시간·수량·가동상태·알람·속도·압력·전류·온도)를 수집 API 로 보낼 때 적재 유실 0 · 순서 보존 · **Gateway 로컬 버퍼링 → 복구 시 재전송 무손실**(TD2 network_standards) · 수집 중단 시 화면 003/022 에 **마지막 수집시각 + 상태** 표시 | `tools/check_ingest.py` — **전 공정 실시간 수집을 만들면 결함**(D-06) |
| G-13 | 전처리 | 단위 표준화(mm·kg·℃·A·초) · 중복 제거(**도면번호+버전+고객사** 기준 — 사업계획서 2.7.4 3단계) · 결측 보정(치수 누락·OCR 오류·재질 미입력을 유사 도면·기준값으로) · 이상치 제거 · Feature 생성(**홀 수량 · 절단 길이 · 형상 복잡도 · 두께 · 재질 · 용접 길이**)이 파이프라인에 **코드로 존재**하고 처리 건수를 `DAT_JOB_LOGS`·`DAT_PREPROCESS_RULES` 에 남긴다 | `tools/check_ingest.py` — 결측·중복·이상치를 주입한 입력으로 전후 비교 |

### 2.3 AI 게이트 (사업계획서 2.6 성능목표 5종 + 2.5 방법론)

> **사업계획서 2.6 의 5개 목표가 게이트다.** 단 **⓶ 산식 칸이 5개 항목 전부 공란**이므로(D-13)
> 산식은 ⓷ 검증방법 문장에서 확정하고 그 결정을 `decisions.md` 에 적는다. 임의로 바꾸지 않는다.

| # | 항목 | 게이트 | 근거·비고 |
|---|---|---|---|
| G-14 | CAD 객체 인식 정확도 | **≥ 80%** — 라벨링 데이터셋 대비 **IoU ≥ 0.5** 기준 TP/FP/FN 산출, **Precision · Recall · F1 전부 보고** | 2.6-1. `EST_CAD_OBJECTS`(도면객체인식결과) · `EST_CAD_FEATURES`. 라벨셋은 `01 경동Data/라벨링 가이드.docx` 규약을 따른다 |
| G-15 | 견적 산출 정확도 | **±5% 이내** — **MAPE 및 MAE** 산출 + **제품군별 오차 분포**(반응기·교반기·진공건조기·누체필터·저장탱크) 병기 | 2.6-2. XGBoost. **p.11 은 "견적 정확도 80% 이상" 으로 적혀 2.6 과 다르다 → D-11. 2.6 을 채택** |
| G-16 | BOM 생성 정확도 | **≥ 85%** — 기준 BOM 과 **항목 단위**(자재·수량·공정) 비교, **Precision · Recall · F1 병기** | 2.6-3. Rule + GNN. `EST_BOM_HEADERS`/`EST_BOM_ITEMS`/`EST_BOM_ROUTINGS` |
| G-17 | 납기 예측 정확도 | **≥ 85%** — 실제 출하일 대비 **허용 오차 범위 내 정확도 + RMSE 병기**. **허용 오차 범위가 사업계획서에 없다 → D-12 가설값(`.env`), 화면에 `가설 (D-12)` 배지** | 2.6-4 |
| G-18 | 설명가능성 신뢰도 | **90%** — SHAP Top-N ↔ **전문가 평가** 일치율 또는 **Spearman ρ** | 2.6-5. **"90% 이내" 표기가 모호하고 전문가 변수 목록이 없다 → D-13 (차단).** SHAP Top-5 와 근거는 화면 015 에 반드시 표시 |
| G-19 | **CAD 견적 파이프라인 5단계** | 화면 010→011→012·013→014→015 가 **① 도면 업로드·Parsing(Autodesk API) + Vision AI(YOLOv8·OCR) 병렬 ② 객체인식 결과 Human-in-the-loop 검증(`EST_OBJECT_REVIEWS` 기록) ③ BOM 자동생성·다단계 전개 ④ XGBoost 견적 자동산출(공정별 원가 `EST_QUOTATION_ITEMS`) ⑤ SHAP 영향요인(`EST_SHAP_FACTORS`)** 을 **하나의 프로젝트로 끝까지 통과**시키고, 각 단계 산출물에 **Confidence Score** 와 근거가 표시된다 | TD1 process_steps · TD3 standard_note 4. 5단계 중 하나라도 `미확정` 없이 비면 FAIL |
| G-20 | RAG Agent 3종·폐쇄형·기록 | 입고(009) · 출하(020) · 통합(038). **외부 검색 0**(폐쇄형 — 사업계획서 9.2-5). 질의·응답·근거·응답시간을 `AGT_QUERY_LOGS` 에 **100% 기록**. `AGT_VECTOR_DOCS` 는 작업표준(SOP)·품질기준·검사기준·MTC 기준·클레임 이력만 | 도구는 §1.2 11종 재배선. Reranker 는 있으면 좋고 없으면 D 로 기록 |
| G-21 | RAG 근거 인용 정확도 | 인용 누락 **0** · 인용 문서가 실제 근거인지 채점. **인용 정확도 목표치가 사업계획서에 없다 → 실측만 보고**(D-09) | 정답셋은 QA3 이 `AGT_VECTOR_DOCS` 본문에서만 만든다(`work/rag_goldset.json` 25건+, 근거 없음형 5건 포함) |
| G-22 | 환각 방지 | ① **근거 0건 → LLM 을 부르지 않고 "검토 필요 — 근거 부족" + 담당자 이관** ② **검색하지 않은 문서명·조항·페이지를 만들어 붙이지 않는다**(시안 프롬프트 8번) ③ **견적 확정·발주·대체자재 승인·검사 합격·출하는 담당자 승인이 필요하다고 명시**. **신뢰도 임계값이 사업계획서에 없다 → D-10 가설값 `.env` `RAG_CONFIDENCE_MIN`, 화면에 `가설 (D-10)`** | 사업계획서 9.2-5 + 시안 prompts.py 3·7~10 |
| G-23 | RAG 응답 지연 | 평균 응답시간을 `AGT_QUERY_LOGS.RESPONSE_MS` 로 **실측 보고**. **목표치가 사업계획서에 없다 → D-09** | 목표 없는 것을 있는 척하지 않는다 |
| G-24 | Human-in-the-loop | **AI 는 조회·분석·추천·경고까지**(사업계획서 2.5 방법론). 승인 없이 `EST_QUOTATIONS`(견적 확정) · `EST_BOM_HEADERS`(BOM 확정) · `SHP_INSPECTIONS`(합격 판정) · `SHP_SHIPMENTS`(출하 확정) · `PRC_STD_CONDITIONS`(표준조건) 가 바뀌는 코드 경로 **0**. 승인 시 `EST_OBJECT_REVIEWS` · `AGT_RECOMMENDATIONS`(`ADOPT_YN`·`ADOPT_BY`) 에 이력 기록. **검사 합격 LOT 만 출하 대상 선택 가능**(422) | TD1 · TD3 standard_note 4 · AD3 018 |
| G-25 | MLOps·재학습 | `EST_ML_MODELS`(모델명·버전·성능·상태) · `EST_ML_TRAIN_RUNS`(학습이력) · `EST_ML_PREDICTIONS`(예측결과)에 버전과 성능이 남고 **직전 버전 롤백 경로**가 있으며, `DAT_TRAIN_DATASETS`/`DAT_DATASET_SPLITS` 로 **Train/Validation/Test 분리**가 강제된다(사업계획서 2.7.4 ⓸) | 자동 재학습 실행은 범위 밖 — **분리·버전·롤백 경로**만 게이트 |

> **학습 데이터 실태 (D-03).** 보유: **CAD 원본 도면 1,283건 / 77개 프로젝트**(REACTOR 448 · RECEIVER TANK 102 ·
> 경동도면함 733). 그러나 **파일명 중복 183건(14.3%) · 0KB 손상 4건 · 제품당 도면 수 편차 최대 184 / 최소 1 / 중앙값 5 ·
> 도면 1건뿐인 제품 9개(11.7%)** 이고, **도면 파일에 발행일·개정일 메타데이터가 통일 형식으로 없어 mtime 을 대체 기준**으로 쓴다.
> **과거 견적금액·실제 제조원가 Label 은 확보 여부가 확인되지 않았다**(D-04). 따라서 G-14~G-18 은
> **시간 앵커 고정 데이터**로 측정하고 **표본 수 · 제품군 분포 · 앵커 5개 분포(min/mean/max)** 를 함께 적는다.
> 경계선이면 경계선이라고 적는다. **모델·앵커·생성기를 게이트에 맞춰 고르지 않고** `차단`으로 남긴다.
> 실 도면 파싱이 불가능하면(`.dwg` 바이너리) **Feature 추출 단계를 합성으로 대체**하고 화면 전부에 `합성 데이터 기준 (D-03)` 고지를 붙인다.

### 2.4 보안·운영 게이트 (사업계획서 9.2 + AD2-054·057·058)

| # | 항목 | 게이트 |
|---|---|---|
| G-26 | 암호화·전송 | **HTTPS(TLS) 암호화 통신** · 외부 접속 최소화(9.2-3) · 비밀번호 **bcrypt 또는 Argon2** 해시. **알고리즘·키 길이가 사업계획서에 명시되지 않았다**(광성의 TLS 1.2+/AES-256 을 그대로 베끼지 않는다 — D-15). 로컬 개발은 TLS 종단이 없으므로 **설정과 코드 경로 존재 + prod 프로파일 강제**로 판정 |
| G-27 | 접근통제·계정 | **역할별 계정 분리**(관리자·생산관리자·품질관리자·현장작업자) · **최소 권한 원칙** · **비밀번호 복잡도·주기적 변경**(9.2-2). 로그인 실패 잠금·자동 로그아웃 **수치 미기재 → D-16 가설값(`.env`)** |
| G-28 | RBAC | TD3 `role_matrix` **6역할 × 8영역** 그대로 — 총괄PM/경영자 · 품질·RCMS 담당 · 공장장·생산관리 · 현장 작업자 · 공급기업 운영담당 · 시스템 관리자 × 대시보드/견적AI/입고/출하/공정/기준·데이터/Agent·KPI/시스템. 권한 없음 = 403. **현장 작업자는 견적AI 영역이 `-`(접근 불가)** · 좌측 메뉴는 권한 있는 것만 노출 |
| G-29 | 감사·개인정보 | 로그인·화면조회·데이터 반출을 `SYS_ACCESS_LOGS`(접속·작업·변경·API·오류 통합) 와 `DAT_DOWNLOAD_LOGS`(반출 별도)에 기록 · AI 사용 로그는 `AGT_QUERY_LOGS` · **사용자명·연락처·이메일·공급처 담당자 정보는 암호화 또는 마스킹**(TD5 criteria 개인정보) · 시드 비밀번호 저장소 리터럴 **0건**(`make db-seed` 가 난수 1회 출력). 보존기간 미확정(D-17) — 삭제 배치를 만들지 않는다 |
| G-30 | 조용한 실패 사냥 | 백엔드·LLM·DB·수집 시뮬레이터·CAD 파서를 각각 죽였을 때 화면이 멀쩡해 보이면 결함. §2.5 상태 코드로만 드러난다. 그리드 셀은 **이스케이프가 기본**(저장형 XSS 시드로 확인, §10-8) |

### 2.5 오류 계약 — QA1 이 그대로 테스트 케이스로 쓴다

| 상황 | HTTP | 화면 |
|---|---|---|
| 필수값 누락 / 코드 중복 / 상·하한 역전 / **검사 합격 아닌 LOT 출하 선택** / **승인 전 견적 확정 시도** | **422** | 공통 팝업 메시지 (TD3 layout_rules 입력 검증) |
| 인증 실패 | **401** | `로그인이 필요합니다` |
| 권한 없음 / 승인 권한 없는 추천값 반영 (예: 현장 작업자의 견적AI 접근) | **403** | `접근 권한이 없습니다` |
| DB 연결 실패 | **503** | `서비스 일시 중단` |
| LLM 미구성 (API 키 없음) | **501** | `LLM 미구성` 명시 렌더링. **조용한 폴백 금지** |
| 임베딩 공급자 미구성 | **200** | 검색 모드 라벨 `tsvector_keyword` 명시 (D-08). 숨기지 않는다 |
| CAD 파서 미구성 / `.dwg` 해석 불가 | **501** 또는 **200 + 명시 배지** | `CAD Parsing 미구성 (D-05)` — 조용히 합성 Feature 를 내지 않는다 |
| RAG 근거 0건 또는 신뢰도 < `RAG_CONFIDENCE_MIN` | **200** | `검토 필요 — 근거 부족` + 담당자 이관 표시. 임의 생성 금지 (G-22) |
| 수집 중단 (마지막 수집 후 임계 초과) | **200** | 화면 상단에 `수집 중단 — 마지막 수집 hh:mm:ss` 배지 |
| 처리되지 않은 예외 | **500** | 공통 오류 화면 + `SYS_ACCESS_LOGS` 기록 |

### 2.6 빌드 게이트

`uv run pytest -q` 전건 통과(skip 은 사유가 테스트 코드에 적힌 것만) · `make check-routes` PASS · 서버 기동 후 `/health` 200.
**매 회전 이 게이트가 실제로 실행됐는지 확인한다** — 수집 단계에서 죽어 0건 실행이면 그동안의 모든 판정이 무효다(§10-15).

### 2.7 범위 밖 — v1 실패로 보지 않는다

**설비 예지보전·품질예측 AI**(사업계획서 2.5 "현 사업 미적용" — D-01) · 용접 비드 이미지 판정 · 설비 제어(전 화면 조회·등록 전용) ·
**전 공정 실시간 설비 수집**(수집 지점 2개소만 — D-06) · 실제 `.dwg` 바이너리 파싱(Autodesk API 미보유 시 — D-05) ·
실제 클라우드(EC2·RDS Multi-AZ·S3·Bedrock·Neptune) 배포 · Data Lake/Kafka/MQTT 실 브로커(수집 API + `IF_GATEWAY_BUFFER` 이력까지) ·
ERP(이카운트) 실 연동(Excel 적재 + `IF_ERP_*` 이력까지 — D-07) · 현황판 65" 2대·현장POP·스마트패드 전용 앱(반응형 Web + 터치 레이아웃까지) ·
무선AP·바코드 하드웨어 연동 · 자동 재학습 실행(분리·버전·롤백 경로까지, G-25) · 실 취약점 스캔(시큐어코딩 준수까지) ·
사용자 교육 22시간·유지보수 체계(AD2-057·058 — 문서 항목).

---

## 3. 팀 편성 7명

```
Phase 0  스캐폴딩·정본 반입 (아키텍트 단독)
   ↓
웨이브 A  아키텍트 1        contracts/ 4종 확정 + 스키마 생성 + 공용 모듈 + interfaces.md
   ↓
웨이브 B  개발 3 (병렬) R1  DB·인증·공용 → R2  45화면 연동   ※ 개발3 ML 학습 회전은 단독 기동(§10-2)
   ↓
웨이브 C  QA 3 (병렬)       기능·계약 / 데이터 정합·수집 / AI·HITL·보안
   ↓
웨이브 D  아키텍트+개발      QA 결함 수정 라운드
   ↓
   └───────── 게이트 전건 PASS 까지 B→C→D 반복 (루프)
```

사용자가 "7명 동시"를 지시하면 웨이브 순서 대신 **동시 기동**한다. 그때 재작업을 막는 장치는 §3.4 소유권과
`contracts/interfaces.md`(파일이 없어도 시그니처를 믿고 쓴다) 두 가지다.

### 3.1 아키텍트 1명

`contracts/db-schema.md`(68테이블·762컬럼) · `contracts/api-contract.md`(45화면 라우트 + 공통 + Agent API + 수집 API + CAD 업로드 API) ·
`contracts/screen-map.md`(45행 + **담당·소유 파일**) · `contracts/interfaces.md`(공용 함수 시그니처) · `db/schema.sql` 생성기 ·
`app/util/` 공용 모듈 · RBAC 6역할 · 오류 규약 · 감사로그 훅 위치 · `tools/check_routes.py` · `tools/gate.py`.

**계약이 확정되기 전에는 개발 3명이 코드를 쓰지 않는다**(동시 기동 시에는 `interfaces.md` 가 그 역할을 한다).

### 3.2 개발 3명 — 45화면 3분할 (한 파일은 한 사람만 만진다)

| 담당 | 화면 | 수 | 소유 테이블 |
|---|---|---|---|
| **개발1** 입고·기준·시스템·데이터 | 입고재고 `005~008` · 사용자/시스템 `026~029` · 기준정보 `030~032` · 데이터관리 `033~036` + **로그인·메인시안·팝업·오류 공통 화면** | **15** | `INV_`(6) `SYS_`(4) `BAS_`(3) `DAT_`(7: SOURCES·INTEGRATION_JOBS·JOB_LOGS·QUALITY_CHECKS·LAKE_OBJECTS·TIMESERIES·DOWNLOAD_LOGS) + `IF_ERP_RECEIPTS` `IF_ERP_SHIPMENTS` `IF_ERP_STOCKS` = **23표** |
| **개발2** 대시보드·공정·출하·KPI | AI 대시보드 `001~004` · 출하물류 `016~019` · 공정관리 `021~025` · KPI `043~045` + **공통 대시보드(현황판 65" 2대)** | **16** | `SHP_`(6) `PRC_`(7) `KPI_`(2) = **15표** |
| **개발3** AI (CAD 견적 + 수집 + RAG Agent) | 입고 AI Agent `009` · 수주견적AI `010~015` · 출하 AI Agent `020` · AI학습 데이터관리 `037` · AI Agent 통합 `038~042` | **14** | `EST_`(16) `AGT_`(3) `DAT_`(4: TRAIN_DATASETS·DATASET_ITEMS·DATASET_SPLITS·PREPROCESS_RULES) + `IF_PLC_SIGNALS` `IF_GATEWAY_BUFFER` `IF_DEVICE_REGISTRY` `IF_CAD_FILES` `IF_CAD_IMPORT_LOGS` `IF_EXT_DOCUMENTS` `IF_DOC_EMBED_LOGS` = **30표** + **PLC 수집 API·시뮬레이터·CAD 수집** |

합계 **15 + 16 + 14 = 45** · 테이블 **23 + 15 + 30 = 68**.

- **R1 (기반)** — 개발1: 스키마 적용·시드·인증·RBAC·공통 레이아웃 / 개발2: 공정·출하 도메인 로직·**KPI 산식 단일 소스 `app/kpi.py`** / 개발3: PLC 수집 API + 시뮬레이터 → 전처리 → CAD 파이프라인 → ML → RAG 파이프라인(§1.2 이식)
- **R2 (화면)** — 각자 담당 화면을 목업(`td3.screens[*].screens[*].mockup`: `search_fields`·`grid_columns`·`buttons`·`cards`·`description`)대로 렌더링하고 실 데이터에 연결. 목업의 `(예시)` 값은 **시드에 넣지 않는다**
- **모든 업무 화면의 최상위 조회 축은 프로젝트(수주)번호·도면번호다**(TD3 standard_note 2). LOT 는 프로젝트 하위 식별자다. 이 축을 어긴 화면은 결함이다
- 채번 형식(프로젝트번호·도면번호·리비전·자재LOT·제품LOT·출하번호)은 **먼저 정한 사람이 `progress-devN.md` §1 에 공표**하고 나머지가 그대로 쓴다
- 현장 단말(현장POP 1대·스마트패드 2대)이 실제 사용 채널인 화면은 **입력 항목 최소화 + 큰 버튼 + 스캔 입력 우선**(TD3 standard_note 3)

### 3.3 QA 3명 — 서로 다른 관점으로 교차 검증

| 담당 | 범위 | 산출 |
|---|---|---|
| **QA1** 기능·계약 | 45화면 200 · 요구사항 1:1(G-04·05) · 오류 계약 10종(§2.5) · RBAC 6역할×8영역(G-28) · 화면 항목이 TD3 목업과 일치하는지 · LOT·프로젝트번호 클릭 → 024(G-08) · 터치 단말 화면(현장POP·스마트패드·현황판)의 버튼 크기·입력 항목 축소(TD3 standard_note 3) · **폰 폭(400px) 메뉴 검증**(§10-13) | `outputs/qa1-기능계약.md` |
| **QA2** 데이터 정합·수집 | 68표·762컬럼(G-01·02) · FK/UK · 시드 멱등·앵커(G-07) · **디지털 스레드·LOT 매핑 85%**(G-08) · 정확성·정합성·연계성(G-09) · 시계열(G-10) · 빈 테이블 0건 경로(G-11) · **수집 유실 0·Gateway 버퍼 재전송(G-12)** · **전처리·Feature 생성(G-13)** · **KPI 산식 독립 재계산**(1,320→1,080 · 1,440→1,200, 메인 카드 = KPI 화면 043~045 동일 산식인지) | `outputs/qa2-데이터정합.md` |
| **QA3** AI·HITL·비기능·보안 | G-14~G-25 AI 게이트 실측(정답셋·라벨셋 작성 포함) · **CAD 견적 5단계 통과(G-19)** · **조용한 실패 사냥**(G-30) · 승인 없는 반영 경로 0(G-24) · 근거 부족 분기(G-22) · 암호화·해시(G-26) · 계정·세션(G-27) · 감사·개인정보 마스킹(G-29) | `outputs/qa3-AI비기능보안.md` |

결함 ID는 `DEF-QA{n}-nnn`. **QA는 고치지 않는다.** 재현 절차와 실측을 적고 담당 개발자에게 넘긴다.
통과 항목도 실측값과 명령을 함께 적는다. 테스트는 **데이터 의존적으로 쓰지 않는다** — 0건 경로와 N건 경로를 둘 다 단언한다(§10-4).
**QA 검사기가 판정 정본 함수를 직접 부르는지 확인한다** — 검사기가 로직을 복제하면 개발자의 수정이 수치에 반영되지 않는다(§10-16).

### 3.4 소유권 표 (동시 기동 시 이 표가 곧 계약 — `contracts/screen-map.md` §2 에 옮겨 적는다)

| 소유자 | 파일 |
|---|---|
| 아키텍트 | `contracts/*` · `src/kyungdong/app/{main,nav,rbac,settings,design,templating,auth}.py` · `app/util/*` · `app/templates/{base,login,_placeholder,_popup,_error}.html` · `app/static/*` · `db/{schema.sql,conn.py,seed.py}` · `tools/{gen_schema,check_routes,gate}.py` · `Makefile` · `pyproject.toml` · `.env.example` |
| 개발1 | `app/routers/{inv,bas,sys,dat}.py` · `app/templates/{inv,bas,sys,dat}/` · `db/seed_dev1.py` · `tests/test_dev1_*.py` · `progress-dev1.md` · `decisions-dev1.md` |
| 개발2 | `app/routers/{dsh,prc,shp,kpi}.py` · `app/kpi.py` · `app/templates/{dsh,prc,shp,kpi}/` · `db/seed_dev2.py` · `tests/test_dev2_*.py` · `progress-dev2.md` · `decisions-dev2.md` |
| 개발3 | `app/routers/{est,agt,ingest}.py` · `src/kyungdong/{agent,ml,cad,ingest}/*` · `app/templates/{est,agt}/` · `tools/{plc_simulator,cad_ingest}.py` · `db/seed_dev3.py` · `tests/test_dev3_*.py` · `progress-dev3.md` · `decisions-dev3.md` |
| QA1 | `tools/check_trace.py` · `tests/test_qa1_*.py` · `outputs/qa1-기능계약.md` |
| QA2 | `tools/{check_schema,check_data,check_ingest}.py` · `tests/test_qa2_*.py` · `outputs/qa2-데이터정합.md` |
| QA3 | `tools/{check_ai,check_security}.py` · `work/rag_goldset.json` · `work/cad_labelset.json` · `tests/test_qa3_*.py` · `outputs/qa3-AI비기능보안.md` |

`gate.py` 는 QA 소유 검사기를 **있으면 호출하고 없으면 `미구현`** 으로 표시한다 — 만들지 않는다.
화면 037(AI학습 데이터관리)은 개발3 소유이므로 `dat.py` 가 아니라 `est.py`(또는 `agt.py`)에 둔다 — `screen-map.md` 에 명시.

---

## 4. 루프 엔지니어링 프로토콜

### 4.1 상태 파일 — 이것 말고 다른 곳에 상태를 두지 않는다

| 파일 | 역할 | 쓰는 사람 |
|---|---|---|
| `goal.md` | 목표·게이트 (이 문서). **판정 기준** | 사람만 |
| `progress.md` | **검증된 현황의 유일한 진실.** 실측값 + 검증 명령만 | 루프 오케스트레이터 |
| `decisions.md` | 결정 대장 `D-nn` — `확정` / `가설` / `차단`. **§7 의 D-01~D-24 를 착수 시 그대로 옮겨 적고 D-25 부터 잇는다** | 전원 |
| `progress-dev{1,2,3}.md` · `decisions-dev{1,2,3}.md` | 개발자별 진행·결정 (`D-1nn`/`D-2nn`/`D-3nn` 대역) | 각 개발자 |
| `outputs/qa{1,2,3}-*.md` | QA 리포트 | 각 QA |
| `contracts/*.md` | 코드 계약. **코드와 다르면 코드가 맞다** — 발견자가 계약을 고치고 `progress-*.md` 에 적는다 | 아키텍트 |
| `.env` | 가설값·비밀(저장소 밖, `.gitignore`). **`sed -i` 로 고치지 않는다** — 편집기 도구로(§10-5) | 오케스트레이터 |

### 4.2 한 회전에서 하는 일 (이 순서를 바꾸지 않는다)

1. **읽기** — `progress.md` 최신 섹션 · `decisions.md` 미결 목록 · 직전 QA 리포트.
2. **판정** — §2 게이트 표를 실행한다(§9 명령). FAIL 목록을 만든다. 개발자 작업 중에는 `make gate`(읽기 전용), 회전 마감에는 `make gate-full`.
3. **선택** — FAIL 중 **가장 앞선 웨이브의 항목 1개**. 웨이브를 건너뛰지 않는다.
4. **기동** — §5 의 해당 프롬프트로 담당 에이전트를 띄운다. 병렬 가능한 것(개발 3, QA 3)은 **한 번에 같이** 띄우되, **ML 학습이 있는 개발3 회전은 단독**(§10-2). 한 에이전트에 **잔여 작업 한 가지**만 주고, **읽으라고 지시하는 파일은 3개 이하**로 줄인다(§10-9).
5. **검증** — 에이전트 보고를 믿지 않는다. 게이트 명령을 **직접 다시 실행**한다. 600초 넘게 응답 없는 에이전트는 기다리지 않고 새 에이전트에 그 한 가지를 다시 준다.
6. **기록** — `progress.md` 에 `| 항목 | 실측 | 검증 방법 |` 3열로 추가. 새 결정은 `decisions.md`. 회전 끝에 **"지금 해야 할 것" · "알아둘 것"** 두 절을 갱신한다.
7. **다음 회전** — 남은 FAIL 이 있으면 1로.

### 4.3 멈추지 않기 위한 규칙

- **막히면 우회한다.** 사람 확인이 필요한 값은 `decisions.md` 에 `상태: 차단` 으로 올리고, 화면에는
  `미확정 (D-nn)` 을 렌더링한 뒤 **다음 항목으로 넘어간다.** 사람을 기다리며 루프를 세우지 않는다.
- **같은 실패를 2회 넘게 반복하지 않는다.** 3회째 같은 오류면 접근을 바꾸고, 바꿔도 안 되면 `차단`으로 기록한다.
- **게이트를 낮추지 않는다.** 테스트를 지우거나 `skip` 하거나 임계값을 내리는 것은 실패로 친다. 정답셋·라벨셋·앵커·합성 생성기를 게이트에 맞춰 고르는 것도 같다.
- **한 파일은 한 사람만.** 병렬 기동 시 담당 밖 파일을 고치지 않는다. 필요하면 `progress-devN.md` 에 요청을 남긴다.
- **보고를 믿지 않는다.** "완료"는 게이트 명령의 출력으로만 인정한다.
- **API 세션 한도(429)를 치면** 병렬 수를 줄인다(개발 2 + QA 1 등). 리셋 시각을 `progress.md` 에 적는다. 죽은 에이전트를 재개하면 컨텍스트 재로딩으로 한도를 다시 소진한다 — 새 에이전트를 쓴다.

### 4.4 종료 조건 (EXIT)

아래 셋을 **동시에** 만족하면 루프를 멈추고 최종 보고를 쓴다.

1. §2 게이트 **G-01~G-30 전건 PASS** (`차단` 표시된 항목은 사유와 D-번호가 `decisions.md` 에 있을 것).
2. QA 리포트 3종의 **Critical 0건** — 해소된 결함은 리포트 문구까지 실측으로 갱신돼 있어야 한다(§10-10).
3. **에이전트가 하나도 돌지 않는 조용한 창**에서 `make db-reset` → `make gate-full` 을 **연속 2회** 돌려 결과가 변하지 않는다(§10-17).

### 4.5 중단·재개

세션이 끊기면 `progress.md` 최신 섹션 + `decisions.md` 미결 목록만 읽고 **4.2 의 1번부터 다시 시작**한다.
대화 기록에 의존하지 않는다. 상태는 전부 파일에 있다. 코드에 `(D-nn)` 주석이 있는데 `decisions.md` 에 없으면
**지어내지 않고 코드 주석 그대로 옮겨 적은 뒤** 다음 번호부터 잇는다.

---

## 5. 에이전트 기동 프롬프트 (그대로 복사해 쓴다)

모든 프롬프트는 아래 **공통 서두**로 시작한다.

> 너는 경동글로벌텍 제조AI 플랫폼(SF26179182, 공급기업 주식회사 재일)의 [역할]이다.
> 작업 전에 `goal.md` · `contracts/` · `decisions.md` 를 읽는다(**세 개까지만**. 더 필요하면 그때 연다).
> 코드 범위의 정본은 `docs/design/design.json` 과 `docs/design/analysis.json` 이고, 수치·목표·정책의 정본은
> `docs/사업계획서.pdf`(보완본)다(§0.3). python 으로 직접 읽는다(docx 는 열지 않는다).
> **지어내지 않는다** — 화면의 모든 칸은 산출물 문장이고, 없으면 비우고 `미확정 (D-nn)` 을 표시한다. 목업의 `(예시)` 값은 시드에 넣지 않는다.
> **조용한 실패 금지** — `try/except` 로 하드코딩 결과를 끼워넣지 않는다. 실패는 사용자에게 보인다(§2.5).
> **이 회사는 ETO 주문 설계·제작 고정밀 산업장비(반응기·교반기·진공건조기·누체필터·저장탱크) 기업이다.**
> 프레스·금형·샷카운트·전착·마킹·프론트쉘(광성정밀), 절임·발효·염도·pH(김치), 와인딩·에이징·타월(송월),
> 포밍·롤포밍·로봇용접·레토르트·판금·금속 구조물(사업계획서 잔재)은 **어디에도 쓰지 않는다**(§6).
> **이 사업에 예지보전·품질예측 모델은 없다**(D-01). 만들면 결함이다.
> 담당 밖 파일은 만지지 않는다(§3.4). 끝나면 실측값과 검증 명령을 [본인 진행 파일]에 적는다.

**아키텍트 (Phase 0 + 웨이브 A)**
> Phase 0: `uv` + Python 3.12 로 프로젝트를 세우고(`src/kyungdong/`, 패키지명 `kyungdong-ai`) `kyungdong_db`(PostgreSQL 17 + pgvector 0.8.6)를 만든다.
> 정본 두 JSON 을 `docs/design/` 에, 사업계획서 PDF 를 `docs/` 에, 화면설명서 HTML 을 `docs/산출물/` 에, CAD 통계 xlsx 3종과 라벨링 가이드를 `docs/cad/` 에 둔다. `.env.example` 에 `KYUNGDONG_*` 키를 전부 적는다(§8).
> 웨이브 A: TD5 `details[*]`(68표·762컬럼, 행 = `[한글명, 영문명, 타입, PK, FK, NULL, 비고]`)에서 `db/schema.sql` 을 **생성기(`tools/gen_schema.py`)로 뽑고**(손으로 고치지 않는다) 생성 중 발견한 TD5 결함(FK 타입·누락 컬럼 등)은 `decisions.md` 에 올린다.
> `contracts/db-schema.md` · `api-contract.md` · `screen-map.md`(45행 + §3.2 담당표 + §3.4 소유권) · `interfaces.md`(`db.conn.q/x/tx` · `app.design.screen/program/requirement/table_def` · `templating.render` · `util.http` 오류 규약 · `ingest` 수집 API · `cad` 업로드 API 시그니처)를 확정한다.
> `app/nav.py`(10영역 45화면 단일 소스 — `td3.menu_shortcuts` 순서) · `app/rbac.py`(TD3 role_matrix **6역할×8영역**) · `app/util/`(오류 규약·세션·속도제한·보안헤더·CSRF) ·
> 미구현 화면용 `_placeholder.html`(TD3/TD4/AD2 문장을 200 으로 렌더) · `tools/check_routes.py` · `tools/gate.py`(§2 판정표, QA 검사기는 있으면 호출 없으면 `미구현`, 라이브 서버는 **빈 포트 + `/health` 200 확인 + 프로세스 그룹 종료**, 시드 비밀번호는 실행용 난수 발급 후 공통 시드로 재정렬 — §10-6) 를 만든다.
> `../08 AI Agent Making Agent/work-kyungdong-cockpit/kyungdong-globaltech-ai-agent/` 의 도구 11종을 TD5 68표에 재배선하는 매핑표를 D-20 에 적는다(§1.2).
> 산출물 결함은 고치지 말고 `decisions.md` 에 올린다(§7 에 이미 24건 등재됨).

**개발1 (입고·기준·시스템·데이터 / 15화면)**
> 담당: 화면 `005~008` `026~029` `030~032` `033~036` + 로그인·메인시안·팝업·오류. 테이블 `INV_`(6) `BAS_`(3) `SYS_`(4) `DAT_`(7) + `IF_ERP_*`(3) = 23종.
> R1: 스키마 적용 · 멱등 시드(시간 앵커 고정, 계정 비밀번호는 난수 1회 출력) · 인증(bcrypt/Argon2 해시, 정책 수치는 `.env` D-16) · RBAC 6역할 · 공통 레이아웃(헤더/좌측메뉴 권한별 노출/조회조건 한 줄/그리드 페이징·정렬·엑셀/하단 버튼/우측 설명 패널 — TD3 layout_rules).
> `BAS_COMMON_CODES` 는 **10공정**(수주견적관리·입고검사·소재가공(외주)·가공(레이저커팅)·제관 및 용접·버핑(외주)·조립·출하검사(수압·기밀·진공)·포장·출하)과 **제품군 5종**(반응기·교반기·진공건조기·누체필터·저장탱크)으로 시드한다. 외주 2공정(소재가공·버핑)은 **발주·반출·반입 상태 관리**이지 설비 수집 대상이 아니다.
> `BAS_QUALITY_STANDARDS` 는 **ASME·KS 기준과 수압·기밀·진공 시험 항목**에 맞춘다. 기준값은 화면 입력, 시드에 지어내지 않는다.
> 입고 흐름: 스마트패드로 자재 LOT·규격(재질·두께)·중량 등록 → **MTC(재질성적서) 첨부** · 검사항목 판정 → 정합성 검증·표준화 → `INV_STOCKS` 반영 → `INV_SUPPLIER_QUALITY` 누적.
> ERP(이카운트) 연계(033 에서 상태 조회): **보유 여부 자체가 사업계획서 안에서 상충한다**(D-07) → **Excel 적재 경로를 정식 입력**으로 만들고 `IF_ERP_RECEIPTS/SHIPMENTS/STOCKS` 에 기록한다. 사용자·공급처 담당자 개인정보는 암호화 저장 + 화면 마스킹(G-29).
> 채번 형식(프로젝트번호·자재LOT·제품LOT·출하번호 접두)을 정하면 `progress-dev1.md` §1 에 공표한다 — 개발2·3 이 그대로 쓴다.

**개발2 (대시보드·공정·출하·KPI / 16화면)**
> 담당: 화면 `001~004` `016~019` `021~025` `043~045` + 공통 대시보드(현황판 65" 2대). 테이블 `SHP_`(6) `PRC_`(7) `KPI_`(2) 15종.
> **KPI 는 확정값이다.** `app/kpi.py` 한 곳에 산식을 두고 `KPI_TARGETS` 에 시드한다:
> **제조 리드타임 = 작업지시 확정시각 ~ 포장완료 시각 평균 소요시간** — 기존 **1,320h(55일) → 목표 1,080h(45일)**, 감소율 (1,320−1,080)÷1,320×100 = **18.2%**, 가중치 0.5 ·
> **수주출하 리드타임 = 출하시각 − 수주확정시각** — 기존 **1,440h(60일) → 목표 1,200h(50일)**, 감소율 **16.7%**, 가중치 0.5. 측정근거는 **2025년 생산 일지**(사업계획서 1.5).
> 메인 카드와 현황판(생산 목표 대비 실적 · 설비 가동률 · AI 분석 결과 · 프로젝트 진행률 · 납기 리스크)은 **KPI 화면 043~045 와 동일 산식**. 현황판은 조회조건 없이 자동 갱신 — 주기는 `.env`(D-09).
> **품질 KPI(044)는 공식 성과지표가 아니다** — 화면에 그렇게 구분 표기한다(TD3 common_screens dashboard checks).
> 공정관리는 **10공정 공통**이되 **자동 수집은 레이저커팅 1공정뿐**이다. 나머지 공정 실적은 터치PC·스마트패드 수동 입력이고, 외주 2공정은 발주·반출·반입 상태다. 자동 수집인 척 렌더하면 결함이다(D-06).
> 출하: **검사 합격 LOT 만** 출하 대상 선택 가능(422) → 출하 등록 → **출하 확정 시각 기록**(KPI 산출 근거) → ERP 회신은 `IF_ERP_SHIPMENTS` 기록까지. **출하 승인은 담당자 승인 없이 확정 불가 + 승인 이력 기록**(G-24).
> 차트는 인라인 CSS/SVG 로 그린다(외부 CDN 0). **LOT·프로젝트번호 클릭 → 024 공정이력조회** 이동 규약을 지킨다.

**개발3 (AI: CAD 견적 자동화 + 수집 + RAG Agent / 14화면)**
> 담당: 화면 `009` `010~015` `020` `037` `038~042`. 테이블 `EST_`(16) `AGT_`(3) `DAT_`(4) `IF_`(7) 30종 + 수집·CAD API.
> R1-a 수집: `ingest` 수집 API(TD4-047 flow: 장비 등록 → 태그 매핑 → 수집 → 시계열 적재 → 수집이력) + `tools/plc_simulator.py`(**레이저커팅기 1대만** — 작업시간·수량·가동상태·알람·속도·압력·전류·온도, 시간 앵커 고정, **이상 주입 옵션**, **Gateway 버퍼 단절/복구 옵션**). 적재 경로는 `IF_PLC_SIGNALS` → `PRC_EQUIP_SIGNALS` / `DAT_TIMESERIES`. 수집 중단 시 화면 003/022 배지(G-12).
> **수집 지점은 2개소뿐이다**(레이저커팅기 PLC · 터치PC). 다른 공정 설비 수집을 만들면 결함이다(D-06).
> R1-b CAD 파이프라인: `IF_CAD_FILES` 업로드 → Parsing(Autodesk API) + Vision AI(YOLOv8 + OCR) **병렬** → `EST_CAD_OBJECTS` → **HITL 검증(`EST_OBJECT_REVIEWS`)** → `EST_CAD_FEATURES`(홀 수량·절단 길이·형상 복잡도·두께·재질·용접 길이) → **Rule + GNN** BOM(`EST_BOM_HEADERS`/`ITEMS`/`ROUTINGS`/`EST_MATERIAL_REQS`) → **XGBoost** 견적(`EST_QUOTATIONS`/`EST_QUOTATION_ITEMS` + `EST_COST_RATES`) → **SHAP**(`EST_SHAP_FACTORS`).
> **Autodesk API·YOLOv8 가 없으면 501 또는 명시 배지로 드러낸다**(D-05). 조용히 합성 Feature 를 내지 않는다. 실 도면은 `docs/cad/` 통계·라벨링 가이드를 기준으로 다루고, 정제(**중복 183건 · 0KB 4건 · 도면번호+버전+고객사 중복 제거**)를 코드 경로로 만든다(D-03).
> R1-c ML: **G-14~G-18 은 실측 보고한다** — 표본 수 · 제품군 분포 · 앵커 5개 분포(min/mean/max)를 함께 적고, 못 맞추면 수치를 만들지 말고 `차단`으로 남긴다. Label(과거 견적금액·실제 제조원가)이 없으면 **D-04 로 차단**하고 지어내지 않는다.
> 모델 버전·성능·상태는 `EST_ML_MODELS`/`EST_ML_TRAIN_RUNS`/`EST_ML_PREDICTIONS` 에, Train/Validation/Test 분리는 `DAT_DATASET_SPLITS` 에 남긴다(G-25).
> R1-d RAG: `kyungdong-globaltech-ai-agent/` 를 D-20 매핑대로 **이식·확장**한다(재작성 금지). 지식문서 = 작업표준서(SOP)·품질기준·검사기준·MTC 기준·클레임 이력(`AGT_VECTOR_DOCS`).
> **폐쇄형이다** — 외부 검색 0. 근거 0건이면 **LLM 을 부르지 않고 "검토 필요 — 근거 부족" + 담당자 이관**. 시안 프롬프트 7~10번(검색하지 않은 문서명·조항·페이지를 지어내지 않는다)을 그대로 지킨다.
> 모든 질의·응답·근거·응답시간을 `AGT_QUERY_LOGS` 에 전량 기록하고 권한별 조회 범위를 제한한다. API 키 없으면 **501 "LLM 미구성"**, 임베딩 미구성이면 `tsvector_keyword` 모드 라벨(D-08).
> Agent 3종(입고 009 · 출하 020 · 통합 038)과 040 의사결정 지원 · 041 알림 및 추천. **전부 추천이다** — 견적 확정·발주·대체자재·검사 합격·출하는 **승인 절차 → `AGT_RECOMMENDATIONS`(`ADOPT_YN`·`ADOPT_BY`)** 기록(G-24).
> 학습이 있는 회전은 **단독 기동**을 요청한다(§10-2).

**QA1 / QA2 / QA3 (웨이브 C, 병렬)**
> §3.3 의 담당 범위를 §2 게이트 표와 §2.5 오류 계약으로 검증한다. **고치지 않는다.**
> 결함은 `DEF-QA{n}-nnn` 으로 번호를 붙이고 재현 절차·실측·기대값·담당 개발자를 적어 `outputs/qa{n}-*.md` 에 쓴다. 통과 항목도 실측값과 명령을 함께 적는다.
> 테스트 시나리오는 `../04 QA Agent/outputs/` 의 단위·통합·사용자테스트결과서와 체크리스트 HTML 3종에서 끌어온다.
> 테스트는 0건 경로·N건 경로를 모두 명시 단언하고 `skip` 하지 않는다. QA3 의 RAG 정답셋은 `AGT_VECTOR_DOCS` 본문에서만 만들고 `D-nn` 같은 문서 식별자를 핵심어로 요구하지 않는다(§10-7).
> **검사기는 판정 정본 함수를 직접 호출한다**(§10-16). 로직을 복제하면 개발자 수정이 반영되지 않아 수치가 굳는다.
> 결함이 해소되면 리포트의 Critical 문구를 **실측으로 갱신**한다 — 문구가 남아 있으면 미해결로 판정된다(§4.4-2).

---

## 6. 지켜야 할 것

- **§0.3 정본 우선순위를 지킨다.** 코드 범위는 산출물, 수치·정책은 사업계획서 보완본. 충돌은 D-번호로 등재하고 조용히 고르지 않는다.
- **타 사업 오염 주의 (이 사업의 가장 큰 위험).** 위험이 **두 겹**이다.
  - **① 직전 사업 코드에서 넘어오는 것** — 광성정밀(프레스·금형·샷카운트·전착·마킹·프론트쉘·**예지보전**), 평창김치(절임·발효·염도·pH·레시피), 송월타월(와인딩·에이징·타월). `grep -rn "kwangsung\|kkotsuni\|songwol\|프레스\|금형\|샷카운트\|전착\|발효\|염도\|pH\|와인딩\|에이징\|예지보전\|불량예측" src/` = **0** 을 G-03 에 포함한다.
  - **② 사업계획서 자체에 남은 잔재** — 실측 확인: **포밍 10건 · 롤포밍 1건 · 로봇용접 2건 · 레토르트 1건(p.58) · 판금 1건 · "금속 구조물/금속 구조재" 2건**(2.5 p.19 · 2.7.4 p.32 · 10.1 p.56). 이 문장들은 1.1 의 ETO 고정밀 산업장비 서술 및 CAD 아카이브 실측(반응기 계열 48프로젝트·470도면)과 정면으로 어긋난다 → **D-02. 채택하지 않는다.** 단 p.2 의 '멸균' 은 **진공건조기 용도 설명이므로 정당**하다.
  - **이 회사 용어**: 반응기(Reactor)·교반기(Agitator)·진공건조기·누체필터(Nutsche Filter)·저장탱크(Receiver Tank)·Pure Steam Generator · ETO · 프로젝트(수주)번호·도면번호·리비전 · CAD Feature(홀 수량·슬롯·절단 길이·형상 복잡도·두께·재질·용접 길이) · 레이저 커팅 · 제관·용접(WPS·용접사 자격) · 버핑 · 수압·기밀·진공 시험 · MTC(재질성적서) · ASME·KS · 외주 반출·반입.
  - 용어 검색으로 안 걸리는 것은 **내용으로 판단**한다. 반복 생산 라인·배치·불량률 중심 화면이 나오면 그 자체가 오염이다 — 이 회사는 **다품종 소량 ETO** 다.
- **예지보전·품질예측 모델을 만들지 않는다**(D-01). 사업계획서 2.5 가 "현 사업 미적용" 으로 명시했다. 확장 구조만 남긴다.
- **AI 는 조회·분석·추천·경고까지.** 설비 제어·자동 반영 금지. 사람이 승인한 결과만 반영하고 **승인 이력을 남긴다**.
- **AI 산출 결과 화면은 반드시 Confidence Score 와 근거를 함께 표시**하고, 관리자 검토·승인 전에는 **확정 상태로 표기하지 않는다**(TD3 standard_note 4).
- **모든 업무 화면의 최상위 조회 축은 프로젝트(수주)번호·도면번호다.** LOT 는 프로젝트 하위 식별자다(TD3 standard_note 2).
- **수집 지점은 2개소뿐이다.** 전 공정 실시간 수집을 전제한 화면·시뮬레이터·문구를 만들지 않는다(D-06).
- **RAG 는 폐쇄형** — 내부 데이터만, 외부 검색 0, 모든 질의·응답·근거를 기록. 근거 0건이면 "검토 필요".
- **중복 만들지 않는다.** 공용 컴포넌트·산식·채번은 한 곳에만 둔다.
- **현장 단말 채널을 화면마다 존중**한다 — `td3.screens[*].channels` 에 현장POP·스마트패드·현황판이 적힌 화면은 터치 레이아웃(입력 항목 축소·큰 버튼·스캔 우선)과 자동 갱신을 갖춘다.
- 산출물 결함은 **고치지 말고** `decisions.md` 에 적고 도입기업에 제안한다.

---

## 7. 알려진 결함·미확정 (착수 전 인지 · 정본에서 실측 확인됨)

### 7.1 확정된 것 — 그대로 쓴다

| ID | 내용 | 확정값 |
|---|---|---|
| **D-01** | **AI 과제 성격** | **CAD 견적 자동화 AI + 입고·출하 RAG Agent 3대 공정.** 가공(포밍)·제관 및 용접·포장 공정의 **설비 예지보전·품질예측은 "현 사업 미적용"**(사업계획서 2.5 · 1.3 AI 대상공정 X · TD1 overview · TD2 review). 화면·프로그램·테이블·모델을 만들지 않는다 |
| **D-21** | 성과지표(KPI) | **제조 리드타임 1,320h → 1,080h**(−18.2%, 가중치 0.5, 작업지시 확정~포장완료) · **수주출하 리드타임 1,440h → 1,200h**(−16.7%, 가중치 0.5, 출하시각−수주확정시각). 측정근거 **2025년 생산 일지**(수주출하는 3개월분) |
| **D-22** | AI 성능목표 5종 | CAD 객체 인식 **80%↑**(IoU 0.5 · P/R/F1) · 견적 산출 정확도 **±5% 이내**(MAPE·MAE) · BOM 생성 정확도 **85%↑**(항목 단위 P/R/F1) · 납기 예측 정확도 **85%↑**(허용오차 + RMSE) · 설명가능성 **90%**(SHAP Top-N vs 전문가, Spearman ρ) |
| **D-23** | 데이터 유효성 | 정확성 **85%↑** · 정합성 **85%↑** · 연계성 **85%↑** · AI 학습 적합성 **판정**(Feature–Label 상관 · 분포·편향 · Train/Val/Test 분리) — 사업계획서 2.7.4 표 |
| **D-24** | 사업비 | 총 **398,510천원** = 개발비 200,000 + H/W 58,150 + S/W 50,000 + N/W 16,000 + 기타 2,360 + 인건비(현물) 36,000 + 클라우드 36,000. 정부지원금 **199,255** / 부담금 163,255 / 현물 36,000. TD2 가 정합 확인 완료 |
| **D-14** | Presentation 스택 | 사업계획서 2.4 가 **Nginx · FastAPI · Streamlit · Docker** 를 명시. 45화면 RBAC·그리드·팝업 규격에는 Streamlit 이 부적합하므로 **FastAPI + Jinja2 서버 렌더**로 구현하고 차이를 기록한다(직전 세 사업 동일 판단) |

### 7.2 산출물·정본 **내부 충돌** — 판정을 적어 둔다

산출물은 고치지 않는다. 아래 판정을 코드에 적용하고 차이를 화면 설명 패널과 최종 보고에 적어 도입기업에 제안한다.

| ID | 충돌 | 판정 |
|---|---|---|
| **D-02** | **회사 정체성 오염.** 사업계획서 2.5(p.19)·2.7.4(p.32)·10.1(p.56)이 이 회사를 「금속 구조물 제조」·「판금 가공 기반」·「금속 구조재 제조」로 서술. 또 **포밍 10건 · 롤포밍 1 · 로봇용접 2 · 레토르트 1(p.58)** 잔재 | **1.1 의 ETO 고정밀 산업장비 서술을 채택한다.** CAD 아카이브 실측(반응기 계열 48프로젝트·470도면)이 1.1 과 일치한다. 잔재 용어는 코드·화면·시드에 **한 글자도 넣지 않는다**(§6). p.2 '멸균' 은 진공건조기 용도로 정당 |
| **D-11** | **견적 정확도 표기 상충.** p.11 「견적 정확도 **80% 이상**」 vs 2.6-2 「**±5% 이내**」 vs TD1 kpi_effects 「±10% → ±5%」 | **2.6 의 ±5% 를 게이트로 쓴다**(G-15). 80% 는 최종 보고에 차이로만 적는다 |
| **D-07** | **ERP 보유 여부 상충.** 2.5 본문은 현행을 「EXCEL 중심」으로 서술, 1.2·1.6·2.7.2 는 **ERP(이카운트) 보유**를 명시(1.6 에서 보유여부 'O' 인 유일한 솔루션) | **EIF 'ERP 연계'(TD4-046)는 설계에 포함하되 실 연동은 범위 밖.** Excel 적재를 정식 입력으로 만들고 `IF_ERP_*` 이력 기록까지. 실사용 범위는 착수 시 확정 — 담당자 확인 필요 |
| **D-18** | **Vector DB 제품 상충.** 2.4 「PostgreSQL + pgvector」 vs 4.7 「EC2 t3.large 자체구축 **Qdrant**」 | **2.4 의 PostgreSQL + pgvector 를 채택**(TD5 criteria 와 동일). 제품 확정은 착수 시 — 담당자 확인 필요 |
| **D-19** | **FP·개발비 상충.** 사업계획서 4.2 **281.08 FP** · 개발비 200,000천원 · 이윤 10% vs 산출내역서 v0.97 **340.76 FP** · 269,999,999원 · 이윤 25%. 단가 605,784원/FP · 보정계수 1.07008 은 양쪽 동일하고 각 문서 내부 산식은 성립 | **사업계획서 4.2(281.08 FP / 200,000천원)를 채택.** 차이 59.68점(21.2%) · 70,000천원 · 이윤율 15%p 는 미확정으로 남기고 도입기업에 제안. **구현에 영향 없다** — 단 **테이블 68 은 산출내역서 FP산정리스트(ILF 17행 RET 58 + EIF 4행 RET 10)에서 도출**됐으므로 그 수치는 유지한다 |
| **D-25** | **장비 명칭 불일치.** 2.1·2.3 「터치PC」 vs 4.3 「현장POP」(각 1대) · 2.1 「분산된 **LOT** 데이터」 vs 2.3 「분산된 **OT** 데이터」 | 동일 장비·동일 대상으로 본다. 화면 라벨은 **「현장POP(터치PC)」** 병기. 확인 필요 |

### 7.3 여전히 **미확정** — 지어내지 않는다

| ID | 내용 | 처리 |
|---|---|---|
| **D-03** | **CAD 학습 데이터 실태.** 1,283건 / 77프로젝트. **파일명 중복 183건(14.3%) · 0KB 손상 4건 · 제품당 편차 최대 184/최소 1/중앙값 5 · 도면 1건뿐인 제품 9개(11.7%) · 발행일·개정일 메타데이터 없음(mtime 대체)** | 정제 경로를 코드로 만들고(도면번호+버전+고객사 중복 제거) 앵커 고정. 표본·분포 병기. 미달·경계선이면 `차단`. 화면에 `합성 데이터 기준 (D-03)` |
| **D-04** | **견적 Label 부재.** 과거 견적금액·실제 제조원가 Label 확보 여부 미확인. 사업계획서 2.7.4 는 "실제 제조원가 기준으로 학습용 Label(견적값) 정제" 를 요구하지만 보유 규모가 없다 | G-15·G-17 은 Label 없이 측정 불가 → **차단 (D-04)**. 합성으로 대체하면 화면·리포트에 명시 |
| **D-05** | **CAD 파서 미확보.** Autodesk API 계정·YOLOv8 가중치·OCR 엔진 미확인. `.dwg` 는 바이너리 | 공급자 인터페이스 추상화. 미구성이면 **501 또는 명시 배지**(§2.5). 조용히 합성 Feature 금지 |
| **D-06** | **수집 지점 2개소 한정.** 레이저커팅기 Master PLC 1 + 터치PC 1. **수집 주기·규모가 사업계획서에 없다** | 시뮬레이터는 레이저커팅기 1대만. 다른 공정은 `수동 입력` 표기. 주기는 `.env` `가설 (D-06)` |
| **D-08** | **LLM·임베딩 미확정.** 4.7 은 Bedrock 을 적었으나 모델명·임베딩 모델·벡터 차원·인덱스 미정 | 공급자 인터페이스 추상화. 키 없으면 501. 임베딩 없으면 `tsvector_keyword` 모드 라벨 |
| **D-09** | **목표치 부재 항목.** RAG 응답시간 · 인용 정확도 · 시계열 동기화율 · 현황판 갱신 주기 · 백업 주기 · RTO/RPO · 로그 보존기간이 사업계획서에 없다 | **실측만 보고한다.** 목표 없는 것을 있는 척하지 않는다. 설정값은 `.env` `가설 (D-09)` |
| **D-10** | **RAG 그라운딩 임계값 부재.** 사업계획서에 신뢰도 임계 수치가 없다(광성의 0.85 는 **다른 사업의 값**이다 — 가져오지 않는다) | `.env` `RAG_CONFIDENCE_MIN` 가설값 + 화면 `가설 (D-10)`. 근거 0건 분기는 임계와 무관하게 항상 적용 |
| **D-12** | **납기 예측 허용 오차 범위 부재.** 2.6-4 「허용 오차 범위 내 정확도」인데 범위 미기재 | `.env` 가설값 + 화면 `가설 (D-12)` |
| **D-13** | **AI 성능목표 산식 칸 5개 전부 공란** + 설명가능성 「90% 이내」 표기 모호 + **전문가 평가 변수 목록 없음** | 산식은 ⓷ 검증방법 문장에서 확정하고 근거를 적는다. G-18 일치율은 `차단 (D-13)` — 전문가 변수 목록 요청 |
| **D-15** | **보안 사양 미상세.** 9.2 는 최소권한·비밀번호 복잡도·주기변경·VPN/암호화 통신·접근 로그·백업/DR·폐쇄형 Agent 까지만. **TLS 버전·저장 암호화 알고리즘·해시 함수 미기재** | HTTPS·bcrypt/Argon2 로 구현하되 **다른 사업의 사양(TLS 1.2+/AES-256)을 정본인 척 적지 않는다.** 차이를 D-15 에 적고 제안 |
| **D-16** | 비밀번호 길이·조합·변경 주기, 로그인 실패 잠금 횟수, 자동 로그아웃 시간 미기재 | `.env` 가설값. 화면에 `가설 (D-16)` |
| **D-17** | 개인정보·로그 보존기간 미확정 | 마스킹·암호화는 구현. 보존·삭제 정책은 설정 키만, 배치 없음 |
| **D-20** | Agent 시안 도구 11종 ↔ TD5 68표 재배선 매핑 | 아키텍트가 웨이브 A 에서 작성 |
| **D-26** | **현행 설비 수량·사양 미기재.** 2.7.2 에 호이스터·레이저절단기(외주)·적재기·운송설비만 있고 수량·사양·위치 없음. 제관/용접·시험 설비, CAD PC 대수 미확인 | 화면 입력 마스터. 시드에 지어내지 않는다. 현장 실사 필요 |
| **D-27** | **네트워크 미기재.** 현행 유선망 회선 속도·구성도·스위치 사양, 설비망/사무망 분리 여부, 무선AP 3대·현황판 2대 설치 위치 미기재 | 범위 밖. 최종 보고에 명시 |
| **D-28~** | 루프가 잇는다 | — |

---

## 8. 스택·환경 (2026-09-11 실측 · Phase 0 에서 확정)

- **PostgreSQL 17.10** (Homebrew, unix socket `/tmp:5432`, peer 인증) · DB **`kyungdong_db`**(새로 만든다) · `vector` **0.8.6** 설치 확인됨 → `create extension vector`
- **Python 3.12** (`/opt/homebrew/bin/python3.12`) · **uv 0.12.1**. 시스템 `python3`(3.9)는 쓰지 않는다
- **FastAPI + Jinja2 + psycopg** (D-14) · Node 24 (빌드 도구용) · **Docker 없음** · **`pdftotext` 없음** → PDF 는 `uv run --with pypdf`
- ML: scikit-learn · xgboost · numpy · pandas · shap. GNN·YOLOv8 은 미확보 시 D-05 로 차단하고 Rule 기반으로 대체(대체 사실을 화면에 표시)
- **API 키 없음** → Agent 는 501 명시 렌더링(D-08). 키가 들어오면 `.env` 에만 두고 `grep -rn 'sk-'` = 0
- 환경변수 접두 **`KYUNGDONG_`** — `PG_DSN`(기본 `postgresql:///kyungdong_db`) `SESSION_SECRET` `ENV`(dev/prod — prod 는 쿠키 Secure·HSTS, 세션 키 미설정 시 기동 거부) `SEED_PASSWORD`(자동화 전용) `LLM_PROVIDER` `LLM_MODEL` `EMBED_PROVIDER`(D-08) `RAG_CONFIDENCE_MIN`(D-10 가설) `RAG_TIMEOUT_SEC`(D-09 가설) `CAD_PARSER`(D-05) `DUE_DATE_TOLERANCE_DAYS`(D-12 가설) `LOGIN_FAIL_MAX` `SESSION_IDLE_MINUTES` `PASSWORD_MIN_LEN` `PASSWORD_CHANGE_CYCLE_DAYS`(D-16) `BOARD_REFRESH_SEC` `INGEST_STALE_SEC`(D-09) `PLC_POLL_SEC`(D-06 가설) `CSRF_ENFORCE`
- 개발 서버 포트 **8020** — **8000·8010 은 이미 점유 중이다**(실측). 게이트용 라이브 서버는 빈 포트를 OS 에서 받는다(§10-6)
- 이웃 DB `kwangsung_db` `kkotsuni_db` `songwol_db` `koryo_solder_db` `mes_db` 가 같은 서버에 있다 — **건드리지 않는다**

---

## 9. 게이트 실행 명령 (§2 판정은 이것으로만 한다)

```bash
make setup          # uv python pin 3.12 && uv sync
make gen-schema     # SF-TD5(design.json) → db/schema.sql 재생성 + 결함 보고 (schema.sql 은 손으로 고치지 않는다)
make db-schema      # kyungdong_db 초기화 + db/schema.sql 적용 → "테이블 68"
make db-seed        # 기준정보·10공정·제품군 5종·역할 6·사용자 시드 (멱등, 앵커 고정, 비밀번호 난수 1회 출력) + seed_dev1/2/3
make db-reset       # db-schema + db-seed  (앵커를 새로 발급하므로 ML 수치는 분포 안에서 움직인다 — §10-3)
make run            # uvicorn kyungdong.app.main:app --app-dir src --port 8020 --reload
make simulate       # tools/plc_simulator.py — 레이저커팅기 1대 → 수집 API (--anomaly-at 이상 주입, --drop Gateway 버퍼 재전송 시험)
make cad-ingest     # tools/cad_ingest.py — 도면 수집·정제(중복·0KB 제거) → EST_CAD_DRAWINGS
make test           # uv run pytest -q
make check-routes   # 45화면 + 공통 4 + 오류 HTTP 200 · nav ↔ screen-map ↔ design.json · RBAC · Agent 501 · 타 사업 용어 0
make check-trace    # AD2-001~045 ↔ TD3 ↔ TD4 1:1 · EIF 4 ↔ TD4-046~049 (G-04·G-05)
make check-schema   # TD5 68표·762컬럼 ↔ information_schema (G-01·G-02)
make check-data     # 디지털 스레드·정확성·정합성·연계성·시계열·빈 테이블·시드 멱등 (G-07~G-11)
make check-ingest   # 시뮬레이터 유실 0 · 순서 · Gateway 버퍼 재전송 · 전처리·Feature (G-12·G-13)
make check-ai       # G-14~G-25 (--base-url 로 라이브 RAG 채점)
make check-security # G-26~G-30
make gate           # 읽기 전용 판정표 G-01~G-30 (개발자 작업 중에도 안전)   ← 루프가 매 회전 부르는 것
make gate-full      # 시드 2회·라이브 서버·RAG 채점까지 실측 (회전 마감·종료 판정용)
```

실측 재확인 (이 문서의 숫자가 의심되면):

```bash
D="../02 Design Agent/outputs/20260814-094524-all/design.json"
A="../01 Analyze Agent/outputs/20260812-143653-all/deliverables.json"
python3.12 -c "import json;d=json.load(open('$D'));print('화면',sum(len(g['screens']) for g in d['td3']['screens']),'영역',len(d['td3']['screens']),'프로그램',sum(len(x['programs']) for x in d['td4']['details']),'테이블',len(d['td5']['details']),'컬럼',sum(len(t['columns']) for t in d['td5']['details']),'공통',len(d['td3']['common_screens']),'역할',len(d['td3']['role_matrix']['rows']))"
python3.12 -c "import json;a=json.load(open('$A'));f=sum(len(g['requirements']) for g in a['ad2']['functional_groups']);n=sum(len(g['requirements']) for g in a['ad2']['nonfunctional_groups']);print('기능',f,'비기능',n)"
python3.12 -c "import json,collections;d=json.load(open('$D'));print(dict(collections.Counter(t['id'].split('_')[0] for t in d['td5']['details'])))"
# 기대: 화면 45 영역 10 프로그램 49 테이블 68 컬럼 762 공통 4 역할 6 / 기능 45 비기능 13
#       BAS 3 SYS 4 INV 6 EST 16 SHP 6 PRC 7 DAT 11 AGT 3 KPI 2 IF 10
uv run --with pypdf python -c "
from pypdf import PdfReader; import re
t='\n'.join((p.extract_text() or '') for p in PdfReader('docs/사업계획서.pdf').pages)
for w in ['1,320','1,080','1,440','1,200','80%','±5%','85%','90%','포밍','롤포밍','로봇용접','레토르트','판금','금속 구조']:
    print(w, len(re.findall(re.escape(w), t)))"
# 기대: KPI 4종 존재 / 잔재 용어 포밍 10 롤포밍 1 로봇용접 2 레토르트 1 판금 1 금속 구조 2 (§6 D-02)
```

---

## 10. 직전 사업(광성정밀·평창김치·송월타월)에서 실제로 겪은 것 — 이 사업에서는 처음부터 지킨다

1. **차단 결정의 가설값 투입** — 사용자가 "값을 임의로 정하고 나중에 조정"을 지시하면 차단 D 를 `가설` 로 바꾸고 값은 **`.env`(설정) · 시드(마스터) · 합성 데이터(ML) 한 곳**에만 둔다. 코드 상수 금지. 화면 배지는 `가설 (D-nn)`. 게이트 임계값은 그대로.
2. **에이전트 5명 이상 동시 기동 시 API 세션 한도(429)** 를 쳐서 전원이 멈췄다. ML 학습이 있는 개발3 회전은 **단독**, 나머지는 3~4명까지. 죽은 에이전트 재개는 컨텍스트 재로딩으로 한도를 또 소진한다 — 새 에이전트를 쓴다.
3. **합성 데이터의 `date.today()` 기준 생성** 때문에 날짜가 바뀌면 ML 수치가 흔들렸다(R² 0.90 ↔ 0.86). 생성 기준일은 **시간 앵커(`SYS_CONFIGS`)** 로 고정하고 앵커 5개 분포로 경계선을 드러낸다. `db-reset` 은 앵커를 새로 발급한다. 표본이 작으면 지표가 크게 요동친다 — **out-of-fold 예측**으로 재측정한다. **이 사업은 도면 1건뿐인 제품이 9개(D-03)라 이 위험이 더 크다.**
4. **런타임 전용 표는 깨끗한 DB 에서 0건** 이라 시드가 채운 상태에서만 검사하면 결함이 숨었다. 화면·테스트 모두 **0건 경로를 명시**한다. 종료 판정은 반드시 `make db-reset` 후.
5. **`.env` 는 권한 분류기가 `sed -i` 를 막는다** — 편집기 도구로 고친다. 셸에 같은 이름의 환경변수가 있으면 `.env` 보다 우선한다.
6. **게이트용 라이브 서버**: 고정 포트에 옛 서버가 남아 있으면 옛 서버가 응답해 결과가 오염됐다. **이 머신은 8000·8010 이 이미 점유 중이다.** 빈 포트를 OS 에서 받고 `/health` **200** 일 때만 준비로 보며 프로세스 그룹째 종료한다. `gate.py` 가 그룹 종료코드 하나로 여러 게이트를 뭉개지 않도록 **게이트별 판정 줄을 파싱**한다.
7. **RAG 정답셋**이 `D-06` 같은 문서 식별자를 핵심어로 요구해 정답을 틀리게 채점했다. 정답셋 핵심어는 **문서 본문의 도메인 용어**만. 임계값을 정답셋을 보고 정했다면 그 사실을 적고 **새 질문 10건**으로 재검증한다.
8. **`grid_table()` 의 `cell|safe`** 로 저장형 XSS 가 들어갔다. 그리드 셀은 이스케이프가 기본이고 링크 셀만 `Markup.format` 으로 만든다. QA3 이 `<script>` 시드로 확인한다.
9. **600초 스톨한 에이전트를 재개**하는 것보다 **새 에이전트 + 잔여 작업 한 가지**가 빨랐다. 광성 사업에서 스톨 4회가 났고 **전부 문서 읽기 단계**에서 멈췄다 — 읽으라고 지시하는 **파일을 3개 이하**로 줄인다.
10. **QA 리포트의 Critical 문구가 실측 해소 후에도 남아** 종료 조건을 막았다. 결함 해소 시 QA 가 리포트를 실측으로 갱신한다.
11. 시드를 여러 에이전트가 동시에 돌리면 **admin 비밀번호가 바뀌어 실패 잠금**이 걸린다. `seed.py` 재실행이 잠금을 푸는지 확인한다. **`check_ai` 가 종료 시 자기 비밀번호로 공통 시드를 되돌려 다른 에이전트 테스트가 401 로 무더기 실패**했다 — 검사기는 끝나면 공통 시드로 되돌린다.
12. 송월 사업에서 **시드 계정이 문서에 적힌 공통 비밀번호를 공유**한 것이 최대 보안 결함이었다(G-29). 계정별 난수 1회 출력 · 속도 제한(계정 5회/IP 20회) · 서버측 세션 무효화 · 전역 500 핸들러(Pydantic `detail` 노출 금지) · 외부 CDN 0(CSP `default-src 'self'`)을 **처음부터** 넣는다.
13. **좁은 화면 메뉴**가 가로 5,604px 스트립으로 터져 링크 51개 중 4개만 보였다. 반응형은 **단일 CSS 파일**에 두고 개발자별 CSS 에 미디어쿼리를 쓰지 않는다. 현장 단말(스마트패드 2대·현장POP 1대)이 실제 사용 채널이므로 **폰 폭(400px) 검증을 QA1 범위**에 넣는다.
14. **건수 카드의 `0 건` 은 정답이지 "미수집" 이 아니다.** 광성 사업에서 G-11 검사기가 정확한 0 에 `미수집` 을 강제해 **수집된 사실을 부정하는 거짓 표시**를 요구했다. G-11 의 뜻은 **"빈 그리드를 조용히 두지 마라"** 이지 "0 을 0 이라 쓰지 마라" 가 아니다. 검사기와 리포트에 **그리드(빈 표 → 문구 필요) vs 건수 카드(0 → 0 이 정답)** 를 명문화한다.
15. **빌드 게이트가 죽어 있었다.** `pyproject` `pythonpath` 설정 때문에 `pytest` 가 **수집 단계에서 통째로 중단**(540 collected · 0건 실행)했고, 그동안 §2.6 빌드 게이트가 무의미했다. **매 회전 "몇 건 실행됐는지" 를 본다.** 0건이면 그 회전의 모든 판정이 무효다.
16. **QA 검사기가 판정 정본 함수를 부르지 않고 로직을 복제**해서, 개발자가 방법론을 바꿔도 수치가 소수점까지 동일했다(혼동행렬까지 같았다). **검사기는 정본 함수를 직접 호출한다.** 설정을 되돌렸을 때 수치가 실제로 움직이는지 한 번 확인한다.
17. **동시 실행이 게이트 수치를 오염시킨다.** 같은 코드로 연속 2회 실행이 PASS/FAIL 로 갈렸고, "표를 비웠는데 행이 늘어난" 것이 증거였다. **§4.4 종료 판정은 에이전트가 하나도 돌지 않는 조용한 창에서만 한다.** 검사기는 측정 중 동시 변경을 감지하면 FAIL 이 아니라 **`판정 불가`** 를 내야 한다.
18. **에이전트가 게이트를 넘길 수 있는 길을 알고도 거부한 것은 옳은 행동이다.** 광성 사업에서 개발3 이 임계·라벨·표본을 조정하면 G-14 를 넘길 수 있다는 걸 알고도 세 번 거부하고 `차단`으로 남겼다. 이 사업에서도 같게 한다 — **수치를 만들지 말고 차단으로 남긴다.**
