# decisions.md — 경동글로벌텍 제조AI 결정 대장 (SF26179182)

`확정` / `가설` / `차단` 세 상태만 쓴다. **여기 없는 것을 마음대로 정하지 않는다.**
D-01~D-27 은 `goal.md` §7 에서 착수 시 그대로 옮겨 적은 것이다. 새 결정은 **D-28** 부터 잇는다.
개발자별 대역: 개발1 `D-1nn` · 개발2 `D-2nn` · 개발3 `D-3nn` · QA `D-4nn`.

---

## 확정 — 그대로 쓴다

| ID | 상태 | 내용 | 근거 |
|---|---|---|---|
| **D-01** | 확정 | **AI 과제 = CAD 견적 자동화 AI + 입고·출하 RAG Agent (3대 공정).** 가공(포밍)·제관 및 용접·포장 공정의 **설비 예지보전·품질예측은 "현 사업 미적용"**. 화면·프로그램·테이블·모델을 만들지 않는다 | 사업계획서 2.5 · 1.3 AI 대상공정 X · TD1 overview · TD2 review |
| **D-21** | 확정 | **KPI 2종.** 제조 리드타임 **1,320h → 1,080h**(−18.2%, 가중치 0.5, 작업지시 확정~포장완료 평균 소요시간) · 수주출하 리드타임 **1,440h → 1,200h**(−16.7%, 가중치 0.5, 출하시각−수주확정시각). 측정근거 2025년 생산 일지(수주출하는 3개월분) | 사업계획서 1.5 |
| **D-22** | 확정 | **AI 성능목표 5종.** CAD 객체 인식 80%↑(IoU 0.5 · P/R/F1) · 견적 산출 정확도 ±5% 이내(MAPE·MAE) · BOM 생성 정확도 85%↑(항목 단위 P/R/F1) · 납기 예측 정확도 85%↑(허용오차 + RMSE) · 설명가능성 90%(SHAP Top-N vs 전문가 · Spearman ρ) | 사업계획서 2.6 |
| **D-23** | 확정 | **데이터 유효성.** 정확성 85%↑(정상 데이터 수 ÷ 전체 × 100) · 정합성 85%↑ · 연계성 85%↑ · AI 학습 적합성 판정 | 사업계획서 2.7.4 표 · AD2-056 |
| **D-24** | 확정 | **사업비 398,510천원** = 개발비 200,000 + H/W 58,150 + S/W 50,000 + N/W 16,000 + 기타 2,360 + 인건비(현물) 36,000 + 클라우드 36,000. 정부 199,255 / 부담금 163,255 / 현물 36,000 | 사업계획서 4.1 (TD2 정합 확인) |
| **D-14** | 확정 | **Presentation 스택 = FastAPI + Jinja2 서버 렌더.** 사업계획서 2.4 는 Streamlit 을 명시하나 45화면 RBAC·그리드·팝업 규격에 부적합. 차이를 최종 보고에 적는다 | 직전 세 사업 동일 판단 |
| **D-28** | 확정 | **개발 서버 포트 8020.** 8000·8010 이 이웃 사업 서버에 점유 중임을 실측 확인(`lsof -nP -iTCP -sTCP:LISTEN`) | 2026-09-11 실측 |
| **D-29** | 확정 | **DB `kyungdong_db` · pgvector 0.8.6.** `create extension vector` 성공. 이웃 DB(`kwangsung_db` `kkotsuni_db` `songwol_db` `koryo_solder_db` `mes_db`)는 건드리지 않는다 | 2026-09-11 실측 |
| **D-30** | 확정 | **`pdftotext` 없음.** PDF 는 `uv run --with pypdf` 로 읽는다. PyMuPDF 도 없다 | 2026-09-11 실측 |

---

## 충돌 — 판정을 적어 둔다 (산출물은 고치지 않는다)

| ID | 상태 | 충돌 | 판정 |
|---|---|---|---|
| **D-02** | 확정 | **회사 정체성 오염.** 사업계획서 2.5(p.19)·2.7.4(p.32)·10.1(p.56)이 「금속 구조물 제조」·「판금 가공 기반」·「금속 구조재 제조」로 서술. 잔재 용어 실측: **포밍 10 · 롤포밍 1 · 로봇용접 2 · 레토르트 1 · 판금 1 · 금속 구조 2** | **1.1 의 ETO 고정밀 산업장비 서술을 채택.** CAD 아카이브 실측(반응기 계열 48프로젝트·470도면)이 1.1 과 일치. 잔재 용어는 코드·화면·시드에 한 글자도 넣지 않는다. p.2 '멸균' 은 진공건조기 용도로 정당 |
| **D-11** | 확정 | **견적 정확도 표기.** p.11 「80% 이상」 vs 2.6-2 「±5% 이내」 vs TD1 kpi_effects 「±10% → ±5%」 | **2.6 의 ±5% 를 게이트로 쓴다(G-15).** 80% 는 최종 보고에 차이로만 적는다 |
| **D-07** | 차단 | **ERP 보유 여부.** 2.5 본문 「EXCEL 중심」 vs 1.2·1.6·2.7.2 「ERP(이카운트) 보유」(1.6 보유여부 'O' 인 유일한 솔루션) | EIF ERP 연계(TD4-046)는 설계에 포함하되 **실 연동은 범위 밖.** Excel 적재를 정식 입력으로, `IF_ERP_*` 이력 기록까지. **실사용 범위 담당자 확인 필요** |
| **D-18** | 확정 | **Vector DB 제품.** 2.4 「PostgreSQL + pgvector」 vs 4.7 「EC2 t3.large 자체구축 Qdrant」 | **2.4 의 pgvector 채택**(TD5 criteria 와 동일). 제품 확정은 착수 시 — 담당자 확인 필요 |
| **D-19** | 확정 | **FP·개발비.** 사업계획서 4.2 **281.08 FP** / 200,000천원 / 이윤 10% vs 산출내역서 v0.97 **340.76 FP** / 269,999,999원 / 이윤 25%. 단가 605,784원/FP·보정계수 1.07008 은 동일하고 각 문서 내부 산식은 성립 | **사업계획서 4.2 채택.** 차이(59.68점 21.2% · 70,000천원 · 이윤율 15%p)는 도입기업에 제안. 구현 무영향. 단 **테이블 68 은 산출내역서 FP산정리스트(ILF 17행 RET 58 + EIF 4행 RET 10)에서 도출**됐으므로 그 수치는 유지 |
| **D-25** | 확정 | **장비 명칭.** 2.1·2.3 「터치PC」 vs 4.3 「현장POP」(각 1대) · 2.1 「분산된 **LOT** 데이터」 vs 2.3 「분산된 **OT** 데이터」 | 동일 장비·동일 대상으로 본다. 화면 라벨은 **「현장POP(터치PC)」** 병기. 확인 필요 |

---

## 미확정 — 지어내지 않는다

| ID | 상태 | 내용 | 처리 |
|---|---|---|---|
| **D-03** | 차단 | **CAD 학습 데이터 실태.** 1,283건 / 77프로젝트. 파일명 중복 **183건(14.3%)** · 0KB 손상 **4건** · 제품당 편차 최대 184/최소 1/중앙값 5 · 도면 1건뿐인 제품 **9개(11.7%)** · 발행일·개정일 메타데이터 없음(mtime 대체) | 정제 경로를 코드로(도면번호+버전+고객사 중복 제거) · 앵커 고정 · 표본·분포 병기 · 경계선이면 차단 · 화면에 `합성 데이터 기준 (D-03)` |
| **D-04** | 차단 | **견적 Label 부재.** 과거 견적금액·실제 제조원가 Label 확보 여부 미확인. 2.7.4 는 "실제 제조원가 기준 Label 정제" 를 요구하나 보유 규모가 없다 | G-15·G-17 은 Label 없이 측정 불가 → **차단.** 합성 대체 시 화면·리포트에 명시 |
| **D-05** | 차단 | **CAD 파서 미확보.** Autodesk API 계정 · YOLOv8 가중치 · OCR 엔진 미확인. `.dwg` 는 바이너리 | 공급자 인터페이스 추상화. 미구성이면 **501 또는 명시 배지**. **조용한 합성 Feature 금지** |
| **D-06** | 가설 | **수집 지점 2개소 한정** (레이저커팅기 Master PLC 1 + 터치PC 1). **수집 주기·규모가 사업계획서에 없다** | 시뮬레이터는 레이저커팅기 1대만. 다른 공정은 `수동 입력` 표기. 주기 `.env` `KYUNGDONG_PLC_POLL_SEC=1` `가설 (D-06)` |
| **D-08** | 차단 | **LLM·임베딩 미확정.** 4.7 은 Bedrock 만 기재. 모델명·임베딩 모델·벡터 차원·인덱스 미정 | 공급자 추상화. 키 없으면 **501 "LLM 미구성"**. 임베딩 없으면 `tsvector_keyword` 모드 라벨 |
| **D-09** | 가설 | **목표치 부재.** RAG 응답시간 · 인용 정확도 · 시계열 동기화율 · 현황판 갱신 주기 · 백업 주기 · RTO/RPO · 로그 보존기간 | **실측만 보고한다.** 목표 없는 것을 있는 척하지 않는다. 설정값은 `.env` `가설 (D-09)` |
| **D-10** | 가설 | **RAG 그라운딩 임계값 부재.** 사업계획서에 신뢰도 임계 수치가 없다. **광성 사업의 0.85 는 다른 사업 값이므로 가져오지 않는다** | `.env` `KYUNGDONG_RAG_CONFIDENCE_MIN=0.70` 가설 + 화면 `가설 (D-10)`. **근거 0건 분기는 임계와 무관하게 항상 적용** |
| **D-12** | 가설 | **납기 예측 허용 오차 범위 부재.** 2.6-4 「허용 오차 범위 내 정확도」인데 범위 미기재 | `.env` `KYUNGDONG_DUE_DATE_TOLERANCE_DAYS=3` 가설 + 화면 `가설 (D-12)` |
| **D-13** | 차단 | **AI 성능목표 ⓶ 산식 칸 5개 전부 공란** + 설명가능성 「90% 이내」 표기 모호 + **전문가 평가 변수 목록 없음** | 산식은 ⓷ 검증방법 문장에서 확정하고 근거를 적는다. **G-18 일치율은 차단** — 전문가 변수 목록 요청 |
| **D-15** | 차단 | **보안 사양 미상세.** 9.2 는 최소권한·비밀번호 복잡도·주기변경·VPN/암호화 통신·접근 로그·백업/DR·폐쇄형 Agent 까지. **TLS 버전·저장 암호화 알고리즘·해시 함수 미기재** | HTTPS·bcrypt/Argon2 로 구현하되 **다른 사업의 사양(TLS 1.2+/AES-256)을 정본인 척 적지 않는다.** 차이를 제안 |
| **D-16** | 가설 | 비밀번호 길이·조합·변경 주기, 로그인 실패 잠금 횟수, 자동 로그아웃 시간 미기재 | `.env` 가설값 4종. 화면에 `가설 (D-16)` |
| **D-17** | 차단 | 개인정보·로그 보존기간 미확정 | 마스킹·암호화는 구현. 보존·삭제 정책은 설정 키만, **삭제 배치를 만들지 않는다** |
| **D-20** | 미착수 | Agent 시안 도구 11종 ↔ TD5 68표 재배선 매핑 | 아키텍트가 웨이브 A 에서 작성 |
| **D-26** | 차단 | **현행 설비 수량·사양 미기재.** 2.7.2 에 호이스터·레이저절단기(외주)·적재기·운송설비만, 수량·사양·위치 없음. 제관/용접·시험 설비, CAD PC 대수 미확인 | 화면 입력 마스터. **시드에 지어내지 않는다.** 현장 실사 필요 |
| **D-27** | 차단 | **네트워크 미기재.** 현행 유선망 회선 속도·구성도·스위치 사양, 설비망/사무망 분리 여부, 무선AP 3대·현황판 2대 설치 위치 | 범위 밖. 최종 보고에 명시 |

---

## 회전 1에서 추가 (2026-09-11 · Phase 0 스키마 생성 중 실측)

| ID | 상태 | 내용 | 근거·처리 |
|---|---|---|---|
| **D-31** | 확정 | **임베딩 벡터 차원 = 1536.** TD5 `AGT_VECTOR_DOCS.EMBEDDING` 이 `VECTOR(1536)` 으로 명시돼 있다 | D-08 의 "벡터 차원 미정" 은 **해소**. 임베딩 모델명·인덱스 종류는 여전히 미확정 |
| **D-32** | 차단 | **코드성 FK 29건이 물리 FK 로 성립하지 않는다.** `PRODUCT_GROUP` `PROCESS_CODE` `ITEM_CODE` `MATERIAL` `CUSTOMER_CODE` `EQUIP_CODE` 등 29개 컬럼이 `VARCHAR(30/50)` 인데 비고는 `FK: BAS_COMMON_CODES` 를 가리킨다. 그 표의 PK 는 `CODE_ID BIGSERIAL` 이다 | 실제 의도는 **`BAS_COMMON_CODES(CODE_GROUP, CODE_VALUE)` 참조**다(`CODE_VALUE` 비고 = "그룹 내 Unique"). **물리 FK 제약을 만들지 않고** 애플리케이션 검증으로 처리한다. 정규화(코드 ID 참조로 통일)를 도입기업에 제안 |
| **D-33** | 차단 | **`EST_QUOTATION_ITEMS.UNIT_PRICE`(`NUMERIC(16,2)`)가 FK='Y' 로 `EST_COST_RATES` 참조로 표기됐다.** 금액 값 컬럼을 외래키로 적은 TD5 결함 | FK 제약 미생성. 참조가 필요하면 **`RATE_ID BIGINT` 컬럼 신설**이 맞다. 단 **컬럼을 추가하지 않는다**(G-02 762 고정) — 결함으로 등재하고 제안 |
| **D-34** | 확정 | **범위 유니크 2건.** `BAS_COMMON_CODES.CODE_VALUE`(비고 "그룹 내 Unique") · `SYS_CONFIGS.CONFIG_KEY`(비고 "구분 내 Unique")는 단일 컬럼 유니크가 아니다 | 복합 제약으로 생성 — `UNIQUE(CODE_GROUP, CODE_VALUE)` · `UNIQUE(CONFIG_TYPE, CONFIG_KEY)`. 범위 컬럼은 `tools/gen_schema.py` `SCOPED_UNIQUE` 에 **명시**한다(추측 금지) |
| **D-35** | 확정 | **KPI 코드 확정.** TD5 `KPI_TARGETS.KPI_CODE` 비고가 `LEADTIME_MFG`, `LEADTIME_O2D` 를 명시 | 제조 리드타임 = `LEADTIME_MFG` · 수주출하 리드타임 = `LEADTIME_O2D`. `app/kpi.py` 와 시드가 이 코드를 쓴다(D-21) |
| **D-36** | 확정 | **TD5 는 내부 정합하다.** FK 128건 전부 실제 68표로 해석 · PK 는 표당 정확히 1개 · 컬럼 행 길이 7 위반 0건 · 타입 어휘 전부 유효한 PostgreSQL 타입 | 생성기가 손 수정 없이 68표 762컬럼 98 FK 를 뽑았다. 광성 사업과 달리 스키마 단계 결함이 적다 |

## 회전 2에서 추가 (2026-09-11 · 웨이브 A)

| ID | 상태 | 내용 | 근거·처리 |
|---|---|---|---|
| **D-37** | 확정 | **타 사업 용어 판정 규칙.** 정본이 "설비 예지보전 AI는 본 사업 범위 밖이다" · "포밍·용접 설비는 제외한다" 처럼 **배제를 명시**한 문장은 오염이 아니라 가드레일 진술이다. 오염 판정은 용어 **주변 160자** 안에 근거(D-01·D-02 · 원 산출물명 · 범위 밖 · 미적용 · 제외 · 포함하지 않 · 수집원이 없 · 만들지 않는다)가 있는지로 한다 | goal.md §6 "내용으로 판단". 본문 어딘가에 근거가 있으면 봐주는 방식은 **검사를 통째로 무력화**한다(§10-16). `tools/check_routes.py` `QUOTE_OK`·`CONTEXT` |
| **D-38** | 확정 | **업무영역과 구현 파일이 갈리는 화면 3건.** 009 입고 AI Agent · 020 출하 AI Agent → `routers/agt.py` · 037 AI학습 데이터관리 → `routers/est.py` (전부 개발3) | **URL·메뉴 위치는 산출물(SF-TD3) 그대로** 두고 구현 파일만 옮겨 "한 파일은 한 사람만" 을 지킨다. `nav.OWNER_OVERRIDE` · `contracts/screen-map.md` §1 ⚠ 행 |
| **D-39** | 확정 | **역할 라벨에서 실명을 제거한다.** TD3 `role_matrix` 가 "총괄PM/경영자 (제미애 대표)" 처럼 실명을 포함한다 | UI·시드에는 직무명만 쓴다(G-29 개인정보). 산출물 원문은 `rbac.Role.source_label` 에 보관 |
| **D-40** | 가설 | **인증 전까지 역할 전환 수단.** 인증은 개발1(웨이브 B) 몫이라, 그때까지 `?as=ROLE` 쿼리 또는 `X-Kyungdong-Role` 헤더로 역할을 바꿔 본다. 기본값 `SYSADMIN` | **개발 편의이며 prod 에서는 인증이 이 자리를 대체한다.** 개발1 이 인증을 넣을 때 이 미들웨어를 제거한다 — 남아 있으면 보안 결함 |

## 회전 3에서 추가 (2026-09-11 · 웨이브 A 계약)

### D-20 — Agent 시안 도구 11종 ↔ TD5 68표 재배선 (확정)

`../08 AI Agent Making Agent/work-kyungdong-cockpit/kyungdong-globaltech-ai-agent/` 의 SQLite 12표를
버리고 **도구 시그니처와 프롬프트 규칙만 이식**한다(goal.md §1.2). 아래가 재배선 정본이다.

| 시안 도구 | TD5 테이블 | 비고 |
|---|---|---|
| `search_knowledge` | `AGT_VECTOR_DOCS` | 폐쇄형. 임베딩 미구성이면 `tsvector_keyword` 라벨(D-08) |
| `get_rules` | `SYS_CONFIGS`(ALERT_CONDITION) + `BAS_QUALITY_STANDARDS` | **자동 실행 엔진이 아니다** — 조건·담당자·조치·근거만 조회 |
| `get_db_records` | 허용목록 내 68표 | 읽기 전용. 쓰기 동사 금지 |
| `get_project_summary` | `EST_PROJECTS` + `EST_CAD_DRAWINGS`(리비전) + `PRC_WORK_ORDERS` + `PRC_PERFORMANCES` | 시안 `projects`+`designs`+`operations` |
| `get_material_status` | `INV_MATERIAL_LOTS` + `INV_STOCKS` + `INV_RECEIPTS`(MTC_NO) | 시안 `materials` |
| `get_incoming_inspection_issues` | `INV_RECEIPTS` + `INV_SUPPLIER_QUALITY` | 시안 `incoming_inspections` |
| `get_quote_analysis` | `EST_QUOTATIONS` + `EST_QUOTATION_ITEMS` + `EST_ML_PREDICTIONS` + `EST_SHAP_FACTORS` | 시안 `quote_predictions`. **데모 상수 예측값은 버린다** |
| `get_procurement_risks` | `INV_SUPPLIERS`(SUPPLY_TYPE) + `INV_SUPPLIER_QUALITY`(OTD_RATE) + `EST_MATERIAL_REQS`(PO_NEED_YN) | 시안 `purchase_orders` — **발주 테이블이 없다(D-41)** |
| `get_fat_quality` | `SHP_INSPECTIONS`(수압·기밀·진공) + `BAS_QUALITY_STANDARDS` | 시안 `fat_tests`. 프롬프트에서 FAT 는 **"출하 전 성능검사"** 로 풀어 쓴다 |
| `get_lead_time_status` | `KPI_MEASURES` + `KPI_TARGETS` + `PRC_PERFORMANCES` + `EST_PROJECTS`(DUE_DT) + `SHP_SHIPMENTS` | `LEADTIME_MFG` · `LEADTIME_O2D`(D-35). **산식은 `app/kpi.py` 한 곳** |
| `get_claim_trace` | `SHP_CLAIMS` + `SHP_CLAIM_CAUSES` + `SHP_LOT_TRACES` + `SHP_INSPECTIONS` | 시안 `claims` |
| (시안 `equipment_readings`) | `PRC_EQUIP_SIGNALS` + `DAT_TIMESERIES` | 전용 도구 없음 — `get_db_records` 로 본다. **수집 지점 2개소뿐**(D-06) |

**이식하는 규칙**: strict JSON schema · 허용목록 밖 도구 이름 거부 · 쓰기 동사 금지 ·
프롬프트 7~10번(검색하지 않은 문서명·조항·페이지를 지어내지 않는다) · 5줄 형식 · `근거: …` 마지막 줄 ·
승인 필요 명시(견적 확정·발주·대체자재·검사 합격·출하).
**버리는 것**: SQLite 스키마 · `demo_data: true` · 데모 예측 상수 · OpenAI 고정 호출(공급자 추상화로 감싼다) ·
Streamlit UI(FastAPI+Jinja2 로 대체, D-14) · `voice.py`(범위 밖).

### 그 밖

| ID | 상태 | 내용 | 처리 |
|---|---|---|---|
| **D-41** | 차단 | **외주 발주 관리 항목 부재.** TD1 `process_steps` 는 소재가공(외주)·버핑(외주) 데이터로 **"외주 발주번호, 자재 LOT, 반출·반입일, 외주처, 수량"** 을 적었으나, TD5 에 **외주 발주번호·외주처 컬럼이 없다.** 구매 발주(PO) 테이블도 없고 `EST_MATERIAL_REQS.PO_NEED_YN`(발주 필요 여부)뿐이다 | **컬럼을 추가하지 않는다**(G-02 762 고정). 있는 것으로 최대한 소화한다 — 반출·반입 이력은 `PRC_PROCESS_HISTORIES.OUTSOURCE_STEP` 과 `INV_MATERIAL_HISTORY.HIST_TYPE`(반출/반입), 외주처는 `INV_SUPPLIERS`(`SUPPLY_TYPE`='외주'), 외주 발주번호는 `PRC_WORK_ORDERS.WORK_ORDER_NO` 로 대체한다. 화면에 `대체 표기 (D-41)` 을 붙이고 **정규 컬럼 신설을 도입기업에 제안** |
| **D-42** | 확정 | **`tools/check_trace.py` 는 QA1 이 만든다.** 아키텍트가 대신 만들지 않는다(§3.4 한 파일은 한 사람만) | 추적 1:1 자체는 `tests/test_arch_smoke.py::test_경로와_추적ID가_중복없이_1대1이다` 가 이미 단언한다. **게이트 G-04·G-05 는 QA1 이 올 때까지 `미구현`** 으로 남는다 — `미구현`은 PASS 가 아니다 |
| **D-43** | 확정 | **계약 3종은 생성 파일이다.** `screen-map.md`(← `nav.py`) · `db-schema.md`(← TD5) · `api-contract.md`(← `nav.py`+TD4). `interfaces.md` 만 손으로 쓴다 | 손으로 고치면 어긋난다. `make contracts` 로 다시 뽑는다 |

## 회전 4에서 추가 (2026-09-11 · 공용 모듈·시드)

| ID | 상태 | 내용 | 처리 |
|---|---|---|---|
| **D-44** | 차단 | **`SYS_USERS.ROLE_ID` FK 설계 결함.** TD5 는 `ROLE_ID BIGINT FK: SYS_ROLE_PERMISSIONS` 로 적었으나 그 표는 **역할 × 업무영역(× 화면) 조합마다 한 행**이다. 사용자가 "역할" 이 아니라 **권한 행 하나**를 가리키게 된다 | **컬럼을 추가하지 않는다**(G-02 762 고정). 시드는 `ROLE_ID` 에 **그 역할의 대표 행(최소 `ROLE_PERM_ID`)** 을 넣고, 권한 판정은 `ROLE_CODE` 로 조인해서 한다. `SYS_ROLE_PERMISSIONS` 에 `ROLE_CODE` 만 있는 역할 마스터 분리를 도입기업에 제안 |
| **D-45** | 확정 | **물리 유니크 `SYS_ROLE_PERMISSIONS UNIQUE NULLS NOT DISTINCT (ROLE_CODE, AREA_CODE, SCREEN_ID)` 를 건다.** 근거: TD5 criteria "물리 인덱스·파티션·실제 제약명은 **구현 단계에서 확정**한다" | 시드를 **upsert** 로 만들기 위해 필요하다 — `delete` 는 D-44 의 FK 가 막는다(실제로 터졌다). `SCREEN_ID` NULL 행도 중복을 막아야 해 `NULLS NOT DISTINCT`(PostgreSQL 15+). **컬럼은 추가하지 않았다** |
| **D-46** | 확정 | **`util/anchor.py` → `util/clock.py` 로 이름을 바꿨다.** 모듈명 `anchor` 와 함수명 `anchor()` 가 겹쳐 `from util import anchor` 가 함수로 해석됐다(실제로 터졌다) | 개발 3명이 똑같이 걸릴 함정이라 미리 없앴다. 함수명은 `anchor()` 그대로 |
| **D-47** | 확정 | **정본에 값이 없는 코드 그룹 7종은 비워 둔다** — 품목 · 재질 · 고객사 · 보관위치 · 불량유형 · 클레임유형 · 원가대상 | 화면 입력 마스터다. **시드에 지어내지 않는다**(§0.2). 화면은 `미확정 (D-26)` 을 렌더한다. 테스트가 "비어 있음" 을 단언한다 |

## 회전 5에서 추가 (2026-09-11 · 보안 기반)

| ID | 상태 | 내용 | 처리 |
|---|---|---|---|
| **D-48** | 가설 | **세션·속도제한 저장소가 프로세스 메모리다.** TD5 68표에 세션 테이블이 없으므로 **테이블을 만들지 않는다**(G-01 68 고정) | 단일 프로세스 개발 환경 기준. 다중 워커·다중 인스턴스로 가면 공유 저장소(예: Redis — 사업계획서 4.7 ElastiCache 에 있다)가 필요하다. **재기동하면 세션이 끊긴다**는 사실을 운영 문서에 적는다 |
| **D-49** | 가설 | **로그인 속도 제한 = 계정 5회 / IP 20회 (10분 창).** 사업계획서 9.2 는 "비밀번호 복잡도·주기적 변경" 까지만 적고 **잠금 횟수·창 길이가 없다** | `.env` `LOGIN_FAIL_MAX`·`RATE_LIMIT_IP` 가설값. 직전 사업(§10-12) 교훈에서 온 수치이므로 화면에 `가설 (D-16)` 을 밝힌다 |
| **D-50** | 확정 | **CSP `default-src 'self'` — 외부 CDN 0.** 차트도 인라인 CSS/SVG 로 그린다. `style-src` 에만 `'unsafe-inline'` 을 둔다(Jinja 인라인 style 속성) | 렌더 결과에 `http://`·`https://` 가 0건인지 테스트가 단언한다. 폰트·아이콘을 외부에서 불러오면 실패한다 |
| **D-51** | 확정 | **prod 에서는 개발용 역할 전환(`?as=`·헤더)을 받지 않는다**(D-40 보강). 세션이 없으면 역할이 빈 값이 되어 **403** 이 난다 | 테스트가 `prod` 에서 `/inv/005?as=SYSADMIN` → **403** 을 단언한다. 개발1 이 인증을 넣으면 미인증은 401 이 되고 이 분기가 사라진다 |

## 회전 6 — 개발1 완료 후 (2026-09-11)

| ID | 상태 | 내용 | 처리 |
|---|---|---|---|
| **D-52** | 확정 | **CSRF 미구현을 회전 5에서 "했다" 고 적었다 — 오기재였다.** `csrf_enforce` 설정 플래그만 만들고 **토큰 발급·검증 코드가 없었다.** 개발1 이 잡아냈다(D-105). 설정이 켜져 있는데 검증이 없으면 **켜져 있다고 착각하게 만드는** 최악의 형태다 | `util/csrf.py` 를 만들었다 — HMAC 토큰, **세션(없으면 CSRF 쿠키)에 묶인다**, 위반 시 **403**. `contracts/interfaces.md` 에 시그니처 공표. **강제 적용은 개발 3명이 끝난 조용한 창에서** 미들웨어로 켠다(돌고 있는 동안 켜면 진행 중 POST 가 깨진다) |
| **D-53** | 확정 | **`util/security.py` 주석의 회사명이 G-03-③ 를 매 회전 FAIL 시켰다**(개발1 D-104). 내 파일이고 내 검사기가 잡은 내 오염이다 | **면제를 추가하지 않고 회사명을 뺐다.** 교훈에 회사명은 필요 없다. 면제 목록을 늘리면 검사가 헐거워진다(§10-16). 수정 후 소스 전체 근거 없는 용어 **0건** 실측 |
| **D-54** | 미결 | **`util/__init__` 이 `audit`·`mask` 를 함수로 재노출해 모듈을 가린다**(개발1 D-106). D-46(`anchor`→`clock`)과 **같은 함정이 두 군데 더** 있었다 | 개발2·3 이 돌고 있어 지금 바꾸면 진행 중 import 가 깨진다. **조용한 창에서 정리한다.** `csrf` 는 처음부터 모듈로 노출했다 |
| **D-55** | 차단 | **공급처 평가 4지표(품질편차·납기준수율·종합점수·등급)의 원천 컬럼·산식이 정본에 없다**(개발1 D-107) | 산출하지 않고 `미확정` 렌더. 불량률만 실측한다. **도입기업에 산식 요청** |
| **D-56** | ~~차단~~ → **해소 (D-181 실측)** | **품목·재질·보관위치 코드와 공급처 목록이 없어 유효 자재 LOT 을 만들 수 없다**(개발1 D-101). `INV_MATERIAL_LOTS.ITEM_CODE` 는 NOT NULL 코드성 FK 인데 그룹 `품목`·`재질` 은 D-47 로 비어 있어야 한다 | **입고·재고·공급처 시드 0건이 정답**이라는 개발1 판정을 받아들인다 — 정본도 같은 말을 한다(TD3-005 "초기 이관 범위를 착수 시 확정"). 유입 경로 3단(코드 등록 → 입고/Excel 적재 → 검증·집계)은 만들어져 있다. **도입기업에 필요: 품목·재질·보관위치 코드 목록, 공급처 목록** · **해소 실측(D-181): 품목 179 · 재질 8 · 자재LOT 40 행** |

## 회전 6 — 개발2 완료 후 (2026-09-11)

| ID | 상태 | 내용 | 처리 |
|---|---|---|---|
| **D-57** | ~~미결~~ → **해소 (D-181 실측)** | **`/board` 가 두 곳에 선언돼 있다**(개발2 D-205). `main.py:99 @app.get("/board")` 와 `routers/kpi.py:232 @router.get("/board")`. 라우터가 먼저 등록돼 이기지만 **한 경로에 핸들러가 둘인 것은 함정**이다 | 현황판 내용은 개발2 소유가 맞다 → **조용한 창에서 `main.py` 의 `board_screen` 을 지운다.** `app/templates/board.html` 도 개발2 소유로 넘긴다(`contracts/screen-map.md` §4 갱신) · **해소 실측(D-181): /board 선언 — main.py 0 · kpi.py 1** |
| **D-58** | 가설 | **KPI 달성률 산식이 정본에 없다**(개발2 D-202). 사업계획서 1.5 는 기존값·목표값·가중치만 적었다 | 개발2 가 정한 산식을 화면에 **함께 띄운다**. `가설 (D-202)` 배지. 도입기업 확인 대상 |
| **D-59** | 차단 | **표준 작업조건·클레임 초기 이력의 값이 정본에 없다**(개발2 D-203·D-204) | 시드하지 않는다. 화면 입력 마스터 |

## 회전 7 — 조용한 창 검증 (2026-09-11 · 오케스트레이터)

| ID | 상태 | 내용 | 처리 |
|---|---|---|---|
| **D-60** | ~~**차단(Critical)**~~ → **해소 (D-181 실측)** | **CSRF 가 POST 라우트 27개 중 4곳에만 연결돼 있다.** `util/csrf.py` 는 회전 6 에 만들었지만 개발 3명이 이미 폼·핸들러를 다 쓴 뒤였다 | **`CSRF_ENFORCE` 기본값을 0 으로 내리고 화면 상단에 `CSRF 미적용 (D-60)` 배지를 띄운다.** 지금 1 로 켜면 쓰기 23곳이 403 으로 죽는다. **조용히 꺼두지 않는다**(G-30) — 다음 개발 회전에서 폼 `_csrf` + 핸들러 `csrf.require()` 를 넣고 1 로 올린다 · **해소 실측(D-181): POST 31 · csrf.require 31** |
| **D-61** | 확정 | **내가 계약에 직전 사업 컬럼명을 옮겨 적었다.** `contracts/api-contract.md` §5·§6 의 `ADOPT_YN`·`ADOPT_BY` 는 **TD5 에 없다** — 광성정밀 사업의 컬럼이다. 실제는 `AGT_RECOMMENDATIONS.REVIEW_STATUS`·`REVIEWER_ID`, `EST_OBJECT_REVIEWS.REVIEW_RESULT`·`REVIEWER_ID` (개발3 D-302 가 잡음) | 계약 생성기를 고쳐 재생성했다. **§6 "타 사업 오염" 은 도메인 용어만이 아니라 컬럼명에도 적용된다** — 내가 경계하라고 쓴 것을 내가 어겼다 |
| **D-62** | 미결 | **CAD 테스트 3건이 데이터가 없으면 `skip` 한다**(`test_dev3_cad.py:93·112·120`). §10-4 는 "0건 경로와 N건 경로를 **둘 다 단언**" 을 요구한다. 깨끗한 DB 에서는 그 경로가 **게이트에서 영원히 검증되지 않는다** | 개발3 다음 회전 몫. `make cad-ingest` 후에는 546 passed 로 전부 돈다(실측). **0건 경로를 명시 단언**으로 바꾼다 |
| **D-63** | 확정 | **`make db-seed` 가 공통 시드만 돌렸다** — goal.md §9 는 `seed_dev1/2/3` 까지 부르게 돼 있다. 내 Makefile 결함이라 §4.4-3 종료 판정이 반쪽이었다 | 수정했다. `db-seed` 가 네 시드를 순서대로 돌린다 |
| **D-64** | 확정 | **`util/audit.py` 를 `auditlog.py` 로 바꿨다가 되돌렸다.** D-54(모듈 가림) 를 고치려다 개발자 호출 **5곳**(`from ..util.audit import audit`)을 깼다 | **되돌렸다.** 개발자들이 서브모듈 경로를 직접 써서 이미 모호함을 피하고 있었다 — 함정보다 내 수정이 해로웠다. D-54 는 **고치지 않는다**로 종결 |

## 회전 9 — QA2 완료 (2026-09-11)

| ID | 상태 | 내용 | 처리 |
|---|---|---|---|
| **D-65** | 확정 | **`gate.py` 가 `check_trace.py` 를 실행하지 않고 G-04·G-05 를 FAIL 로 고정했다**(QA2 DEF-QA2-009). 파일 존재만 보고 `NOIMPL if not trace else FAIL`. 안내 문구는 여전히 "미작성" 이라고 말한다 | 내 파일이다. **QA1 이 끝난 뒤 고친다** — 돌고 있는 중 바꾸면 QA1 이 보는 출력이 달라진다 |
| **D-66** | 확정 | **`gate.py` 가 G-07~G-13 을 종료코드 하나로 뭉갠다**(QA2 DEF-QA2-008). §10-6 위반. `check_routes` 에는 게이트별 줄 파싱을 넣어 놓고 `CHECKERS` 목록에는 안 넣었다 | 같이 고친다. **게이트별 판정 줄을 파싱**한다 |
| **D-67** | 확정 | **`contracts/missing-policy.md` 가 없었다**(QA2 DEF-QA2-007). goal.md G-10 이 가리키는 문서인데 내가 만들지 않았다 | **만들었다.** 수집 지점 2개소 · 분모·분자 · **라이브/재전송 구간별 순서 판정**(QA2 실측 교훈) · **'정상 데이터' 조작적 정의**(정본에 없다 — D-68) · **분모 0 은 판정 불가** · 동시 변경 2신호 |
| **D-68** | 차단 | **사업계획서 2.7.4 가 '정확도 = 정상 데이터 수 ÷ 전체' 라고만 적고 '정상' 을 정의하지 않았다**(QA2 DEF-QA2-012) | 지어내지 않고 **조작적 정의를 `missing-policy.md` §4 에 명시**했다(코드성 FK 29건 유효 + NOT NULL + 물리 FK). **수치 범위 표가 없어 그 항목은 판정에서 제외**하고 그 사실을 적는다. 도입기업 확인 대상 |
| **D-69** | ~~차단(Critical)~~ → **해소 (D-181 실측)** | **'수집 중단' 배지가 운영 시각에서 구조적으로 뜰 수 없다**(QA2 DEF-QA2-001). `collection_badges()` 가 `anchor() - last_dt` 로 판정해 실시각 수집이면 항상 음수(실측 **−108,426초**) | 담당 **개발2**. 배지 경로 자체는 살아 있다(앵커 −2h 면 뜬다) · **해소 실측(D-181): 판정 함수 stale_verdict 단일화 True · dsh 가 자체 뺄셈 False** |
| **D-70** | ~~차단~~ → **해소 (D-181 실측)** | **중단 판정이 두 곳에 복제됐다**(QA2 DEF-QA2-002). `collector.status()`(실시각) vs `collection_badges()`(앵커) — 같은 DB·같은 시각에 **반대 결론**. §10-16 이 경고한 유형 | 담당 개발2·개발3. **판정 정본 함수 하나로 합친다.** 덤으로 `progress-dev3.md` 2번 서술이 실측과 정반대임이 드러났다 · **해소 실측(D-181): 판정 함수 stale_verdict 단일화 True · dsh 가 자체 뺄셈 False** |
| **D-71** | ~~차단~~ → **해소 (D-181 실측)** | **G-13 전처리가 어휘만 있고 구현이 없다**(QA2 DEF-QA2-003~006) — 결측 **보정** 0건 · 이상치 판정/제거 0건(`--anomaly` 로 40A 초과가 그대로 적재) · Feature **6종 중 1종**만 산출 가능(`형상 복잡도` 는 TD5 어휘에도 없다) · 처리 건수가 `DAT_JOB_LOGS` 에 안 남는다 | 담당 **개발3**. **G-13 FAIL 은 정당하다** · **해소 실측(D-181): 결측 보정 True · 이상치 표시 True · 처리건수 기록 True · Feature 차단 5종은 D-05·D-90 으로 남는다** |
| **D-72** | 확정 | **§7.1 판정 불일치 1건** — `PRC_PROCESS_HISTORIES` 를 개발2 가 "시드 대상" 으로 판정했으나 **실제 0건**(QA2 DEF-QA2-011). 나머지 5건은 일치 | 담당 개발2. 판정을 실제와 맞춘다 |

## 회전 9 — QA1 완료 (2026-09-11)

| ID | 상태 | 내용 | 처리 |
|---|---|---|---|
| **D-73** | 확정 | **내 CSRF 보고가 틀렸다.** 회전 7 에 "POST 27개 중 4곳 연결" 이라고 적었으나 **실제는 0곳**이다(QA1 DEF-QA1-002). 내가 센 "4" 는 **내가 쓴 `util/csrf.py` 안의 문서·상수 줄**이었다 — 호출 위치가 아니다 | 재실측: `csrf.require()` 호출 **0** · 템플릿 `_csrf` **0** · POST 라우트 **27**. D-60 을 정정한다. **`CSRF_ENFORCE=1` 을 먼저 켜면 쓰기 27개가 전부 403** 이다 — 폼·핸들러를 먼저 고친다 |
| **D-74** | 확정 | **`gate.py` 의 `G-04 FAIL` 은 거짓이었다**(QA1 DEF-QA1-005 = D-65). 검사기를 실행하지 않고 FAIL 을 찍었다. **실제 G-04 는 PASS** — AD2 45 ↔ TD3 45 ↔ TD4 45 · 고아 0 · 중복 0 | **고쳤다.** `gate.py` 가 검사기를 실행하고 **게이트별 판정 줄을 파싱**한다(D-66 도 함께). 이제 게이트 30개가 개별로 찍힌다 |
| **D-75** | ~~차단~~ → **해소 (D-181 실측)** | **G-05 FAIL** — `/dat/033`(데이터통합관리)이 인터페이스 연계 상태 **0/4** 를 보여 준다(`MES-TD4-046~049`). goal.md G-05 는 "연계 상태는 033 에서 조회" 로 정했다 | 담당 **개발1**. QA1 실측: **`/sys/029` 에 `DAT_SOURCES.INTERFACE_CODE` 로 4/4 가 이미 있다** — 열 하나를 033 으로 옮기면 G-05 가 바로 PASS 다 · **해소 실측(D-181): /dat/033 렌더 200 · 인터페이스 표기 4/4** |
| **D-76** | ~~차단~~ → **해소 (D-181 실측)** | **프로젝트번호 링크가 항상 0건을 연다**(QA1 DEF-QA1-004). `est.py` 가 프로젝트번호를 `/prc/024?lot=` 로 건다 — `?project=` 여야 한다 | 담당 **개발3**. G-08 디지털 스레드 클릭 규약 위반 · **해소 실측(D-181): est.py 가 프로젝트번호를 ?lot= 로 거는 곳 0** |
| **D-77** | ~~차단~~ → **해소 (D-181 실측)** | **`/api/ingest/plc` 본문 파싱 실패가 500 이다**(QA1 DEF-QA1-006). 오류 계약상 **422** 여야 한다 | 담당 **개발3** · **해소 실측(D-181): 본문 파싱 실패 → validation(422) 처리 True** |
| **D-78** | 확정 | **`Form(...)` 라우트는 폼 검증이 권한 검사보다 먼저 돈다**(QA1 DEF-QA1-008). FastAPI 의존성이 핸들러 본문보다 앞서므로, **권한 없는 역할이 403 이 아니라 422** 를 받는다 | **오류 계약에 순서를 명문화한다** — 권한 검사가 폼 검증보다 **앞서야** 한다. 라우트는 폼을 `Request` 로 직접 읽거나 권한 의존성을 앞에 두는 방식으로 고친다. 아키텍트가 `contracts/api-contract.md` §4 에 규약을 적고 개발 3명이 적용 |
| **D-79** | 확정 | **prod 미인증이 401 이 아니라 403 이다**(QA1 DEF-QA1-007). 세션이 없으면 역할이 빈 값이 되어 권한 검사에서 403 이 난다 | 인증(개발1)이 들어오면 **401 이 맞다**. 지금은 인증이 없어 구조적으로 403 이다. 개발1 의 `app/auth.py` 회전에서 해소 |
| **D-80** | 확정 | **계약 경로와 코드 경로 불일치**(QA1 DEF-QA1-003). 계약 §5 `/api/agent/recommend/{id}/review` ↔ 코드 `/adopt` | **코드가 맞다**(§4.1). 계약을 `/adopt` 로 되돌린다. 단 **컬럼명은 `REVIEW_STATUS`·`REVIEWER_ID` 가 맞다**(D-61) — 경로 이름과 컬럼명은 별개다 |
| **D-81** | ~~차단~~ → **해소 (D-181 실측)** | **`test_dev1_flow.py:352` 가 `ORDER BY` 없이 `[0]` 을 단언한다**(QA1 DEF-QA1-009). D-62 와 같은 계열 — 행 순서가 보장되지 않는다 | 담당 **개발1** · **해소 실측(D-181): ORDER BY 없는 [0] 단언 False** |
| **D-82** | 판정 불가 | **터치 버튼 크기 기준이 정본에 없다.** TD3 standard_note 3 은 "큰 버튼" 이라고만 적고 px 를 정하지 않았다 | QA1 이 **남의 기준(44px)을 끌어와 FAIL 을 만들지 않았다.** 실측만 기록: 개발2 `min-height:36px` · 개발1 약 38px · **좁은 폭 미디어쿼리에 버튼 규칙이 아예 없다.** 도입기업 확인 대상 |
| **D-83** | 판정 불가 | **폰 폭 400px 실제 렌더를 못 쟀다** — 브라우저가 없다 | CSS 정적 판정 5개 조건은 전부 통과(미디어쿼리 1곳 · 메뉴 nowrap/overflow-x 없음 · 최장 항목 256px · 표 45화면 전부 `.tablewrap` · 400px 초과 고정폭 0). **픽셀 확인은 못 했다고 적는다** |

**QA1 이 확인해 준 것**: D-57 경로 중복 **0건**(`/board` 는 `kpi.py` 단독) · D-61 계약에만 있는 컬럼 **0건**(`contracts/*.md` 의 `TABLE.COLUMN` 전수 대조).

## 회전 10 — 아키텍트 대기 작업 · QA2 재개분 (2026-09-11)

| ID | 상태 | 내용 |
|---|---|---|
| **D-78** | **해소** | **권한 가드를 `main.py` 미들웨어로 만들었다.** 폼 파싱보다 먼저 돈다. 실측: `OPERATOR POST /est/012/confirm` **422 → 403**, 권한 있는 `SYSADMIN` 은 그대로 422(폼 누락). 계약 §4.0 검사 순서 준수 |
| **D-84** | 확정 | **가드를 처음에 너무 거칠게 만들어 QA1 테스트 4건을 깼다.** POST 에 일괄 `can_write` 를 요구했는데 **총괄PM/경영자(EXEC)는 견적AI·출하가 `조회/승인`** — **등록·수정 권한 없이 승인만** 가능하다. 승인해야 할 역할을 가드가 막았다 → **가드는 "권한이 아예 없는 경우" 만 막고**(`can_write or can_approve`) 세부 판정은 핸들러가 한다. 1145 passed 로 회귀 0 확인 |
| **D-85** | 확정 | **내 `missing-policy.md` §2.1 이 두 군데 틀렸다**(QA2 DEF-QA2-013·014). ① 태그 수를 `IF_PLC_SIGNALS`(수집 결과)에서 뽑게 적어 **순환**이었다 — 태그가 통째로 유실되면 분모도 같이 줄어 **결측률이 구조적으로 0 에 수렴**한다(실측: 0건 상태 `count(distinct TAG_NAME)`=0). ② 분자를 `PRC_EQUIP_SIGNALS + DAT_TIMESERIES` 로 적었으나 **행 단위가 다르다**(5주기 실측 5 / 30 / 40). → **분모는 정본 태그표(8종), 분자는 `IF_PLC_SIGNALS` distinct(태그,시각)** 로 정정. 현장POP 은 수동 입력이라 분모 제외 |
| **D-86** | 확정 | **결측률이 두 가지다**(QA2 DEF-QA2-015) — **미적재 비율**(이 규약의 정본)과 **값 결측 비율**(부가 실측). 섞어서 하나의 수치로 보고하지 않는다 |
| **D-87** | 확정 | **내가 결함 번호를 잘못 전달했다.** QA2 에게 "DEF-QA2-005/007 해소" 라고 보냈으나 **005 는 Feature 6종**(미해결, `DERIVABLE={홀 수량}` · `BLOCKED` 5종)이고 규약 문서 부재는 **007** 이다. QA2 가 번호 오기를 잡아 리포트 기준으로 맞췄다. 해소된 것은 **007 · 012 · 008 · 009** |
| **D-88** | 확정 | **내 계약 문구 수정이 QA1 테스트를 깼다.** `ADOPT_YN` 경고에서 리터럴 **"TD5 에 없다"** 를 빼고 "직전 사업 컬럼명을 옮겨 적은 계약 오류" 로만 적었다. QA1 테스트는 ±120자 안에 그 리터럴을 요구한다 — **테스트가 맞다**(내 새 문구는 "TD5 에 없다" 는 사실 자체를 잃었다). 복원했다 |

## 회전 11 — 웨이브 D 준비 (2026-09-11)

| ID | 상태 | 내용 |
|---|---|---|
| **D-89** | 확정 | **DEF-QA1-001(G-05 FAIL)은 작은 변경이다.** 코드 확인: `routers/sys.py:391` 이 `DAT_SOURCES` 에서 `INTERFACE_CODE` 를 이미 읽어 `/sys/029` 에 그린다. 같은 질의를 `/dat/033` 에 두면 **G-05 즉시 PASS**. 담당 개발1 |
| **D-90** | 차단 | **DEF-QA2-005(Feature 6종 중 1종)는 고칠 수 없다.** `cad/pipeline.py:38 BLOCKED_FEATURES` 가 5종 차단 사유를 **전부 D-05**(Parsing·OCR 미구성)로 적어 두었다 — 총 절단장(경로 길이 컬럼 없음) · 판재 면적(외곽 좌표 없음) · 용접장(용접선 유형이 TD5 어휘에 없음) · 재질·두께(표제란 OCR). **개발3 이 손댈 수 없다** |
| **D-91** | 확정 | **G-13 의 정직한 끝 상태는 `차단`이지 PASS 가 아니다.** 개발3 이 결측 보정·이상치·`DAT_JOB_LOGS` 기록을 고쳐도 Feature 6종이 D-05 로 막혀 있다. **`FAIL` → `차단`** 으로 바뀌는 것이 맞다. Feature 를 지어내 채우면 결함이다(§10-18) |
| **D-92** | 확정 | **CSRF 는 아키텍트가 패턴을 먼저 박고 개발 3명을 기동한다.** 27개 POST 에 걸쳐 있어 셋이 제각각 하면 어긋난다 — `render()` 가 `csrf_token` 주입 · `_macros.html` 폼 매크로 · 핸들러 첫 줄 `csrf.require()` 규약 공표 · **`CSRF_ENFORCE` 는 셋이 다 연결한 뒤에 1 로** (먼저 켜면 쓰기 27개가 403) |

| **D-93** | 확정 | **스톨 판정에 시간 축이 필요하다.** QA3 를 16:32 에 스톨로 의심했으나(트랜스크립트 90초 정지·파일 변경 0·**프로세스 0**) 16:48 재확인에서 **정상 작동**이었다(+574KB · 프로세스 3). 90초 정지는 몇 분짜리 도구 호출이었고, 프로세스 0 은 **도구 호출 사이**였다. → **세 신호를 한 순간에 읽지 않는다.** `600초 동안 지속적으로` 세 신호가 모두 0 일 때만 스톨이다. §10-9 의 "600초" 는 임계가 아니라 **관측 창의 길이**다. 순간 판독으로 죽였다면 `check_ai.py` 788줄 · `check_security.py` 750줄 · 정답셋 2종을 버렸을 것이다 |

## 회전 12 — QA3 완료 · 전 게이트 최초 측정 (2026-09-11)

| ID | 상태 | 내용 |
|---|---|---|
| **D-94** | ~~**차단(Critical)**~~ → **해소 (D-181 실측)** | **내가 회전 5에 "보안 기반(세션·속도제한·보안헤더)" 을 했다고 적은 것은 또 과대 보고였다.** 실측: `POST /login` **0** · 로그아웃 **0** · `ratelimit.*` 호출 **0** · `session.create` 호출 **0**. **모듈은 있고 부르는 경로가 없다.** 보안 헤더·CSP 만 실제로 살아 있다. **D-52(CSRF 설정만 있고 구현 없음)와 같은 계열을 내가 두 번째로 저질렀다.** 담당: 개발1 인증(`app/auth.py`)이 들어와야 세션·잠금이 연결된다 · **해소 실측(D-181): POST /login 1 · ratelimit 호출 6 · session.create 1** |
| **D-95** | 확정 | **전 게이트가 처음으로 측정됐다** — `PASS 14 · FAIL 7 · 차단 11 · 미구현 0 / 32`(빌드 1197 passed). 미구현 17 → 0. G-14~G-19 는 **`차단`이 맞았다** — QA3 가 합성 라벨·합성 예측을 만들지 않았다 |
| **D-96** | 확정 | **QA3 가 §10-16 되돌림 시험을 통과했다.** 설정을 바꾸면 수치가 실제로 움직인다: `RAG_CONFIDENCE_MIN` 0.0/0.70/0.99 → 근거 부족 **7/24/30** · `CAD_IOU_THRESHOLD` 0.2/0.5/0.9 → TP/FP/FN **(2,1,1)/(1,2,2)/(0,3,3)** · `ENV=prod` → 기동 거부·HSTS · `PG_DSN` 죽이면 200→**503**. 광성 사고(설정을 바꿔도 소수점까지 동일)가 재발하지 않았다. **임계 0.0 에서도 7건이 '근거 부족'** 인 것이 근거 0건 분기가 임계와 무관함(D-10)의 실증이다 |
| **D-97** | 확정 | **접속 감사 16화면 누락은 공용 가드 한 곳 문제다**(QA3). 개발1 `bas.guard` 는 안에서 `audit()` 을 부르고 개발2 `dsh.guard` 는 부르지 않는다. **16화면을 각각 고칠 일이 아니다** — 가드를 맞춘다. 담당 개발2 |
| **D-98** | ~~차단~~ → **해소 (D-181 실측)** | **라벨링 가이드의 클래스가 TD5 와 다르다**(QA3). 가이드는 `title_block·bom_table·rev_table` **3종**이고 "노즐·플랜지는 2차 고도화 별도 프로젝트" 로 적었는데, TD5 `EST_CAD_OBJECTS.OBJECT_TYPE` 은 **홀·슬롯·노즐·플랜지·치수문자 5종**이다. 모집단도 **835 vs 1,283** 으로 어긋난다. **QA3 가 어느 쪽이 맞는지 정하지 않았다 — 옳은 판단이다.** 도입기업 확인 대상 · **해소 실측(D-181): TD5 비고가 열린 목록 True · 도입기업 확정 어휘 ['title_block', 'bom_table', 'rev_table']** |
| **D-99** | 확정 | **QA3 가 임계값을 내리지 않았다.** `RAG_CONFIDENCE_MIN` 0.70 → 0.55 로만 내려도 G-21·G-22 가 좋아지지만 **QA 가 정할 값이 아니라며 DEF-QA3-008 로 넘겼다**(§10-18). TLS 버전·저장 암호화 알고리즘도 적지 않았다(D-15) |
| **D-100** | 확정 | **QA3 가 못 잰 것을 명시했다** — 백엔드를 죽여 본 **브라우저 실측은 하지 않았다**(검사기가 서버를 띄우지 않는다). 대신 클라이언트 폴백 표면이 0(js 0 · fetch 0)이라는 사실만 적었다 |

## 회전 14 — 웨이브 D 마무리 (2026-09-11)

| ID | 상태 | 내용 |
|---|---|---|
| **D-101** | 확정 | **내 CSRF 패턴에 숨은 결함이 2개 있었다.** ① `{% from "_macros.html" import csrf_field %}` 를 **`with context` 없이** 쓰게 해서 매크로가 `csrf_token` 을 못 봤다 — **토큰이 빈 문자열로 렌더**됐다. **그대로 `ENFORCE=1` 을 올렸다면 쓰기 전부가 403** 이다. ② 세션 없는 요청의 CSRF 쿠키를 **응답에서 처음 심어** 화면이 발급한 토큰이 다음 POST 에서 항상 무효였다. 둘 다 마무리 에이전트가 **켜기 전에 재서** 잡았다 — 순서가 옳았다 |
| **D-102** | ~~**차단(Critical)**~~ → **해소 (D-181 실측)** | **`CSRF_ENFORCE` 를 올리지 못한다.** 켜면 **85건 403**(끈 상태 14건). 이유 둘: ① 테스트 하네스가 토큰을 안 싣고 그중 **42건이 QA 소유 파일** ② **`/api/ingest/plc` 등 기계 클라이언트는 세션이 없어 토큰 발급 경로가 없다 — 켜면 PLC 수집이 죽는다.** **G-26 FAIL 의 사유가 이것이다** · **해소 실측(D-181): CSRF_ENFORCE=True** |
| **D-103** | 확정 | **기계 대 기계 엔드포인트에 CSRF 는 적용 대상이 아니다.** CSRF 는 **브라우저가 쿠키를 자동으로 실어 보내는 것**을 막는 장치다. `/api/ingest/plc` · `/api/cad/files` · `/api/docs/import` 는 PLC·Gateway·배치가 부르며 쿠키를 쓰지 않는다 → **면제하고 다른 수단(장비 등록 기반 인증)으로 보호한다.** 면제 목록과 근거를 `contracts/api-contract.md` 에 적고 그 뒤 `ENFORCE=1` 을 올린다. **"막을 수 없으니 끈다" 가 아니라 "이 위협에 해당하지 않는다" 는 판단이다** |
| **D-104-b** | 확정 | **감사 누락의 진짜 원인은 내 D-78 가드였다.** `main.py` 의 권한 가드가 라우터 `guard()` 보다 **먼저** 돌아 거부 시 감사에 도달하지 못했다. 16화면을 각각 고칠 일이 아니었다(QA3 D-97 지적이 맞았다). 가드에서 기록하도록 고쳐 **G-29 PASS** |
| **D-105-b** | 확정 | **개발1 테스트의 정규식이 낡아 깨졌다.** `발급 비밀번호 ([A-Za-z0-9_\-]+)` 가 복잡도 정책(D-16)으로 들어간 `+`·`!` 를 못 받아 **앞 1~2자만 잡혔고** 그 조각이 argon2 해시 안에서 우연히 발견돼 실패했다. **구현이 맞고 테스트가 낡았다** — 정규식만 `(\S+)` 로 고쳤다. 구현을 테스트에 맞추지 않았다 |
| **D-106-b** | 미결 | **G-12·G-13·G-25 FAIL 은 이제 QA 검사기 갱신 대기다.** G-12 는 `check_ingest.py ③b` 가 "실시각 수집 직후 배지 없음 = 결함" 으로 보는 낡은 가정(지목한 `anchor() - last_dt` 코드는 이제 grep 0건) · G-13 은 **결측 보정·이상치 처리가 생겼고** 남은 것은 Feature 5종 **D-05 차단**뿐 → **`차단`이 정직한 끝**(D-91) · G-25 는 롤백이 생겨 미충족 5/5 → **4/5** 인데 `check_ai.py` 가 FAIL 하드코딩. **담당 QA2·QA3** |

## 회전 15 — CSRF 면제 결정·하네스 헬퍼 (2026-09-11)

| ID | 상태 | 내용 |
|---|---|---|
| **D-103** | **확정·구현** | **기계 대 기계 엔드포인트 4건을 CSRF 에서 면제한다** — `/api/ingest/plc` · `/api/ingest/gateway/resend` · `/api/cad/files` · `/api/docs/import`. 근거: CSRF 는 **브라우저가 쿠키를 자동으로 실어 보내는 것**을 막는 장치인데 이들은 PLC·Gateway·배치가 부르고 **쿠키를 쓰지 않는다.** **"막을 수 없으니 끈다" 가 아니라 "적용 대상이 아니다"** 다. 면제를 개발자 파일에 흩뿌리지 않고 **`util/csrf.py EXEMPT_PATHS` 한 곳에 사유와 함께** 두었다 — 목록에 없으면 무조건 검사한다. **대신 장비 등록(`IF_DEVICE_REGISTRY`) 기반 인증으로 보호해야 한다 — 미구현이라 차단으로 남긴다** |
| **D-107** | 확정 | **`ENFORCE=1` 을 아직 켜지 않는다 — 순서 때문이다.** 켜면 **71건 실패**(면제로 85→71). **전부 테스트 하네스가 토큰을 안 싣는 문제이지 제품 결함이 아니다** — 제품은 토큰 없는 쓰기를 올바르게 **403** 으로 거부한다(실측). 지금 켜면 71건이 빨개져 **다른 게이트가 전부 측정 불가**가 된다(§10-15). **하네스를 먼저 고치고 그다음 켠다.** 이것은 게이트를 낮추는 게 아니라 **순서를 지키는 것**이다 |
| **D-108** | 확정 | **공용 하네스 헬퍼 `tests/conftest.py` 를 아키텍트가 제공한다.** `csrf_post(client, path, data, page=None)` — 화면에서 토큰을 꺼내 붙이고, **면제 경로는 그대로 보낸다.** 실측 확인: 헬퍼 POST **422**(폼 검증 단계) · 토큰 없이 **403** · 면제 경로 **422**. 소유자 5명이 각자 `post()` 를 이 헬퍼로 바꾸면 된다 |

## 회전 17 — CAD 원본 자료 실측 (2026-09-12 · 사용자가 위치 지정)

원본: `/Users/gerardo92/Desktop/26년 제조AI 스마트공장  프로젝트 /01 경동글로벌텍 제조AI 플랫폼 구축 프로젝트/01 경동Data`
**지금까지 나는 통계 xlsx 4종만 `docs/cad/` 에 복사해 썼다. 실제 도면·견적 파일을 열지 않았다.**

| ID | 상태 | 내용 |
|---|---|---|
| **D-109** | 확정 | **실측 파일 6,038건** — dwg 2,240 · cad 1,030 · **pdf 730** · jpg 728 · bak 565 · **xls 164** · **dxf 29** · docx 56. `20 경동도면함.zip`(1.6GB·1,595파일)·`RECEIVER TANK.zip` 은 **이미 폴더로 풀려 있다** |
| **D-110-b** | **확정 — D-05 판정 정정** | **DXF 29건은 Autodesk API 도 OCR 도 필요 없다.** 순수 텍스트다. 실측 엔티티(한 파일): **LINE 9,471 · ARC 2,564 · CIRCLE 1,443 · TEXT 437 · DIMENSION 216**. → **홀 수량**(CIRCLE) · **총 절단장**(LINE·ARC·POLYLINE 길이) · **판재 면적**(닫힌 POLYLINE) · **재질·두께**(표제란 TEXT — OCR 불필요)가 **실제로 산출 가능**하다. `cad/pipeline.BLOCKED_FEATURES` 가 5종 전부 "D-05 Parsing·OCR 미구성" 이라 적은 것은 **.dwg 에는 맞지만 .dxf 에는 틀리다** |
| **D-111** | **확정 — D-04 판정 정정** | **견적 자료가 실재한다.** `견적` 파일 **73건**(xls 31 · pdf 29 · docx 8). xls 31건 중 **29건에서 금액 표기가 읽힌다 · 읽기 실패 0**. 표본(`견적서-REACTOR 일성.xls`, 2001)에 **工事名 REACTOR · 納期 發注後 50日間 · 合計金額 · 품명/규격/수량/단가/금액 명세**가 있다 → **G-15 견적 Label · G-17 납기 Label 후보** |
| **D-112** | 확정 | **프로젝트(수주)번호 체계가 폴더명에 있다** — `GD` + YYMM + 일련번호 + 제품명/고객사. **GD 코드 폴더 99개**(2016 15 · 2017 12 · 2018 10 · 2019 16 · 2020 46). 예: `GD160801-진공건조기` · `GD160803-누체필터10L` · `GD2007-00 동광제약`. **제품명이 정본 제품군 5종과 일치한다**(반응기·진공건조기·누체필터·저장탱크) |
| **D-113** | 확정 | **도면과 견적이 같은 프로젝트 폴더에 있는 것이 14건** — 최대 `REACTOR/반응기`(도면 304 · 견적 32 · pdf 233). **이것이 ETO 디지털 스레드의 실제 조인 키다**(G-08 분모 후보) |
| **D-114** | 확정 | **고객사 실명이 폴더명에 있다** — 두루텍 · 한모루 · 동광제약 · 웰이엔씨 · 대한열기 · 대호테크 · 엔에프테크 · 비나텍 · 그린텍 등. **D-56(고객사 목록 부재)의 후보**다. 단 이것은 **파일시스템에서 파생한 것이지 도입기업이 준 마스터가 아니다** — 공식 코드 여부는 확인이 필요하다 |
| **D-115** | 확정 | **내 탐침이 또 조용히 틀렸다 (이번 세션 4·5번째).** ① macOS 한글 파일명이 **NFD** 라 `find -iname "*견적*"` 가 **0건**을 냈다(실제 73건) ② `pandas.astype(str)` TypeError 와 DXF 앞 4KB만 읽은 탓에 "못 읽는다" 로 결론낼 뻔했다(실제 29/31 읽힘, 엔티티 수만 건). **탐침 실패를 데이터 부재로 결론내지 않는다** |

### 이 발견이 바꾸는 것 — 그리고 바꾸지 않는 것

**바꾼다**: G-13 Feature 5종이 "구조적으로 불가능" 이 아니라 **"DXF 표본에서는 가능, 표본이 작다"** 로 바뀐다.
G-08·G-09 의 분모 0 도 **14개 프로젝트의 실제 도면↔견적 연결**로 세울 수 있다.

**바꾸지 않는다**: DXF 는 전체 도면 **3,299건 중 29건(0.9%)** 이다. dwg 2,240 · cad 1,030 은 여전히 바이너리라
변환기가 없으면 못 읽는다. **G-14 CAD 객체 인식 80% 를 29건으로 주장할 수 없다.**
견적 14건도 ML 학습 표본으로는 작다. **표본 수를 숨기지 않고 함께 보고한다.**

## 회전 18 — 실 CAD 연동 · **내 숫자 정정** (2026-09-12)

### ⚠ D-109~D-114 의 내 숫자가 부풀려져 있었다 — 에이전트가 중복을 걷어내 잡았다

| 내가 적은 값 | 실제 (중복 제거) | 차이의 원인 |
|---|---|---|
| 프로젝트 **99** (D-112) | **34**(에이전트, 전체 코드 기준) / **20**(내 재검, 접두 기준) | 65개가 `20 경동도면함/` 아래 **복사본**이다. 정의(접두 vs 일련번호 포함)에 따라 20~34 — **어느 쪽이든 99 는 아니다** |
| DXF **29건** (D-110-b) | **9건** | 내용 해시로 세면 20건이 복사본. **표본은 9 다** |
| 견적 xls **31 중 29 금액 읽힘** (D-111) | **26건**(서로 다른 것) 중 **총액 실제 추출 22 (84%)** | "금액 **표기**가 있다" 와 "총액을 **뽑았다**" 는 다른 사실이다. 내가 전자를 후자처럼 적었다 |
| 도면·견적 동거 **14 프로젝트** (D-113) | **21**(직접 포함) / **46**(하위까지) | 세는 방식이 둘이고 답이 다르다. 하나만 적으면 오해를 부른다 |

**교훈**: 파일 수를 표본 수로 쓰면 안 된다. **중복 제거 전 수치를 근거로 제시하지 않는다.**
이번 세션에서 내 측정이 조용히 틀린 것이 **여섯 번째**다(find NFD · CSRF 4/27 · routes 7 · pandas astype · DXF 4KB · 이번 중복).

### 새 결정

| ID | 상태 | 내용 |
|---|---|---|
| **D-116** | 확정 | **순수 파이썬 DXF 파서를 만들었다**(`cad/dxf.py` 383줄, 외부 라이브러리 0, 145MB 를 2.2초). 실측 산출: **총 절단장 9/9(100%)** · 홀 수량 7/9(77%, **지름 필터 없음 — 기준 부재**) · 판재 면적 5/9(55%) · 두께 5/9 · 재질 5/9(**값은 읽히나 적재 불가** — `EST_CAD_FEATURES.FEATURE_VALUE` 가 NUMERIC) · **용접장 0/9**(용접 레이어 표준 부재 — DXF 로도 못 가름) |
| **D-117** | 확정 | **`BLOCKED_FEATURES` 를 파일 형식별로 갈랐다** — **DXF 4종 산출 가능** / **DWG 2,240 · CAD 1,030 은 5종 전부 차단**(변환기 없음). 키 집합은 유지해 QA 검사기(`check_ingest ⑤`)를 깨지 않고 사유에 D-05·D-110-b 를 함께 적었다 |
| **D-118-b** | 확정 | **견적 파서를 만들었다**(`cad/quote.py`). 총액 **22/26** · 납기 **8/26**(40·45·50·70일) · 품목 **462행/19파일** · 갑지 단위 총액 **90건** — **파일 1건 ≠ 견적 1건**이다. 한 행에 금액 후보가 둘이면(1,759,443 vs 5,200,000) **고르지 않고 실패로 적었다** |
| **D-119** | 확정 | **한자 금액의 장식 단위 함정을 실측으로 잡았다** — `…六千七百二萬六千萬院整` 에서 장식 단위를 떼지 않으면 **67,026,000 이 127,020,000 으로 두 배**가 된다 |
| **D-120** | ~~차단~~ → **해소 (D-181 실측)** | **DB 적재를 못 한다 — 구조적 이유다.** `EST_PROJECTS.CUSTOMER_CODE` NOT NULL + `BAS_COMMON_CODES` '고객사' 그룹 **0건**(D-47·D-56) → `require_code()` 가 **422**. `EST_QUOTATIONS.PROJECT_ID` NOT NULL FK 라 위가 0이면 **한 행도 못 넣는다.** 값은 `docs/cad/archive_scan.json` 에 남겼다. **폴더명 고객사는 마스터가 아니라 후보다**(D-114) · **해소 실측(D-181): 고객사 코드 19 · EST_PROJECTS 12 행** |
| **D-121** | 확정 | **DXF 파서 버그 1건을 테스트가 잡았다** — HEADER 의 `$INSUNITS` 상태가 ENTITIES 까지 살아남아 LWPOLYLINE 의 group code 70(닫힘 플래그)을 삼켜 **닫힌 도형이 열린 것으로 뒤집혔다**. 고치고 회귀 시험을 붙였다 |

### 도입기업 확인 요청 — 추가 4건 (우선순위 순)

| # | 필요한 것 | 풀리는 것 |
|---|---|---|
| ⑦ | **고객사 코드 마스터** | `EST_PROJECTS` → `EST_QUOTATIONS` → **G-08·G-15·G-17 이 한 번에 열린다.** 지금 가장 값싼 한 방이다 |
| ⑧ | **`.dwg` → `.dxf` 변환 3,270건** | 파서가 이미 있다 — **Autodesk API 계정보다 싸다.** G-14 표본이 9 → 수천으로 |
| ⑨ | 홀 지름 기준 | 홀 수량 정확도 (지금 CIRCLE 전량) |
| ⑩ | 문자형 Feature 칸 | 재질 적재 (값은 읽히는데 NUMERIC 컬럼이라 못 넣는다) |

## 회전 19 — **DWG 변환으로 D-05 가 실제로 풀린다** (2026-09-12)

| ID | 상태 | 내용 |
|---|---|---|
| **D-122** | 확정 | **`EST_CAD_FEATURES` 에 문자값 칸이 없다 (TD5 결함).** `FEATURE_TYPE` 비고가 **재질을 유효한 값으로 명시**하는데 값 칸은 `FEATURE_VALUE NUMERIC(16,4)` 숫자뿐이다. **컬럼을 추가하지 않는다**(G-02 762 고정) — 관측값은 `SOURCE_DESC` 에 증거로 남기고 `FEATURE_VALUE` 는 NULL 로 둔다. 정규 칸 신설을 도입기업에 제안(요청 ⑩) |
| **D-123** | **확정 — D-05 판정이 바뀐다** | **`dwg2dxf`(GNU libredwg 0.14)로 DWG 를 변환할 수 있다.** 사용자 승인으로 `brew install libredwg` 설치. **무작위 표본 30건 변환 성공 21건(70%)**. 변환 결과에 **우리 파서가 그대로 돈다 — 파싱 21/21(100%)**: 총 절단장 **21/21** · 홀 수량 **19/21(90%)** · 두께 **17/21(81%)** · 재질 **16/21(76%)** · 판재 면적 **14/21(67%)**. 실측 예: 홀 254개 · 절단장 227,458mm · 판재면적 29,279,912mm² · 두께 3mm |
| **D-124** | 확정 | **요청 ⑧(dwg→dxf 변환)은 도입기업을 기다릴 일이 아니었다.** Autodesk API 계정 없이 **무료 GNU 도구로 된다.** 3,270건 전량이면 약 **2,290건**(70%)이 DXF 가 되고 **G-14 표본이 9 → 2,000+ 로** 간다. 팽창률 3배 · 추정 8.5GB(디스크 1.6TB 여유) — **변환 → 파싱 → 삭제를 한 건씩 흘려** 최대 사용량을 한 파일로 줄인다 |
| **D-125** | 확정 | **내 탐침이 또 틀렸다 (일곱 번째).** `DxfMeasure.features` 는 **메서드**인데 dict 로 다뤄 `TypeError: 'method' object is not iterable` 이 21건 전부에 났다. **"파싱 성공 0/21" 로 결론낼 뻔했다** — 실제는 21/21 이다. **탐침 오류를 데이터·코드 결함으로 결론내지 않는다** |

## 회전 20 — DWG 전량 변환 실측 (2026-09-12)

### 실측 — 네 단계를 각각 센다

| 단계 | 수치 |
|---|---|
| ① 대상 | 파일 **3,288** · **서로 다른 도면 1,136**(sha256) · 중복 파일 2,152. **"3,270건 처리" 는 거짓이었다**(D-115 재발) |
| ② 변환 | **772/1,127 (68.5%)**. **`.dwg` 만 보면 772/802 = 96.3%** |
| ③ 파싱 | **781/781 (100%)** |
| ④ Feature | 총 절단장 **777(99.5%)** · 홀 수량 **708(90.7%)** · 두께 **591(75.7%)** · 재질 **586(75.0%)** · 판재 면적 **564(72.2%)** · **용접장 0** |

### 변환 실패는 버전으로 완전히 갈린다 — 도구 문제가 아니다

| 서명 | 대상 | 성공률 |
|---|---|---|
| AC1015(R2000) 360 · AC1021(R2007) 237 · AC1018 77 · AC1024 50 · AC1014 45 · AC1027 2 · AC1032 1 | 772 | **전부 100%** |
| **AC1009 (R11/R12)** | 30 | **0%** (`DWG_SENTINEL_R11_*` — libredwg 미지원 구간) |
| **`V10.00` / `V15.00` (AutoCAD 아님)** | 325 | **0%** — 앞 6바이트가 `AC1xxx` 가 아니다. **변환기가 실패한 게 아니라 대상이 아니다** |

| ID | 상태 | 내용 |
|---|---|---|
| **D-126** | **확정 — 뜻밖의 소득** | **재질·두께 마스터 후보를 도면 586건에서 실측했다.** 재질 14종: **STS304 200 · STS316L 199 · SUS304 69 · SS400 52 · SUS316L 28 · STS304F 12 · A240316L 7 · STS316 6 · A516 6 · SUS316 3 · SUS304L · AL6061 · STS430 · ALUMINUM 각 1.** 두께 15종: 3.0(92) · 12.0(60) · 6.0(57) · 4.0(50) · 5.0(43) · 10.0(39) · 15.0(38) · 9.0(31) · 1.5(27) …  → **도입기업 요청 방식을 바꾼다**: "재질 코드 목록을 주세요" 대신 **"당신 도면 586건에서 이 14종을 실측했습니다 — 확인·정정해 주세요"**. 훨씬 쉬운 요청이고 근거가 붙는다. **단 아직 마스터로 등록하지 않았다** — 관측값이지 도입기업 확정이 아니다 |
| **D-127** | ~~**차단(새로 발견)**~~ → **해소 (D-181 실측)** | **단위가 섞여 있어 원가 환산에 바로 못 쓴다.** 781건 중 **`도면단위`(미상) 620 · mm 139 · inch 22.** 합계(총 절단장 531,596,852.2 · 판재 면적 545,975,103,520.4)는 **단위 혼합이라 의미가 없다.** DXF `$INSUNITS` 가 0(미지정)인 도면이 다수다. **원가 산출 전에 도면별 단위 확정이 필요하다** — 새 확인 요청 · **해소 실측(D-181): 단위 확정 mm (D-164 회신 mm)** |
| **D-128** | 확정 | **`EST_CAD_DRAWINGS` 는 적재된다**(781행 실측) — `PROJECT_ID` 가 NULL 허용이라 `EST_PROJECTS` 0행(D-120)이어도 도면만 넣을 수 있다. **`EST_CAD_FEATURES` 는 차단** — 3,226행을 실제로 넣어 재 보니 `check_ingest ⑤` 가 **G-13 을 PASS→FAIL** 로 떨어뜨렸다(확정 객체 0 + Feature N행 = 합성 판정). **그 규칙이 맞다** — 확정은 `review_object()` 사람 승인으로만 생긴다(G-24). **검사기를 우회하지 않았다.** 실측값은 `docs/cad/dwg_features.json` 이 정본이다 |
| **D-129** | **차단 유지 — G-14** | **표본이 9 → 781(87배)로 늘었지만 G-14 는 한 걸음도 안 움직인다.** `labels` 0 · `predictions` 0 · `EST_CAD_OBJECTS` 0행(내가 독립 확인). **늘어난 것은 입력이고 없는 것은 정답이다.** 우리가 뽑은 것은 **기하 집계**(홀 수량·절단장)이고 G-14 는 **IoU 0.5 기준 P/R/F1** 이다. 차단 축이 둘(파서·라벨)에서 **하나(라벨)** 로 줄었을 뿐이다 |
| **D-130** | 확정 | **내 측정이 여덟·아홉 번째로 틀렸다.** ① 산출물 JSON 키를 추측해 단위 분포가 `{}` 로 나왔다(실제 구조는 `_meta`·`요약`·`도면별`) ② `find -path '*label*'` 이 **`.venv` 안 sklearn/scipy 테스트 픽스처 4건**을 라벨로 셌다 — 에이전트의 "0건" 이 맞다. **이제 0 이나 실패가 나오면 먼저 내 명령을 의심한다** |

### 도입기업 확인 요청 — 갱신 (우선순위 순)

| # | 필요한 것 | 풀리는 것 | 비용 |
|---|---|---|---|
| **⑦** | **고객사 코드 마스터** | `EST_PROJECTS` → `EST_QUOTATIONS` → **G-08·G-15·G-17** | 목록 한 장 |
| **⑪** | **CAD 객체 정답 라벨** | **G-14·G-19 의 유일한 남은 구속** | 라벨링 공수 |
| **⑫** | **도면별 단위 확정**(도면단위 620건) | **원가 환산 — 없으면 Feature 가 숫자일 뿐이다** | 도면 규약 확인 |
| **⑬** | **`.cad`(`V10.00`) 포맷의 출처 CAD 제품** | 표본 781 → **1,106**(325건 추가) | 제품명만 알면 됨 |
| ⑭ | R11/R12 DWG 30건 재저장 | 표본 +30 | AutoCAD 로 열어 저장 |
| ⑨ | 홀 지름 기준 | 홀 수량 정확도(지금 CIRCLE 전량) | 기준값 |
| ⑩ | 문자형 Feature 칸 | 재질 적재(D-122) | 스키마 1컬럼 |
| **⑮** | **재질 14종·두께 15종 실측값 확인** (D-126) | 코드 마스터 — **우리가 목록을 만들어 드렸다** | 확인·정정만 |

## 회전 21 — 고객사 후보 마스터 (2026-09-12)

| ID | 상태 | 내용 |
|---|---|---|
| **D-140** | **확정 — 내 1차 실측 오류 정정** (※ 처음에 `D-131-a` 로 적었으나 코드가 `D-131` 을 다른 뜻으로 51곳에서 쓰고 있어 **대장을 옮겼다** — §4.1 코드가 맞다) | **내가 견적서에서 뽑은 7종 중 3종은 경동의 고객이 아니다.** `삼현엔지니어링`·`한국코아`·`탑산건설` 은 `REACTOR/…/암모니아 배관 업체/견적서갑지세로.xls` 의 **타사 견적 갑지 모음**에서 나왔다 — 같은 워크북에 호텔·모텔·병원·E-MART·안마시술소 수신처 **36종**이 있고 **경동 제품군과 0건 일치**다. **내가 그대로 넣었다면 무관한 회사가 고객사 마스터에 들어갔다.** 이번 세션 **열한 번째** 내 측정 오류이고 결과가 가장 무거울 수 있었던 건이다 |
| **D-132** | 확정 | **고객사 후보 46종**(높음 16 · 중간 7 · 낮음 23)을 `docs/cad/customer_candidates.{md,json}` 에 근거와 함께 만들었다. **적재하지 않았다.** 분류: 고객사(발주처) **12** · **수요처 7**(비나텍·에니젠·썬바이오·경동제약·연성정밀화학·서한보그워너·나눔허브) · 역할 미정 8(버리지 않음) · 협력사·공급사 16 · 관계사 3 · 별표 사람이름 17 · 제품명 26 · **출처 의심 36** |
| **D-133** | 확정 | **정식 상호·사업자번호를 얻은 유일한 경로는 발주서·계약서 PDF 다.** `웰 이 앤 씨 ㈜ / WELL E & C CO.LTD / 138-81-30002 / 대표 박용묵`(발주서 letterhead) · `두루텍 ㈜ 대표이사 변용준`(계약서 구매자, 판매자 = 경동글로벌텍 대표 김미란). **추측 보정 없이 원본에 적힌 것만** 얻었다 |
| **D-134** | 확정 | **발주처와 수요처를 가르는 근거는 폴더 2단 구조다** — `GD2003-00 웰이엔씨` → `GD2003-01-썬바이오` · `-02-에니젠` · `-03-비나텍`. 회의록의 `웰이엔씨(경동제약)` 도 같은 모양. **웰이엔씨가 발주하고 최종 수요처가 따로 있다** |
| **D-135** | 차단 | **도면 표제란의 라벨↔값 짝짓기가 실패했다.** `CLIENT`/`END USER` 라벨을 **434 도면**에서 찾았으나 DXF TEXT 순서로는 다음 항목이 또 다른 라벨인 경우가 많아 값을 못 붙였다. **좌표 기반 표제란 셀 인식이 필요하다.** 부풀리지 않고 실패로 적었다 |
| **D-136** | ~~미결~~ → **해소 (D-181 실측)** | **`cad/archive.py:52` `KNOWN_CUSTOMERS` 에 고객사가 아닌 것이 4종 들어 있다** — `최성배`(사람) · `원명에스티에스`·`리트산업`·`태양기어`(셋 다 **`경동글로벌텍 귀중` 견적서의 공급자**). 담당 개발3. 지금은 다른 에이전트가 도는 중이라 고치지 않았다 · **해소 실측(D-181): 고객사 아닌 이름 없음 · 확정 19종** |
| **D-137** | 확정 | **케이테크는 고객사가 아니라 관계사·전신 후보다.** 케이테크 견적서의 TEL/FAX `031-493-4298 / 494-4298` 가 **경동글로벌텍 견적서와 동일**하다. `김규영 부장`은 케이테크 담당자다 |
| **D-138** | 차단 | **못 읽은 것** — AES 암호 PDF 3문서(`cryptography` 미설치) · **스캔 PDF 244문서 텍스트 0**(OCR 미수행) · DWG 변환 실패 355 도면 · hwp 8 · pptx 3 · jpg 2 |

### 도입기업에 물어야 할 것 — 3가지 (후보 목록과 함께 보내면 된다)
① **정식 상호·사업자번호** (웰이엔씨 138-81-30002 외 미확보) ② **이미 쓰는 고객 코드 체계** ③ **발주처만 코드로 둘지, 수요처까지 둘지**(D-134 — 2단 구조가 실재한다)

## 회전 22 — G-08 합성 데이터 체인 (2026-09-12 · 사용자 지시)

### 번호 선언 (에이전트 요청)

| ID | 뜻 |
|---|---|
| **D-131** | **합성 데이터 기준** — 자재LOT·작업지시·공정실적·LOT추적·검사·출하는 **원천이 없어 지어낸 것**이다. 코드·테스트·화면 배지 **51곳**이 이 번호를 쓴다 |
| **D-139** | **고객사 코드 19종 확정** — 사용자 확정 2026-09-12, 관측 기반. 발주처 12 + 수요처 7 |
| **D-140** | (구 `D-131-a`) 내 고객사 1차 실측 오류 정정 — 타사 견적 갑지 3종 |

### 결과

| | |
|---|---|
| **G-08** | **`PASS   합성 데이터 기준 D-131 · 표본 40 · LOT 매핑 87.5% (기준 85.0%) · 사슬 끊김 정방향 0 · 역방향 0`** |
| 게이트 | `PASS 19 · FAIL 0 · 차단 13` → **`PASS 20 · FAIL 0 · 차단 12`**. 움직인 것은 **G-08 하나** |
| 빌드 | **1,245 passed · 0 failed** |

| ID | 상태 | 내용 |
|---|---|---|
| **D-141** | 확정 | **코드 마스터 — 고객사 확정 19종(D-139)** `ATTR1`=역할·확정표시 · `ATTR2`=확신도(높음 8 · 중간 6 · 낮음 5). **품목 가설 179종(D-131)** — 견적 고유문서 26건 중 **19문서 · 462행**(D-118-b 와 정확히 일치)에서 추출. 집계·원가계정 **86행/20표기**(`소 계`·`이 윤` 등)는 품목이 아니라 제외. **품목명을 지어내지 않았다** — "목록 밖 이름이 있으면 FAIL" 을 테스트가 단언한다. 재질·보관위치·불량유형·클레임유형·원가대상 5그룹은 **0건 유지**(D-47) |
| **D-142** | 확정 | **프로젝트는 실측 12건이다.** GD 폴더 99건 중 **고객사·제품군·도면 셋 다 이어지는 것만**. 못 이어지는 것은 **뺐다**(지어내 붙이지 않았다). 수주일시는 프로젝트번호 `YYMM` 에서 파생 — `date.today()` 없음(§10-3) |
| **D-143** | **확정 — 중요** | **합성 표시를 담을 비고 컬럼이 없는 표가 9종이다** — `EST_BOM_HEADERS`·`INV_RECEIPTS`·`INV_STOCKS`·`PRC_WORK_ORDERS`·`PRC_PERFORMANCES`·`PRC_PROCESS_HISTORIES`·`SHP_LOT_TRACES`·`SHP_INSPECTIONS`·`SHP_SHIPMENTS`. **컬럼을 추가하지 않았다**(G-02 762 고정). 대신 ① 게이트 판정 줄 ② 화면 배지 ③ `SYS_CONFIGS('시스템설정','SYNTHETIC_THREAD')='D-131'` 선언이 표시를 나른다 — **선언을 지우면 배지와 고지가 함께 사라져** 데이터만 남기고 표시만 떼는 것이 불가능하다 |
| **D-144** | 확정 | **의도적 단절 5/40 = 12.5%** — 전역 순번에 **고정**(무작위 아님, 재시드해도 같은 자리): #3 BOM 미연결 · #9 도면 미연결 BOM · #16 자재LOT 미투입 · #24 공정실적 누락 · #33 검사·출하 누락. **단절을 빼서 100% 를 만들지 않았다.** 9단계 완주 35/40 이고 선언값 `MAPPING_OK_YN` 도 35 — 시드가 선언하지 않고 **실제 연결을 확인해 다시 쓴다** |
| **D-145** | 확정 | **합성 데이터가 아니었으면 안 보였을 제품 결함 3건을 잡았다.** ① **D-210** `POST /shp/016` 채번이 `count(*)+1` 이라 연도별 시드 번호와 충돌 → `SHIPMENT_NO` UNIQUE 위반 **500**. 그 해 마지막 일련번호+1 로 수정 ② **D-211** `/kpi/044` 클레임 발생률 카드가 G-11 ③ 에 **오탐**(분모 39·분자 0인데 "분모 0을 0%로 메웠다" 로 판정) — **검사기를 고치지 않고** 보조설명을 `분모 … · 분자 …` 로 갈랐다. **QA2 에 보고 필요** ③ **D-206 정정** 출하 품목 코드를 제품군에서 가져오던 것을 BOM 레벨1 품목으로 수정 |
| **D-146** | 차단 유지 | **G-09·G-10 은 그대로 차단이다.** 정합성 분모가 0이다(`재질` 코드 · `EST_CAD_FEATURES` · `EST_QUOTATIONS` 부재). **분모를 만들려면 또 지어내야 해서 두었다.** `check_ingest ⑤` 도 우회하지 않았다(`EST_CAD_OBJECTS/FEATURES` 0행 유지) |

## 회전 23 — 가정 답변 예행 (2026-09-12 · 사용자 지시)

| ID | 상태 | 내용 |
|---|---|---|
| **D-150** | 가정 | **도입기업 답변을 가정해 예행했다.** 가정은 `docs/assumed/customer_answers.json` **한 곳**이고 **두 겹 선언**이다 — 파일 + `SYS_CONFIGS('ASSUMED_ANSWERS')='D-150'`. 둘이 같은 번호를 가리킬 때만 파생물이 산다. **고지 문구는 코드에 박지 않고 선언 파일에서 꺼낸다** |
| **D-151** | **확정 — G-14 는 수치를 냈지만 `차단`이다** | `IoU 0.5 · TP 3055 · FP 336 · FN 336 · **P/R/F1 0.9009**` — **임계 80% 를 넘지만 PASS 로 올리지 않았다.** 라벨과 예측을 **같은 DXF 파싱에서 파생**해 순환이기 때문이다. 판정 줄: `가정 답변 기준 (D-150) · 순환 라벨 — 정확도 증거 아님`. **고지를 수치 앞에 두어** `gate.py` 의 120자 절단에도 살아남는다 |
| **D-152** | **확정 — 내 가정이 틀렸다** | `customer_answers.json` ② 에 내가 적은 **"재질 코드가 서면 G-09 정합성 분모가 열린다" 는 틀렸다.** Feature **672행**을 넣어도 정합성 5항목 분모가 **전부 0** 이다 — `MATERIAL`·`THICKNESS_MM`·`LENGTH_MM` 이 NULL 이다. **가정을 세울 때 내가 확인하지 않았다** |
| **D-153** | 확정 | **내가 "두께 15종" 이라 적은 것도 틀렸다** — `dwg_features.json` 요약의 **상위 15종 절단**이었고 실측 distinct 는 **43종**이다. 요약 표를 원자료로 착각했다 (이번 세션 **열두 번째** 내 측정 오류) |
| **D-154** | 확정 | **예행이 죽은 코드 경로를 찾았다** — `tools/cad_ingest.py --dxf-features` 가 `got["skipped_text"]` vs 실제 `text_rows` 로 **첫 도면에서 KeyError**. **한 번도 실행된 적 없는 경로**였다. 고쳤다 |
| **D-155** | 확정 | **주입 377/377 ↔ 실측 336/336 이 어긋난다** — 같은 도면 안 좌표가 겹치는 CIRCLE 34개를 탐욕 매칭이 흡수했다. **맞추려고 고치지 않고** 검사기가 차이를 찍게 했다 |
| **D-156** | 확정 | **`check_ingest ⑤` 충돌은 실재한다** — Feature 를 적재하면 G-13 이 `차단→FAIL`(결함 2건). **우회하지 않고** 옵션으로 두고 되돌렸다. HITL 경로(`review_object()` → `CONFIRM_YN='Y'` → Feature)가 살아 있음을 실측 확인하고 테스트로 고정했다 |
| **D-157** | 확정 | **선언 제거 시험 2종을 실제로 돌렸다** — (A) `SYS_CONFIGS` 행 삭제 → G-14 가 **라벨 3,391건을 두고도 `차단` 복귀**, G-19 고지 소멸 (B) 출처 파일 이동 + `db-seed` → 재질 8행 회수 · 선언 0 · 라벨 생성기 exit 1. **가정을 지우면 파생물이 함께 사라진다** |
| **D-158** | 확정 | **에이전트 보고의 게이트 수치가 틀렸다** — `PASS 19` 라고 했으나 내 실측은 **`PASS 20 · FAIL 0 · 차단 12 · 미구현 0 / 32`** 다. **보고를 믿지 않고 다시 잰 것이 맞았다**(§4.2-5) |
| **D-159** | 확정 | **도입기업 확인 요청서를 썼다** — `docs/요청서_도입기업확인.md`. **7개 항목 전부 실측값을 먼저 보여주고 ○/× 만 받는 형태**다. ① `.cad` 325건 제작 프로그램 ② 재질 14종·두께 43종(200mm 11건 의심) ③ 단위 미표기 620건(틀리면 **25.4배 오차**) ④ CAD 정답 라벨 30~50건 + 어휘 3종/5종 불일치(D-98) ⑤ 실제 제조원가·실납기 ⑥ 고객사 19종·협력사 16종·케이테크 승계 ⑦ 나머지. **내 오류 2건(D-152 재질→G-09 가정 오류 · D-153 두께 15종)을 문서에 명시**해 도입기업이 내 착오를 물려받지 않게 했다 |
| **D-160** | **확정 — 도입기업 답변이 왔다** | 2026-09-12 회신 **`② O · ③ mm · ④ 3종 · ⑥ O`**. 선언 파일 `docs/assumed/customer_answers.json` 을 **D-150(가정) → D-160(답변)** 으로 갈아끼웠다. **항목마다 `성격` 이 다르다** — 확정 항목에는 가정 딱지가 아니라 **출처 표기**(`도입기업 확정 (D-160) 2026-09-12`)가 붙고, 미확정 항목에는 차단 고지(`미확정 (D-160)`)가 붙는다. **둘을 섞지 않는다**: 확정에 가정 딱지를 붙이면 쓸 수 있는 값을 못 쓰고, 미확정에서 떼면 거짓 증거다. ① 의 가정(`CADian` 추정)은 **회수했다** — 답이 없는 칸에 추정을 남기면 다음 사람이 확인된 사실로 읽는다 |
| **D-161** | **확정 — D-98 해소 · 내 판단이 틀렸다** | `가이드 3종을 쓰면 TD5 에 담을 자리가 없다`(D-98) 는 **틀렸다.** TD5 `EST_CAD_OBJECTS.OBJECT_TYPE` 비고는 `홀/슬롯/노즐/플랜지/치수문자 **등**` 이다 — **`등` 으로 끝나는 열린 목록**이고 컬럼이 VARCHAR(50) 이라 3종이 들어간다. 닫힌 어휘로 읽은 것은 나였다. 도입기업이 3종으로 확정하면서 충돌 자체가 없어졌다 |
| **D-162** | **확정 — G-14 수치를 회수했다** | 가정 답변 ④(D-150)에서 만든 **홀 라벨 3,391건과 `P/R/F1 0.9009`(D-151)를 회수**했다. **사유가 둘이고 각각 단독으로 회수 사유다** — ① 홀은 확정 어휘 3종 **밖**이다(범위 밖) ② 라벨과 예측을 같은 DXF 파싱에서 뽑은 **순환**이다. `tools/gen_cad_labelset.py` 는 생성기에서 **회수기**가 됐다: 어휘에 홀이 없으면 `labels`·`predictions` 를 0으로 비우고 회수 사유를 파일에 적는다. `evaluator_selftest` 는 남긴다 — 라벨과 무관한 채점기 자기검증이다(§10-16). **3종 라벨을 우리가 만들지 않는다** — `_title_block` 은 이름과 달리 영역이 아니라 TEXT 를 보므로 경계상자가 안 나오고, 없는 탐지로 정답을 만들면 그게 다시 순환이다. G-14 는 **차단 유지** |
| **D-163** | **확정 — 중요 · 내가 만들 뻔한 조용한 결함** | **표시 문구를 SQL LIKE 패턴에 복사해 둔 곳이 7군데였다** (`%단위 가정 (%` 5곳 · `가정 답변 기준 %` 2곳). ②③ 이 확정으로 바뀌며 문구가 `도입기업 확정 (D-160)` 이 되자 **일곱 곳이 전부 조용히 0건**이 될 참이었다 — 표지가 안 걸리면 **선언을 지워도 파생물이 남는다**. 회수 장치가 통째로 죽는 것이다. 문구가 아니라 **불변 부분**으로 건다: `assumed.ATTR1_LIKE = "%(D-%)%"`(결정번호 괄호) · `assumed.UNIT_TAG_LIKE = "%도면단위→%"`(변환 화살표). 테스트 2건이 이 사실을 단언한다 |
| **D-164** | 확정 | **③ 단위 = mm.** `$INSUNITS` 없는 620건이 mm 다. **확정된 것은 620건의 단위이지 22건의 환산이 아니다** — inch 로 명시된 22건은 여전히 inch 이고, 길이·면적을 한 축으로 합산할 때 **25.4 배 환산이 필요하다**. 표지는 없애지 않았다: `unit_tag()` 가 빈 문자열이면 `pipeline.store_dxf_features` 가 **단위 변환 자체를 하지 않는다**(둘을 묶어 뒀다). 문구만 `가정` → `확정` 으로 바뀐다 |
| **D-165** | 확정 | **② 재질 = 실측 14종 그대로 · STS = SUS 동일.** 8종으로 통합해 `BAS_COMMON_CODES` 에 섰다(STS304 282 · STS316L 234 · STS316 9 · SS400 52 · A516 6 · AL6061 1 · ALUMINUM 1 · STS430 1). **그래도 G-09 정합성 분모는 열리지 않는다** — D-152 가 이미 실측으로 밝혔다. 답변 ② 는 코드 마스터를 세울 뿐 그 차단을 풀지 못한다. 두께 **200mm 11건의 진위는 답을 받지 못했다** — 값을 지우지 않고 의심 표시만 남긴다 |
| **D-166** | 확정 | **⑥ 은 이미 확정된 것을 재확인했다** — 고객사 19종은 D-139 에서 확정됐고 코드 변경은 없다. 답변이 새로 닫은 것은 **케이테크 승계**(케이테크 시절 고객 5종이 경동 고객으로 이어진다 — 이미 19종 안에 있었다)와 **협력사·공급사 16종·미분류 8종을 고객사에서 뺀 판단**이다 |
| **D-167** | 차단 | **답을 못 받은 것 — 게이트는 차단이 정답이다.** ① `.cad` 325건 제작 프로그램(표본 781 → 1,106 이 막혀 있다) · ④ **3종 정답 라벨 30~50건**(G-14·G-19 의 유일한 분모) · ⑤ 실제 제조원가·실납기(G-15·G-17) · ⑦ 전문가 변수 목록(G-18) · 정식 상호·사업자번호 · R11/R12 30건 · 스캔 PDF 244건 · 두께 200mm 진위 · 홀 최소 지름 · 용접선 레이어 규약 |
| **D-168** | **차단 — 실측 결함 · prod 에서 수집이 안 된다** | **`POST /api/ingest/plc` 와 `/api/ingest/gateway/resend` 가 prod 에서 403 이다.** 세션이 없으면 `role_code` 가 빈 문자열이고 `rbac.can_write()` 가 막는다 — **PLC 와 Gateway 가 운영에서 데이터를 넣지 못한다.** 개발에서는 `x-kyungdong-role` 헤더가 SYSADMIN 을 줘서 G-12 를 포함한 게이트가 전부 통과했고 **그래서 안 보였다.** D-103 이 `면제하고 다른 수단(장비 등록 기반 인증)으로 보호한다` 고 적어 둔 그 수단이 **구현되지 않은 채였다** |
| **D-169** | **차단 — 고칠 자리가 없다** | 장비 인증에 쓸 칸이 없다. TD5 `IF_DEVICE_REGISTRY` **11컬럼에 비밀키·인증서·토큰이 없고**(DEVICE_NAME·DEVICE_TYPE·MODEL_NAME·IP_ADDRESS·PROTOCOL·COLLECT_INTERVAL·LOCATION_DESC·USE_YN·CREATED_DT·UPDATED_DT), **컬럼을 추가하지 않는다**(G-02 762 고정). 쓸 수 있는 유일한 칸 `IP_ADDRESS` 는 **등록된 2대 모두 NULL** 이다 — 도입기업이 장비 IP 를 주지 않았고 시드가 **지어내지 않았다**. 두 갈래뿐이다: ① 도입기업이 장비 IP 를 준다 ② 정본에 인증 칸을 넣는다(설계 변경) |
| **D-170** | 확정 | **동작을 바꾸지 않고 진단을 고쳤다.** `util/device.py` 를 넣어 역할로 막힌 뒤 **등록 장비인지 한 번 더 본다.** IP 가 전부 NULL 이라 오늘은 **아무도 통과하지 못한다 — 동작 동일**. 바뀐 것은 문장이다: `데이터관리 등록 권한 없음`(권한 설정을 뒤지게 된다) → `등록된 수집 장비 2대 전부 IP_ADDRESS 가 비어 있다 … 화면 034 에서 IP 를 넣으면 열린다`. **조용한 실패는 아니었지만 틀린 진단이었다.** IP 대조의 한계(`암호학적 인증이 아니다`)도 문장에 함께 싣는다 — 강도를 부풀리지 않는다 |
| **D-171** | 확정 | **되돌림 시험으로 실제로 열리는 것을 확인했다** — 장비 IP 를 `testclient` 로 등록하니 prod·세션 없음에서 `200 {"stored":1}`, 다른 IP(`10.0.0.99`)로 바꾸니 **403** 이다. 0건 경로만 재면 '막힌다' 만 알고 '열리는지' 는 모른다(§10-4). 시험 뒤 IP 를 NULL 로 되돌리고 적재 행도 지웠다 — `IF_PLC_SIGNALS` 는 런타임 전용 표라 0건이 정상이다(G-11). **시드가 IP 를 채우지 않았는지**를 테스트가 단언한다 — 채워 두면 '열린다' 가 검사기 자신 때문에 참이 된다 |
| **D-172** | 확정 | **화면 POST form 마다 토큰이 실렸는지 세는 검사를 넣었다.** 전에는 라우트 수(`csrf.require()` 31/31)만 셌다 — 라우터가 멀쩡해도 화면 form 이 토큰을 빠뜨리면 **그 화면의 쓰기는 403** 이고 게이트는 통과한다. D-73 이 정확히 그 사고였다(`with context` 누락 → 토큰이 빈 문자열). 지금 **10/10** 이고, 토큰 하나를 빼는 되돌림 시험에서 G-26 이 **PASS → FAIL** 로 떨어지며 빠진 form 을 이름으로 찍는 것을 확인했다. 면제 경로로 보내는 form 은 토큰이 없는 것이 맞다 |
| **D-173** | **확정 — D-136 해소** | **`cad/archive.py` 의 하드코딩 고객사 18종을 확정 정본에서 읽도록 바꿨다.** 박아 뒀더니 실제로 틀려 있었다 — `최성배`(사람) · `원명에스티에스`·`리트산업`·`태양기어`(*경동글로벌텍 귀중* 견적서의 **공급자**) 4종이 섞였고 `그린텍` 은 미분류였으며, 확정 19종 중 **7종이 빠져** 있었다. 실측 결과 `GD1806-최성배` **3폴더가 사람을 고객사로** 세고 `GD2003-01-썬바이오` **2폴더는 진짜 고객사를 빠뜨리고** 있었다(붙은 프로젝트 40→39 · 고유 11→10). 도입기업 ⑥ 회신(D-166) 뒤로는 정본과 **증명 가능하게** 어긋난 상태였다. **진실을 두 곳에 두면 한 곳이 조용히 낡는다** — D-163 과 같은 결함이다. 출처가 사라지면 **0건**이고 하드코딩으로 돌아가지 않는다 |
| **D-174** | **확정 — PLC 측을 세워 시뮬레이션한다** | 도입기업 장비 IP 가 아직 없어(D-169) 실물을 못 붙인다. **PLC 쪽을 만들었다** — `work/plc_device.db`(SQLite)가 장비의 레지스터 메모리다. **우리 PostgreSQL 밖에 둔 것이 요점이다**: 같은 DB 에 넣으면 *장비가 들고 있던 값*과 *우리가 적재한 값*이 구분되지 않는다. 두 다리로 나눠 돈다 — ① **PLC 스캔**(레지스터에 쓰고 멈춘다) ② **Gateway 폴링**(미전송분을 가져가 적재하고 **성공한 것만** 보냄 표시). 곧장 `collector.ingest_batch()` 를 부르던 예전 방식은 이 두 다리가 통째로 빠져 **끊긴 동안 장비가 값을 들고 있는 것**을 잴 수 없었다. 레지스터 주소 `SIM-D0001…` 은 **실물이 아니다**(D-176) |
| **D-175** | **확정 — 내 모델이 틀렸던 것을 고쳤다** | 처음 만들 때 **단절 하나에 Gateway 버퍼와 PLC 레지스터가 같은 값을 둘 다** 들고 있었다 — 복구하면 **두 번 올라간다**. 끊긴 자리가 다르면 값이 남는 자리도 다르다: ① **PLC↔Gateway** 단절(`--plc-offline`) → 폴링 자체가 안 되므로 값은 **장비에 남고** Gateway 버퍼는 **0**이다(Gateway 는 본 적도 없다) ② **Gateway↔클라우드** 단절(`--disconnect`) → 폴링은 됐으므로 PLC 는 **넘긴 것으로 보고** TD5 `IF_GATEWAY_BUFFER` 가 진다. 실측 — ①: 스캔 80 · 적재 32 · **장비 잔량 48** → 복구 후 80/80 유실 0 · ②: 버퍼 3배치 → 재전송 후 **80행 유실 0** |
| **D-176** | 차단 | **PLC 태그맵(태그 ↔ 실물 레지스터 주소)이 없다.** 시뮬레이터는 `SIM-D0001~D0008` 을 쓰는데 **실물 XGB(XBC-DN32H) 주소를 아는 척하지 않는다**. Gateway 가 실제로 어느 레지스터를 읽어야 하는지는 도입기업이 줘야 한다. 장비 IP(D-169)와 함께 받는 것이 자연스럽다 |
| **D-177** | 확정 | **시뮬레이터 장비 IP 는 선언과 함께만 존재한다.** `--register` 가 `IF_DEVICE_REGISTRY.IP_ADDRESS` 와 `SYS_CONFIGS('시스템설정','PLC_SIMULATOR')='D-174'` 를 **같이** 넣고 `--unregister` 가 **같이** 거둔다. 선언 없는 IP 는 **실물 장비가 붙은 것처럼** 읽힌다. 기본 상태는 **미등록**이다 — 실측(IP 없음)을 그대로 두고, 시뮬레이션은 명령 한 줄로 켠다. 테스트 `장비_IP_는_선언_없이는_존재할_수_없다` 가 **0건 단언을 뒤집어** 유지한다(지우지 않았다): 전에는 `IP 0건인지`, 이제는 `있다면 시뮬레이터라고 선언돼 있는지` |
| **D-178** | 확정 | **prod·HTTP 경로를 실제로 통과시켰다** — 서버를 `KYUNGDONG_ENV=prod` 로 띄우고 `--via http` 로 진짜 `POST /api/ingest/plc` 를 보냈다. 장비 인증·태그 8종 검증·`RUN_STATUS` 어휘를 전부 지나 `IF_PLC_SIGNALS 80 · DAT_TIMESERIES 60 · PRC_EQUIP_SIGNALS 10`. **D-168 이 막아 뒀던 그 경로다.** `--via inproc`(기본)은 HTTP·장비 인증을 지나지 않으므로 그것만으로는 D-168 을 못 잡는다 — 서버가 없으면 `--via http` 는 **소리내어 죽는다**(조용히 inproc 으로 되돌아가지 않는다) |
| **D-179** | **확정 — 게이트가 내 코드를 잡았다 (2건)** | 시뮬레이터를 넣고 게이트를 돌리니 **`PASS 20 → 17 · FAIL 3`** 이 됐다. ① **G-07** — `plcdb.mark_sent` 가 `datetime.now()` 를 썼다(§10-3 위반). 검사기가 **파일·줄·함수와 고칠 방법까지** 찍었다(`src/kyungdong/ingest/plcdb.py:144 (mark_sent) — … clock.anchor() 를 쓴다`). 벽시계로 적으면 날짜가 바뀔 때마다 같은 시뮬레이션이 다른 값을 낸다 → `clock.anchor()` 로 고쳤다 ② **G-26** — `urllib` import 를 **외부 통신 흔적**으로 잡았다(사업계획서 9.2 외부 접속 최소화) |
| **D-180** | 확정 | **검사기를 느슨하게 하지 않고 정밀하게 만들었다.** 전에는 `import urllib` 만 보고 잡아서 **"우리 앱에 POST 하는 시뮬레이터"와 "외부 API 호출"이 구분되지 않았다.** 이제 갈라서 본다 — **제품 `src/` 는 통신 모듈 0건이 절대 기준**(그대로), `tools/` 는 **외부 목적지 리터럴이 있을 때만** 결함이다. 동시에 시뮬레이터 쪽을 **실제로 막았다**: `check_loopback()` 이 `127.0.0.1`·`localhost`·`::1` 밖이면 거절한다 — 임의 호스트로 POST 할 수 있으면 폐쇄형 경계가 이 파일 하나로 뚫린다. 되돌림 시험으로 외부 URL 리터럴 하나를 넣으니 G-26 이 **PASS → FAIL** 로 떨어지며 목적지를 찍는 것을 확인했다 |
| **D-181** | **확정 — 대장이 사실과 어긋나 있었다** | `차단`·`미결` **45건 중 16건이 이미 해소**돼 있었다. D-120 은 '고객사 코드 0건이라 적재 불가' 인데 코드는 **19건** 서 있고 `EST_PROJECTS` 12행이 들어 있었다. D-77 은 '파싱 실패가 500' 인데 **422**였고, D-69·D-70(**Critical**)은 판정이 `stale_verdict()` **한 곳**으로 합쳐져 있었다. D-102 는 `CSRF_ENFORCE=True` 다. **`CLAUDE.md` 가 "대장에 없는 것을 마음대로 정하지 않는다" 고 못박는데 그 대장이 낡아 있었다** — 다음 할 일을 이 목록에서 고르면 끝난 일을 또 한다. 실제로 사용자에게 이 목록을 그대로 드릴 뻔했다 |
| **D-182** | 확정 | **손으로 닫으면 또 낡는다 → `tools/check_decisions.py` 를 만들었다.** 항목마다 **근거를 다시 재는 probe** 를 둔다(27종). probe 가 `해소` 인데 대장이 `차단` 이면 **결함**이고, probe 가 없는 것은 **`수동` 이라고 적는다**(18종 — 전부 정본 부재·도입기업 미제공이라 코드로는 알 수 없다). **확인한 척하지 않는다.** 닫은 항목은 **되열림**도 재검한다. 원문은 **지우지 않고** 취소선 + 실측 근거를 붙였다 — 왜 열려 있었는지가 역사다. **대장은 사람이 닫는다** — 검사기가 쓰면 어떤 근거로 닫혔는지가 사라지므로 `write_text` 금지를 테스트가 단언한다 |
| **D-183** | 확정 | **게이트를 늘리지 않았다.** `goal.md` 는 **32건 고정**(G-01~G-30 + 정본 + G-빌드)이다. 대장 검사를 게이트로 만들면 33이 되어 정본 수치가 흔들린다 → **테스트가 진다**(`tests/test_arch_decisions.py` 5건). 깨지면 `G-빌드` 가 FAIL 이라 **대장이 낡은 채로 회전이 끝나지 않는다**. probe 가 상수를 돌려주지 않는지도 단언한다 — 실제로 D-32·D-71·D-75 의 첫 probe 가 조건을 안 재고 문장만 돌려줬고 그래서 **D-32 를 해소로 잘못 찍었다**(코드성 FK 29개를 0개로 셌다). 셋 다 실측으로 바꿨다 |
| **D-184** | **확정 — G-10 이 차단에서 풀렸다** | **`PASS 시뮬레이터 기준 D-174 — 실물 설비 실측이 아니다 · 결측률 10.0% (주입 10.0%) · 정렬 위반 0/0`**. `make db-reset` 직후 런타임 표는 늘 0이라 G-10 은 **영원히 차단**이었는데, 측정은 `check_ingest.py` 가 시뮬레이터로 **이미 하고 있었고** 그 결과가 G-10 에 귀속되지 않았을 뿐이다. **결함 수정이 아니라 판정이 사실을 말하게 만든 것**이다. 분모 0이면 시뮬레이터로 채워 재고 **되돌린다** — 런타임 표가 0으로 안 돌아오면 G-11 이 오염되므로 정리 실패를 찍는다 |
| **D-185** | 확정 | **완벽한 시계열을 만들지 않았다 — 결측을 일부러 냈다.** 20주기 중 **2주기(7·13, 무작위 아님)** 를 빼서 기대 결측률 10.0% 를 만들었다. 완벽하게 만들면 결측률이 늘 0% 이고 그러면 **이 지표가 결측을 잡는지 알 수 없다**(§10-16). 되돌림 시험 — 주입을 4주기로 늘리니 **10.0% → 20.0%** 로 따라 움직였다. 주입과 실측이 어긋나면 **맞추려고 고치지 않고 그 차이를 적는다**(D-155 선례) |
| **D-186** | 확정 | **고지를 수치 앞에 뒀다** — `gate.py` 가 판정 줄을 120자에서 자르므로 뒤에 두면 `PASS · 결측률 10.0%` 만 남아 **실물 설비 실측처럼** 인용된다(D-151 과 같은 이유). 테스트가 소스에서 두 문자열의 **순서를 직접 확인**한다. 또 정렬 위반 SQL 이 두 판정에 복사될 뻔한 것을 `order_violations()` 한 곳으로 뺐다 — **같은 SQL 을 둘이 들고 있으면 한쪽만 고쳐져 같은 DB 에 반대 결론이 난다**(D-70 이 정확히 그 사고였다) |
| **D-187** | 확정 | **내 파서가 상태를 글자 포함으로 봐서 틀렸다.** `확정 — G-10 이 차단에서 풀렸다` 는 **확정**인데 '차단' 이 들어 있어 열린 항목으로 셌다 — 이 문장 하나로 29 → **30** 이 됐다. **맨 앞 상태어**로 판정하게 고쳤다(`확정` 으로 시작하거나 `해소` 가 있으면 닫힘). D-151(`확정 — G-14 는 수치를 냈지만 차단이다`)도 같은 이유로 잘못 세어지고 있었다. 최종 **열림 28건**. 대장이 커질수록 이런 문장이 늘어나므로 테스트가 8가지 표기를 직접 단언한다 |
| **D-188** | **확정 — 미비 항목을 화면에 남겼다** | 사용자 요청: *"미비하거나 부족한 건 개발된 화면에 남겨줘. 팔로우업할테니."* 지금까지 "무엇이 부족한가" 는 `decisions.md`·`progress.md`·게이트 판정줄에만 있었다 — **전부 개발자가 보는 자리**다. 화면을 쓰는 사람은 어떤 칸이 왜 비어 있는지 알 수 없었다(빈 그리드에 `미확정 (D-nn)` 이 떠도 그 번호가 무슨 뜻인지 화면 어디에도 없었다). `app/followup.py` 가 대장을 읽어 **화면별 미비 항목**을 렌더한다 — `render()` 한 곳에서 주입하므로 45화면 전부에 자동으로 붙는다 |
| **D-189** | 확정 | **출처는 대장 하나다.** 화면이 자기 목록을 들면 대장과 어긋난다 — 그 사고를 이미 두 번 겪었다(D-163 표시 문구 7곳 복사 · D-181 대장 16건 낡음). 그래서 `tools/check_decisions.py` 가 **`app/followup.py` 의 파서를 쓴다**(자기 파서를 지웠다). 테스트가 **검사기 건수 = 화면 건수**를 단언한다 — 둘이 다르면 하나는 거짓이다. 현재 **28건** |
| **D-190** | 확정 | **화면에 적는 것은 D-번호가 아니라 말이다.** 네 칸을 낸다 — `상태`(차단/미결·Critical 은 빨강) · **`닫으려면`**(도입기업 제공 / 산출물 보완 / 개발 잔여) · **`왜 비어 있나`** · **`지금은 어떻게 하고 있나`**. 마지막 칸이 핵심이다: 왜 비었는지만 보이고 지금 뭘 하고 있는지가 없으면 **"고장 났나" 로 읽힌다**. 대장 본문의 `관찰부 \| 대응부` 를 갈라서 둘 다 싣는다 |
| **D-191** | 확정 | **어디에도 안 뜨는 항목이 없다.** `SCREEN_MAP` 에 배정된 것은 그 화면에, 배정이 없는 것은 **`029 시스템 설정`** 에 모인다. 화면을 새로 못 만들기 때문이다(화면 45 고정 — G-01). `029` 는 **전체 28건**을 보여 준다 — 화면마다 자기 것만 보이면 "전부 몇 건인지" 를 볼 자리가 없다. 테스트가 `029 건수 = 열린 항목 전체` 를 단언한다 |
| **D-192** | 확정 | **죽은 배정 4건을 테스트가 잡았다** — `D-56`·`D-151`·`D-06`·`D-09` 가 `SCREEN_MAP` 에 있는데 대장에서는 이미 닫혀 있었다. 닫힌 항목의 배정은 **화면에 영영 안 뜬다** — 남겨 두면 "배정은 해 뒀다" 는 느낌만 주고 실제로는 아무 데도 안 보인다. **허용목록으로 덮지 않고 배정표에서 뺐다.** 대장이 닫힐 때마다 이 단언이 다시 잡는다 |
| **D-193** | 확정 | **내가 화면 번호를 틀렸다** — 장비 IP 등록처를 `화면 034 수집장비관리` 라고 적었는데 034 는 **데이터조회**다. 실제로는 **`029 시스템 설정`** 안의 `수집 장비 등록` 표다(`routers/sys.py:411`). `util/device.py`·`contracts/interfaces.md`·요청서 **3곳**에 잘못 적혀 있었고 전부 고쳤다. 도입기업이 그 문구를 보고 034 로 갔으면 없는 화면을 찾았을 것이다 |
| **D-194** | 확정 | **대장을 못 읽어도 화면은 뜬다.** 미비 목록은 **부가 정보**라 파일이 없거나 깨져도 화면이 죽으면 안 된다 — `open_items()` 는 빈 튜플을 돌려주고 패널이 사라질 뿐이다. 다만 **조용히 채우지는 않는다**: 빈 목록이지 "미비 없음" 이 아니다. 테스트가 대장 경로를 없는 파일로 바꿔 실제로 확인한다 |
| **D-195** | 확정 | **`--realtime` 이 시뮬레이터 표지를 떼고 있었다.** 시각 기준(`앵커` vs `실시각`)과 데이터 출처는 **다른 축**인데 한 분기에서 둘을 같이 정해서, `--realtime` 이면 `COLLECT_PATH` 가 실수집 라벨(`PLC→Gateway→시계열DB`)로 적혔다 — **시뮬레이터 데이터가 실수집과 구분되지 않는다.** 표지를 분기 밖으로 빼 **언제나 붙게** 했다 |
| **D-196** | **확정 — 같은 결함이 한 겹 더 있었다** | `--via http` 로 보내면 **HTTP payload 에 `collect_path` 가 빠져서** 수집 API 가 기본값(실수집 라벨)으로 적었다. `inproc` 은 `collect_path` 를 직접 넘겨 표지가 붙는데 **HTTP 경로만 빠져 있었다** — 표지를 두 경로가 **따로** 붙이고 있었던 것이 원인이다(D-163·D-70 과 같은 계열: 진실이 두 곳에 있으면 한 곳이 빠진다). D-178 에서 "prod·HTTP 실통과" 를 보인 그 데이터도 **실수집으로 적혀 있었다** |
| **D-197** | **차단 — 판정하지 않은 것이 `정상` 으로 적힌다** | 이상치 판정은 표본이 모자라면(`MIN_HISTORY` 미만·산포 0) **판정하지 않는다**(`OutlierVerdict(decided=False)`). 그런데 저장은 `QUALITY_FLAG='정상'` 이다 — **TD5 어휘가 `정상/노이즈/결측` 3종뿐이고 `등` 도 없어 판정 불가를 담을 값이 없다.** `DAT_TIMESERIES` 9컬럼에 자유 서술 칸도 없다(D-143 과 같은 상황). 실측: 40주기 중 이상치 10행을 주입했는데 **첫 2행이 `정상`** 으로 남았다(그 시점에 이력이 없어 판정 불가). `IngestResult.undecided_rows` 가 건수를 세지만 **적재되면 사라진다** → `QUALITY_FLAG='정상'` 을 세는 쪽은 **판정 안 한 행을 검증된 정상으로 센다.** 컬럼을 늘리지 않는다(G-02 762 고정) — 도입기업·정본에 어휘 추가를 제안한다 |
| **D-198** | **확정 — 화면 6곳에 파이썬 객체가 찍히고 있었다** | `{{ td3.items }}` 가 **dict 의 `.items` 메서드**로 잡혀 화면에 `<built-in method items of dict object at 0x…>` 가 그대로 나왔다. Jinja 는 속성 접근을 먼저 하므로 `items` 라는 **키**가 같은 이름의 메서드에 가려진다 — **D-46(`anchor`→`clock`)·D-54(`util.audit`)와 같은 이름 가림 함정**이 템플릿에서 재발한 것이다. `login.html`·`_popup.html`·`common.html`·`board.html`·`_placeholder.html`·`dsh/_kit.html` **6곳**을 `td3['items']` 로 고쳤다. 테스트 2건이 막는다 — ① 템플릿에 `.items`·`.keys`·`.values` 키 접근이 있는지 ② **45화면을 실제로 렌더해** `<built-in method …>`·`<… object at 0x…>` 가 새는지. ②는 문자열 규칙이 아니라 화면이 답한다 |
| **D-199** | 확정 | **표본 크기를 파라미터로 뺐다** (`KYUNGDONG_SAMPLE_LOTS`, 기본 40). 사용자 요청 *"샘플데이터 100개씩 넣어줘"*. **기본 40 은 함부로 안 바꾼다** — 선정 프로젝트 12종 × `BOMS_PER_PROJECT` 4(견적 갑지 2~5장 실측 근거)를 도면 수로 자른 **근거 있는 수**다. 키울 때는 라운드로빈으로 목표를 채우되 **도면이 모자라면 거기서 멈춘다**(없는 도면으로 BOM 을 만들지 않는다). 100 으로 깔면 `SYS_CONFIGS('시스템설정','SAMPLE_SCALE')` 선언이 붙고 **45화면 전부에 `표본 확대 (D-199)` 배지**가 뜬다 — 늘린 표본 수치를 기본 실측처럼 인용하지 못하게 |
| **D-200** | **확정 — 중요 · 표본만 늘리면 게이트가 좋아 보인다** | 단절 5건을 고정해 두고 표본만 40 → 100 으로 늘리면 단절이 **12.5% → 5%** 로 묽어져 **LOT 매핑률이 87.5% → 95%** 가 된다. **나아진 것은 하나도 없는데 수치만 오른다** — 표본을 키워 게이트를 통과시키는 짓이다. 그래서 단절도 비율(`BREAK_RATIO` 12.5%)로 함께 늘린다. 표본 40 에서는 **기존 자리 (3, 9, 16, 24, 33) 를 그대로 재현**하고(테스트가 대조), 100 에서는 12자리로 편다. 실측 결과 **88.0%** — 40일 때 87.5% 와 같은 수준이다 |
| **D-201** | **확정 — 조용히 깨지고 있었다** | **표본 크기를 섞으면 사슬이 깨진다.** 자재LOT 번호가 `LOT-{날짜}-{그날 일련번호}` 이고 일련번호는 계획 순서로 매겨져서, 표본 100 DB 에 40 으로 재시드하면 같은 날짜의 앞 번호가 **다른 BOM 을 가리키도록 덮어써지고** 뒤 번호는 남는다. 실측: BOM 키 100 → **89**, LOT 매핑 88.0% → **82.0%** 로 G-08 이 FAIL. **깨졌다는 신호는 어디에도 없었다 — 숫자만 나빠졌다.** 두 겹으로 막았다 — ① `sample_lots()` 가 환경변수 없으면 **DB 에 기록된 크기를 따른다**(재시드가 언제나 안전) ② 크기를 **바꾸려 하면** `guard_sample_size()` 가 `SystemExit` 로 세우고 `db-reset` 을 안내한다 |
| **D-202** | 확정 | **표본을 키우니 테스트 2건이 깨졌는데 둘 다 테스트가 틀린 것이었다.** ① `test_017_LOT_열이_024_링크다` 가 `ORDER BY` 없는 `limit 1` 로 아무 LOT 이나 뽑아 **1페이지에 있는지** 봤다 — 100건이 되자 밀려났다(D-81 과 같은 계열). **특정 행이 아니라 불변식**을 단언하게 바꿨다: *화면에 뜬 LOT 은 전부 링크여야 하고 그 링크는 실재해야 한다*. ② 그 과정에서 `chain` 픽스처가 자기 LOT(`PLOT-2026-*`)을 따로 넣는다는 것도 드러났다 — 비교 대상을 표 전체로 넓혔다 |
| **D-203** | **확정 — 중요 · 단절이 조용히 치유되고 있었다** | **"만들지 않는다" 는 단절(`공정실적 누락`·`검사·출하 누락`)은 멱등이 아니었다.** 그 자리가 한 번이라도 단절이 아닌 적이 있으면(표본 크기가 달랐다거나) 그때 만든 행이 **그대로 남아 사슬을 잇는다** — 선언은 '끊었다' 인데 데이터는 이어져 있다. 실측: 재시드할 때마다 LOT 매핑이 **88.0% → 91.0% → 92.0%** 로 **올랐다.** 나아진 것은 없는데 숫자만 오르는 것이라 `_clear_broken()` 이 **남은 행을 지운다**. FK 때문에 순서가 있다 — `PRC_PROCESS_HISTORIES.PERF_ID` 를 먼저 비우고(단절의 뜻이 *이력은 남기되 PERF_ID 가 빈다* 이다) 공정실적을 지우며, 출하 품목의 `INSPECT_ID` 를 떼고 검사를 지운 뒤 **품목이 하나도 안 남은 출하 헤더만** 지운다. 이제 깨끗한 시드와 재시드가 **둘 다 88.0%** 다 |

### 라벨 30건의 성격 (D-151 보강)
박스 **3,768개** · **FP 10%(i%10==3) · FN 10%(i%10==7)** 고정 주입, 무작위 없음, 377자리를 통번호·도면·박스로 전부 기록.
어휘는 **TD5 5종**(가이드 3종 아님 — D-98). 5종 중 **홀만 생기고 나머지 4종은 0건** — **빈 어휘를 박스로 채우지 않았다.**
