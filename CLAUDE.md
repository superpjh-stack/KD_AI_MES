# CLAUDE.md

**경동글로벌텍**(경기 화성 · **ETO 주문 설계·제작 고정밀 산업장비** — 반응기·교반기·진공건조기·누체필터·저장탱크 ·
제약/식품/화학/바이오 공급 · ASME·KS · 종업원 8명)의 **제조AI특화 스마트공장** 코드.
공급기업 **주식회사 재일**, 과제번호 **SF26179182**.

## 먼저 읽는다

1. **`goal.md`** — 목표·수용 게이트 G-01~G-30·팀 편성·루프 프로토콜. **7명 전원 필독. 이것이 메타프롬프트다.**
2. `contracts/` — 코드 계약(db-schema · api-contract · screen-map · interfaces). **코드와 다르면 코드가 맞다** → 계약을 고친다.
3. `decisions.md` — 결정 대장 D-01~. **여기 없는 것을 마음대로 정하지 않는다.**
4. `progress.md` — 검증된 현황의 유일한 진실. 실측값 + 검증 명령만 적는다.

## 정본 우선순위 (goal.md §0.3)

| 무엇 | 정본 |
|---|---|
| 수치·목표·정책·과제 성격 (KPI · AI 과제 · 성능목표 5종 · 데이터 유효성 · 보안 · HW · 수집 지점 · 공정 · 사업비) | **`docs/사업계획서.pdf`** (보완본 60p) |
| 코드 골격 (화면 45 · 프로그램 49 · 테이블 68 · 컬럼 762 · ID 체계 · 그리드 열) | `docs/design/design.json` · `docs/design/analysis.json` |

- `design.json` — SF-TD1~TD5. `td3.screens[*].screens[*]`(화면 45 · `mockup` dict) · `td4.details[*].programs[*]`(49 · `flow`·`functions`·`tables`) · `td5.details[*].columns`(68표·762컬럼, 행 = `[한글명, 영문명, 타입, PK, FK, NULL, 비고]`) · `td3.role_matrix`(**6역할×8영역**) · `td3.common_screens`(4).
- `analysis.json` — SF-AD1 · AD2(기능 45 + 비기능 13 = 58) · AD3(45행).
- 화면설명서 HTML 은 `docs/산출물/`. CAD 통계·라벨링 가이드는 `docs/cad/`. 테스트 시나리오는 `../04 QA Agent/outputs/`.

python 으로 바로 읽는다. docx 는 열지 않는다. **`pdftotext` 가 없다** → PDF 는 `uv run --with pypdf`.

## 이 사업의 AI 는 **CAD 견적 자동화 + RAG Agent** 다

**AI 대상 3대 공정** — ① **수주·견적: CAD 견적 자동화 AI** ② **입고·보관: 입고 RAG Agent** ③ **보관·출하: 출하 RAG Agent** (사업계획서 2.5).

**예지보전·품질예측 모델은 만들지 않는다** — 가공(포밍)·제관 및 용접·포장 공정은 사업계획서 2.5 가 **"현 사업 미적용"** 으로 명시했다(**D-01**).
직전 사업(광성정밀)이 예지보전이었으므로 코드를 가져올 때 **가장 위험한 오염원**이다.

**CAD 파이프라인 5단계** (화면 010→011→012·013→014→015):
Parsing(Autodesk API) + Vision AI(**YOLOv8**+OCR) 병렬 → 객체인식 결과 **HITL 검증**(`EST_OBJECT_REVIEWS`) →
**Rule + GNN** BOM 자동생성 → **XGBoost** 견적 자동산출 → **SHAP** 영향요인.
각 단계 산출물에 **Confidence Score 와 근거**를 표시하고, **담당자 승인 전에는 확정 상태로 표기하지 않는다**.

## 규모 (실측)

화면 **45** / 업무영역 **10** / 프로그램 **49**(화면 45 + 인터페이스 4) / 테이블 **68** / 컬럼 **762** /
요구사항 **58**(기능 45 = MES-AD2-001~045, 비기능 13 = 046~058) / **281.08 FP**(사업계획서 4.2).

메뉴 10영역 — 대시보드 · 견적AI(EST) · 입고(INV) · 공정(PRC) · 출하(SHP) · 기준(BAS) · 데이터(DAT) · Agent(AGT) · KPI · 시스템(SYS).
테이블 접두 — `BAS`3 `SYS`4 `INV`6 `EST`16 `SHP`6 `PRC`7 `DAT`11 `AGT`3 `KPI`2 `IF`10 = **68**.
인터페이스 4종(화면 없음, 상태는 화면 033 데이터통합관리에서 조회) — ERP · IoT/PLC · CAD 파일 · 외부 표준문서.

**공정 10단계** — 수주견적관리 → 입고검사 → **소재가공(외주)** → 가공(**레이저 커팅**) → 제관 및 용접 → **버핑(외주)** → 조립 → 출하검사(**수압·기밀·진공**) → 포장 → 출하.

**수집 지점은 2개소뿐이다** — 신규 레이저커팅기 **Master PLC 1지점**(Ethernet/OPC-UA) + **터치PC(현장POP) 1지점**.
그 외 공정 실적은 터치PC·스마트패드 **수동 입력**. 외주 2공정은 발주·반출·반입 상태 관리. **전 공정 실시간 수집을 만들면 결함이다**(D-06).

**모든 업무 화면의 최상위 조회 축은 프로젝트(수주)번호·도면번호다.** LOT 는 프로젝트 하위 식별자다.
디지털 스레드: `EST_PROJECTS` → 도면 → BOM → 자재LOT → 작업지시 → 공정실적 → `SHP_LOT_TRACES` → 검사 → 출하.

## 숫자 — 지어내지 않는다

| 항목 | 값 | 출처 |
|---|---|---|
| **제조 리드타임** | **1,320h(55일) → 1,080h(45일)** (−18.2%, 가중치 0.5). 산식 = 작업지시 확정 ~ 포장완료 평균 소요시간 | 사업계획서 1.5 |
| **수주출하 리드타임** | **1,440h(60일) → 1,200h(50일)** (−16.7%, 가중치 0.5). 산식 = 출하시각 − 수주확정시각 | 사업계획서 1.5 |
| KPI 측정근거 | 2025년 생산 일지 (수주출하는 3개월분) | 사업계획서 1.5 |
| AI 성능목표 5종 | CAD 객체 인식 **80%↑**(IoU 0.5 · P/R/F1) / 견적 산출 정확도 **±5% 이내**(MAPE·MAE) / BOM 생성 정확도 **85%↑**(항목 단위 P/R/F1) / 납기 예측 정확도 **85%↑**(허용오차 + RMSE) / 설명가능성 **90%**(SHAP Top-N vs 전문가, Spearman ρ) | 사업계획서 2.6 — **⓶ 산식 칸 5개 전부 공란 (D-13)** |
| 데이터 유효성 | 정확성 **85%↑** · 정합성 **85%↑** · 연계성 **85%↑** · AI 학습 적합성 판정 | 사업계획서 2.7.4 · AD2-056 |
| CAD 원천 데이터 | 전체 3,237건 중 **CAD 원본 1,283건**(dwg 918 · cad 355 · dxf 10) · **77 프로젝트**. REACTOR 448 · RECEIVER TANK 102 · 경동도면함 733. **파일명 중복 183건 · 0KB 손상 4건 · 제품당 편차 최대 184/최소 1/중앙값 5 · 도면 1건뿐인 제품 9개** | AD1 [분석 산출] (D-03) |
| 전처리 | 이상치 제거 · 결측 보정(치수·OCR 오류·재질 미입력을 유사 도면·기준값으로) · 단위 표준화(mm·kg) · **중복 제거 기준 = 도면번호+버전+고객사** · Feature 생성(홀 수량·절단 길이·형상 복잡도·두께·재질·용접 길이) | 사업계획서 2.7.4 |
| HW | 레이저커팅기 1(FC3015S_6000/6kW) · Master PLC 1식(XBC-DN32H + XBL-EMTA) · **현황판 65" 2대** · 현장POP(터치PC) 1 · Data Gateway 1 · 관리용 컴퓨터 1 · **스마트패드 2** · 무선AP 3 = 58,150천원 | 사업계획서 2.1·4.3 |
| 클라우드 | AP 1(4vCPU/8G, Nginx·FastAPI·Streamlit·Docker) · DB 2(4vCPU/16G, PostgreSQL) · Vector DB 1(**pgvector**) · AI 1(8vCPU/16G, LangChain·LangGraph·Prophet) · 36개월 36,000천원 | 사업계획서 2.4·4.7 (4.7 은 Qdrant 기재 — **D-18**) |
| 사업비 | 총 **398,510천원** (정부 199,255 / 부담금 163,255 / 현물 36,000). 개발비 200,000 · H/W 58,150 · S/W 50,000 · N/W 16,000 · 기타 2,360 · 클라우드 36,000 | 사업계획서 4.1 |
| 보안 | CISO 지정 · 역할별 계정 분리 · 최소 권한 · 비밀번호 복잡도·주기 변경 · 정기 보안 점검 · 외부 접속 최소화 · **VPN/암호화 통신** · 접근 로그 · 백업/DR · **폐쇄형 RAG Agent** | 사업계획서 9.2 — **TLS 버전·암호화 알고리즘·해시 미기재 (D-15)** |

**목표치가 없는 것**: RAG 응답시간 · 인용 정확도 · 그라운딩 임계값 · 시계열 동기화율 · 현황판 갱신 주기 · 백업 주기 · RTO/RPO · 보존기간 (D-09·D-10).
**다른 사업의 값(0.85 · TLS 1.2+ · AES-256)을 정본인 척 적지 않는다.**

## 환경

- PostgreSQL 17.10 (unix socket `/tmp:5432`), DB **`kyungdong_db`**, `vector` 0.8.6.
- `uv` 0.12.1 / Python 3.12(`/opt/homebrew/bin/python3.12`). 시스템 `python3`(3.9)는 쓰지 않는다.
- FastAPI + Jinja2 + psycopg. Docker 없음. Node 24 있음. **개발 서버 포트 8020** (8000·8010 은 점유 중).
- API 키 없음 → Agent 는 **501 "LLM 미구성"** 명시 렌더링(D-08). 조용한 폴백 금지. 임베딩 없으면 `tsvector_keyword` 모드 라벨.
- CAD 파서 — **Parsing 은 `KYUNGDONG_CAD_PARSER=dxf`(dwg2dxf 변환 + 자체 DXF 파서, D-123·D-235)로 돈다.** 내는 객체는 홀·치수문자 2종 · 신뢰도 없음 · DETECT_METHOD 는 `Parsing(dwg2dxf+DXF)`. Autodesk API·YOLOv8(Vision) 은 미확보 → **501 또는 명시 배지**(D-05), 정합성 검증 NULL. 조용히 합성 Feature 금지.
- 환경변수 접두 `KYUNGDONG_` (`.env.example`).
- 이웃 DB `kwangsung_db` `kkotsuni_db` `songwol_db` `koryo_solder_db` `mes_db` 는 **건드리지 않는다**.

## 명령

`make setup` `gen-schema` `db-schema` `db-seed` `db-reset` `run` `simulate` `cad-ingest` `test`
`check-routes` `check-trace` `check-schema` `check-data` `check-ingest` `check-ai` `check-security`
**`gate`**(전건 판정표) `gate-full`.

## 작업 원칙

- **지어내지 않는다.** 화면의 모든 칸은 산출물 문장이다. 없으면 비우고 `미확정 (D-nn)`. 목업의 `(예시)` 값은 시드에 넣지 않는다.
- **조용한 실패 금지.** `try/except` 로 하드코딩 결과를 끼워넣지 않는다. 실패는 사용자에게 보인다 (401/403/422/500/501/503).
- **한 파일은 한 사람만 만진다.** 소유권은 `goal.md` §3.4 → `contracts/screen-map.md` §2.
- **AI 는 조회·분석·추천·경고까지.** 설비 제어·자동 반영 금지. **견적 확정 · 발주 · 대체자재 승인 · 검사 합격 · 출하는 사람이 승인한 결과만 반영**하고 승인 이력을 남긴다.
- **RAG 는 폐쇄형** — 내부 데이터만, 외부 검색 0. 근거 0건이면 **LLM 을 부르지 않고 "검토 필요"**. 검색하지 않은 문서명·조항·페이지를 지어내지 않는다. 모든 질의·응답·근거·응답시간을 `AGT_QUERY_LOGS` 에 기록.
- **타 사업 오염 주의 (최대 위험) — 두 겹이다.**
  - ① 직전 사업 코드: 광성정밀(프레스·금형·샷카운트·전착·마킹·프론트쉘·**예지보전·불량예측**) · 평창김치(절임·발효·염도·pH) · 송월타월(와인딩·에이징·타월). `grep -rn "kwangsung\|kkotsuni\|songwol\|프레스\|금형\|샷카운트\|전착\|발효\|염도\|pH\|와인딩\|에이징\|예지보전\|불량예측" src/` = **0**.
  - ② **사업계획서 자체의 잔재** (실측): **포밍 10건 · 롤포밍 1 · 로봇용접 2 · 레토르트 1(p.58) · 판금 1 · 「금속 구조물/구조재」 2건**(2.5 p.19 · 2.7.4 p.32 · 10.1 p.56). 1.1 ETO 서술 및 CAD 아카이브 실측과 어긋난다 → **D-02, 채택하지 않는다.** 단 p.2 '멸균' 은 진공건조기 용도로 정당.
  - **이 회사 용어**: 반응기 · 교반기 · 진공건조기 · 누체필터 · 저장탱크 · Pure Steam Generator · ETO · 프로젝트(수주)번호 · 도면번호·리비전 · CAD Feature · 레이저 커팅 · 제관·용접(WPS) · 버핑 · 수압·기밀·진공 시험 · MTC(재질성적서) · ASME·KS · 외주 반출·반입.
  - 용어 검색으로 안 걸리는 것은 **내용으로 판단**한다. 반복 생산 라인·배치 중심 화면이면 그 자체가 오염이다 — 이 회사는 **다품종 소량 ETO** 다.
- 산출물 결함은 **고치지 말고** `decisions.md` 에 적고 도입기업에 제안한다.

## 이웃 프로젝트

- `../01 Analyze Agent` → `../02 Design Agent` → `../03 UI Agent` → `../04 QA Agent` → `../06 Coding Agent` — 산출물 파이프라인. 산출물은 여기서 고치지 않는다.
- `../08 AI Agent Making Agent/work-kyungdong-cockpit/kyungdong-globaltech-ai-agent/` — **같은 회사용 Agent 시안**(1,368줄). 도구 11종·프롬프트 11원칙·승인 규칙·`sample_docs/` 3종을 **이식·확장**한다(재작성 금지, goal.md §1.2).
- `../08 AI Agent Making Agent/onetouch-cad-quotation-agent-cockpit/` — 타사 CAD 견적 Cockpit. **구조만** 참고, 도메인 문자열 금지.
- `../KwangSung AI Platform` — 직전 사업. 골격·도구·계약 양식을 본뜨되 **도메인 문자열과 예지보전 코드는 가져오지 않는다**.
